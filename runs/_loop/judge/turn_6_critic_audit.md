VERDICT: FAIL

§Verdict on T5's mechanism: REJECTED (load-bearing Nambu-doubling claim is wrong).

§Corrected mechanism statement:
The 2D×2D Bose-Bogoliubov matrix H_bdg = [[L, M], [-M*, -L*]] is pseudo-Hermitian with respect to η = diag(I_D, -I_D) (i.e. η H_bdg η = H_bdg^†), so its spectrum has the symmetry (λ, -λ*): real eigenvalues come in ±λ pairs and complex eigenvalues come in quartets (±λ, ±λ*), with purely imaginary eigenvalues forming ±iΩ pairs (Castin-Dum 1998; Blaizot-Ripka 1986; Ronen-Bohn 2006). A dynamically unstable Bose mean field therefore produces *purely imaginary* (or genuinely complex) Nambu eigenvalues — the imaginary modes do NOT get re-cast as large positive reals by Nambu doubling. The actual 3000× offset must therefore arise either from (i) a small residual `real(ev)` of order the LAPACK conditioning of the non-Hermitian matrix slipping through `> 1e-10` and being amplified by a miscalibrated `mu_b^2/(2 ek)` UV-subtraction term, (ii) the genuine real ±|λ| pairs from real-but-negative stiffness eigenvalues (different physical origin than imaginary modes), or (iii) the per-branch `c_star = argmax(|u_part|²)` label being ill-defined on near-degenerate unstable eigenvectors, so the wrong diagonal `h_total[c*,c*]` enters `mu_b` and the UV cancellation `omega - ek - mu_b + mu_b²/(2ek)` no longer subtracts the correct large-k asymptote — yielding O(n^{5/2}) unregularized contributions summed over D modes × n_k k-points.

§Code-level prediction (line 230-231):
For F=6 polar with ~12/13 physically imaginary modes, the Nambu 2D=26 eigenvalues will consist of ~24 imaginary ±iΩ pairs (real parts at LAPACK noise floor ~1e-12 to 1e-8 depending on conditioning) plus ~2 real ±λ pairs from the remaining stable channel. The `> 1e-10` filter retains: (a) the 1 real positive partner from the stable pair, plus (b) an undetermined fraction of the imaginary modes whose numerically-computed `real(ev)` exceeds 1e-10 due to non-Hermitian LAPACK noise. The dominant 3000× contribution most likely comes from the miscalibrated `mu_b` UV-subtraction on the few imaginary modes that survive the filter — NOT from "large real Nambu partners of imaginary modes" as T5 claims.

§Recommended fix shape:
Escalate to theorist. The audit FALSIFIES T5's Nambu-doubling mechanism (C1, C2), which means T5's specific F-α gate ("count imaginary fraction of upper-D eigenvalues") is built on a wrong picture and may or may not be empirically curative for the wrong reason. F-γ (auto-dispatch to PolarContactLHY when GS purity > threshold, plus retaining the existing `@warn` for `:full_bdg` on F=6 polar) is the safest *operational* path documented in `full_bdg_F6_polar_broken.md` already, but a clean F-β fix needs the theorist to (1) derive what `eigen(H_bdg)` actually returns for the Bose pseudo-Hermitian matrix at an unstable GS, (2) decide whether to diagonalize η·H_bdg in the proper symplectic basis (Colpa 1978 / Blaizot-Ripka) instead of raw `eigen()`, and (3) re-derive `mu_b` for genuinely unstable modes (or prove it is undefined and the LHY integral must be gated to zero).

§Citations referenced:
- T5's cached refs (Lima-Pelster 2011/2012, Petrov 2015, Bisset 2016, UKU 2010, Zhang 2023) all describe the "drop imaginary part" convention but none of them claim Nambu doubling converts imaginary physical eigenvalues into real Nambu eigenvalues. T5 introduces this claim without citation.
- Standard Bose-Bogoliubov pseudo-Hermitian structure: Castin-Dum 1998, Blaizot-Ripka "Quantum Theory of Finite Systems" 1986, Colpa Physica A 93, 327 (1978). [Not in T5 cache; flagged as load-bearing for the corrected mechanism — theorist should pull these.]

§Confidence (per claim):
- T5 mechanism wrong (C1, C2): high. The (λ, -λ*) pseudo-Hermitian symmetry is textbook and contradicts "imaginary becomes real."
- Actual root cause is `mu_b` miscalibration + LAPACK noise: medium. Plausible from code structure but not proven without running the diagnostic.
- F-γ is the safest interim path: high. Already empirically validated by the existing closed-form modes and `@warn`.
- F-α as a *correct* fix: low. May happen to work because it gates F=6 polar to zero, but the stated mechanism is wrong, so it would be load-bearing on a false premise for any paper writeup.

RECOMMENDED ACTION: Reject, return to theorist with: "Derive the eigen-spectrum of H_bdg = [[L,M],[-M*,-L*]] for a Bose mean-field-unstable GS. Specifically, prove or disprove: (a) does `eigen(H_bdg)` for a state with ~12 imaginary physical Bogoliubov pairs produce ~24 large-positive-real eigenvalues (T5's claim), or ~24 imaginary eigenvalues preserving the iΩ structure (textbook claim)? (b) For the surviving modes that pass `real(ev) > 1e-10`, is `mu_b = ek + n0·h_total[c*,c*] - mu + zee[c*]` the correct UV asymptote, or does it need a symplectic-basis quasi-particle weight? Provide the citation chain (Castin-Dum / Colpa / Blaizot-Ripka) for the pseudo-Hermitian eigenstructure."
