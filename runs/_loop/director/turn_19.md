---
turn: 19
subagent: director
topic_tags: [barnett, scenario-c-aftermath, orbital-channel, lz-fz-bias, theorist-extension, full-ddi-coherent, pre-julia-mechanism-lock]
paper_section: null
depends_on: [11, 13, 14, 15, 16, 17, 18, "sim/turn_18.md §5 Scenario C verdict", "theorist/turn_17.md §8 Q8.3 / §12 Scenario C path", "judge/turn_18.json physical_red_flags"]
produces: "theorist directive to extend T17 spin-only single-particle Lindblad with the trapped orbital channel that T11 §2.4 named (-Ω(L_z+F_z) rotating-frame bias) AND/OR the full-DDI off-diagonal coherent coupling that T11 §2.8 named — derive WHICH mechanism flips Δ⟨F_z⟩(±Ω) sign from spin-only +4.82 (Scenario C) to empirical -4.60; produce a falsifiable closed-form τ_Barnett(Ω,p_z,p_perp,c_dd,F) for the trapped+weak-field+full-DDI regime that the 22:00 JST julia γ_dr=0 + c_dd=0 control runs can decisively test."
---

# Turn 19 — Director Report

## 1. Project state snapshot

- **Barnett campaign turn 9 of an extending arc.** T11 (rank-1 wrong) → T12 (critic rank-2 fix) → T13 (sympy τ_cascade 78ms, factor 5-11x wrong) → T14 (Born-Markov disconfirmed; p_z=0.315 authoritative) → T15 (cascade Candidate C refuted) → T16 (critic identified missing coherent p_perp Rabi channel) → T17 (theorist coupled Lindblad with H^rot = -(p_z-Ω)F_z - p_perp F_x; closed-form τ_asym=4.9ms; γ_dr=0 prediction Δ=5.96) → **T18 numerical integration of T17 eq(T1) at γ_dr=0.02 produced SCENARIO C: spin-only Lindblad gives Δ=+4.82 (wrong sign — empirical is -4.60); endpoint errors 100% and 1032% on individual sides — spin-only single-particle Lindblad is REFUTED for late-time asymmetry direction.** T18 also revealed a sign error in T17 eq(12) (dFy/dt|_0: T17 wrote -p_perp·F, correct is +p_perp·F; doesn't affect τ_asym closed form because cancels in Δ but is a verified theorist-paper bug).
- **T18 simultaneously validated the γ_dr=0 prediction** (Δ=5.95 numerical vs 5.96 T17 closed form, max ODE deviation 2.68e-11) — so T17 framework is CORRECT in its claimed scope (early-time coherent Rabi) but INSUFFICIENT for the empirical late-time observation. The decisive missing physics is the mechanism that flips ⟨F_z⟩(+Ω) from numerical 0.040 → empirical 5.02 and ⟨F_z⟩(-Ω) from numerical 5.99 → empirical 0.42. T17 §8 Q8.3 / §12 Scenario C / T11 §2.4 all named two candidates: (i) **trapped orbital -Ω(L_z+F_z) rotating-frame energetic bias** — many-body DOF the spin-only single-particle Lindblad cannot see; (ii) **full-DDI off-diagonal coherent Q_{xy/xz/yz} coupling** (T11 §2.8) — Yan-Li-Saito-style trapped m+v=ℓ analog with N-body coherence.
- **The 22:00 JST julia window opens in ~4.7h** (per scheduler T19: window ends at 22:00 JST 2026-05-16, 16910s = 281min left of TEXT_ONLY). The decisive julia tests are TWO controls: γ_dr=0 (does the asymmetry survive at zero dissipation? — T17 already predicts Δ=5.95 at γ_dr=0 in spin-only; many-body DDI dephasing may bring this to [3, 5.96]) AND c_dd=0 (does the asymmetry survive at zero DDI? — if YES, full-DDI off-diagonal coupling is NOT the mechanism; if NO, full-DDI is the mechanism). seed.md §"Specific data targets" line 77-82 already named these intermediate p and intermediate Ω sweeps. **For the julia runs to be decisively interpretable, the theorist must produce — BEFORE 22:00 JST — a falsifiable closed-form prediction of Δ⟨F_z⟩(±Ω) under each candidate mechanism, so that the c_dd=0 / γ_dr=0 julia data can be cleanly classified.** This is the seed.md essence §2 deliverable.
- **Scheduler T19 (`runs/_loop/_local/scheduler_19.json`)**: policy `TEXT_ONLY`, decision `go`, allowed_workloads = theorist / researcher / critic / implementer_text / implementer_sympy / noop. Probe is JULIA_GPU_OK (12579 MB VRAM free, 1% util, no foreign julia) but window-policy lock holds until 22:00 JST. Window seconds left 16910 = 4h 41m. After 22:00 JST → PROBE_DRIVEN sleep window where julia becomes possible. ✓
- **Drift signals (T18 history)**: topic_repetition=0.083 (each turn moved a sub-issue), subagent_repetition=0.333, **manuscript_delta_zero=1.0 (RED — DRIFT_MANUSCRIPT_DELTA_ZERO escalation = director_must_address per §B6)**, code_delta_zero=0.0 (T18 modified runs/_loop/sim), **verdict_drift=0.6 (still YELLOW plateau, but T18 FAIL_PHYSICS is verdict-class so will reset within 2-3 turns)**, cost_inflation=1.165 (yellow advisory), novel_claim_zero=0.0. DRIFT_MANUSCRIPT_DELTA_ZERO + DRIFT_COST_INFLATION advisories active. §B6 requirement: "this turn MUST advance a manuscript chapter (or explain why deferring is correct)." **Explicit defer**: seed.md line 60 verbatim "Manuscript polish remains OUT of scope (per `feedback_manuscript_is_not_the_essence`)." The campaign goal is closed-form τ_Barnett that data validates (Tier 1→2 lift), not manuscript drafting. Per `feedback_manuscript_is_not_the_essence.md` and seed.md line 96-108 "essence", the right move is theory grounded in the new T18 Scenario C constraint, NOT a manuscript chapter. Drift advisory acknowledged and deferral justified.
- **Subagent rotation last 8 turns**: T11 theorist / T12 critic / T13 implementer_sympy / T14 researcher / T15 implementer_sympy / T16 critic / T17 theorist / T18 implementer_sympy. Implementer_sympy ran T13+T15 (2-in-a-row consumed at T15) + T18 (1 of 2 again). Theorist last ran T17 (2 turns ago). Critic last T16 (2 turns ago). Researcher T14 (4 turns ago). **Theorist is NOT 2-in-a-row** (T17→T18 implementer intervened); next theorist dispatch is rotation-clean.

## 2. Recent-turn audit (last 3)

| Turn | Topic | Verdict | Value delivered | Was it right? |
|---|---|---|---|---|
| T16 | critic audit of cascade framework | CRITIC_WEAK_PASS | Identified missing coherent p_perp Rabi channel; named numerical thresholds; concrete theorist brief for T17; γ_dr=0 julia falsifier config | YES — exact §B4 anti-pattern intervention |
| T17 | theorist coupled Lindblad derivation | NOOP (self-directive) | Coupled coherent+dissipative Lindblad in Ω-rotating frame; H^rot = -(p_z-Ω)F_z - p_perp F_x; t⁴ closed form τ_asym=4.9 ms; γ_dr=0 prediction Δ=5.96; 5 consistency checks PASS; explicit T18 deferral §6 Q8.1 | YES — settled the framework but EXPLICITLY admitted in §8 Q8.3 that the sign-reversal mechanism is missing; deferral was justified |
| T18 | implementer_sympy numerical integration of T17 eq(T1) | FAIL_PHYSICS | Numerical confirms T17 γ_dr=0 (max dev 2.68e-11 from eq 14) AND refutes T17 at γ_dr=0.02 (Δ=+4.82 wrong sign vs empirical -4.60); endpoint errors 100%/1032%; T17 eq(12) sign error discovered; tau_asym Omega-fit α=0.171 vs predicted 0.25 (Taylor radius violated); Scenario C definitively classified | **YES — this is the cleanest D1 outcome of the campaign**: a precise framework boundary verified numerically. The spin-only single-particle Lindblad is settled (correct in scope, insufficient for empirical regime). Sets up clear T19 theorist directive. |

**Trajectory check (§B4)**: T16→T17→T18 = 3 different subagents (critic / theorist / implementer). Topic_repetition stays low (0.083). **Verdict_drift now 0.6 yellow but T18 FAIL_PHYSICS broke the NOOP/WEAK_PASS streak with a verdict-class outcome.** T19 theorist with a falsifiable directive can produce another verdict-class outcome (FAIL_PHYSICS if the derivation fails closed-form, PASS if a coherent closed form emerges that the 22:00 JST julia can test).

**Specific value of T18 outcome**: T18's Scenario C is the cleanest D1 contraction of the hypothesis space in the campaign. We now know: (a) coherent Rabi + rank-2 cascade alone CANNOT produce the empirical sign-asymmetry; (b) magnitude is roughly right at γ_dr=0.02 (|Δ|=4.82 vs empirical 4.60, within 5%); (c) the spin-only single-particle Hilbert is structurally insufficient — the mechanism must involve EITHER orbital DOF (L_z) coupling to F_z OR many-body DDI coherence (Q_{αβ}). This is exactly the "physical sign-flip requires the orbital rotating-frame energetic bias -Ω(L_z+F_z) to actively protect the +Ω side from cascade" claim T18 §7 made, but it's still a CLAIM — derivation is pending.

## 3. Bottleneck analysis

Filtered to TEXT_ONLY allowed_workloads, ranked by leverage. T18 closed the "spin-only Lindblad numerical" gap and opened a clean fork: derive the sign-flip mechanism.

### B-1: theorist — extend T17 (T1) with the trapped orbital -Ω(L_z+F_z) coupling, derive closed-form Δ⟨F_z⟩(±Ω) including L_z DOF

*Issue*: T17 §8 Q8.2 explicitly deferred this to "a later turn (post-22:00 JST julia outcome)." But T18 Scenario C makes this PRE-julia critical: without a closed-form prediction under the orbital-coupling hypothesis, the 22:00 JST julia run can only confirm γ_dr=0 (Δ ≈ 5.95) — which T17 already predicts — and CANNOT distinguish between orbital-coupling vs full-DDI-coherent mechanisms. T11 §2.4 eq (2) wrote the rotating-frame Hamiltonian as H_rot = -Ω(L_z+F_z) + ... — this is the lab-Zeeman energy in the rotating frame. The crucial physics: at +Ω, the |L_z=+ℓ, F_z=+F⟩ states are energetically LOWERED (since -Ω·(ℓ+F)<0 for Ω>0, ℓ+F>0), so cascade pulling F_z DOWN simultaneously RAISES (-Ω)·L_z·F_z because L_z stays put (DDI angular momentum conservation locks total L_z+F_z). Thus the +Ω side has a thermodynamic ENERGETIC BIAS protecting F_z from cascade.

*Category*: physics gap (D3 dominant — research-grounded new theory; D1 secondary verification).

*Leverage*: **5**. Cost ~1.5M effective (typical theorist turn). Value:
- Produces the closed-form prediction needed to interpret the 22:00 JST julia γ_dr=0 + c_dd=0 control runs. Without this, julia data is ambiguous.
- Directly addresses seed.md priority Q1 ("Closed-form derivation of τ_Barnett(Ω, p, c_dd, c_1, F)... Does it match the empirical 7-14 ms?") — T17 produced the spin-only version which T18 refuted; T19 produces the orbital-extended version which the julia data tests.
- Directly addresses seed.md priority Q2 ("Mechanism audit: is the secular-DDI suppression picture rigorous? Could the activation come from c_1 effect, K3 dissipation, trap geometry, or resonance crossing?"). The orbital-Zeeman bias mechanism is a specific candidate WITHIN the secular-suppression framework: at strong Bz (Klaus), p_z·F_z >> -Ω(L_z+F_z), so the orbital bias is negligible; at weak Bz (current setup), Ω~p_z, so -Ω(L_z+F_z) competes with p_z·F_z and becomes load-bearing.
- Sets a clear falsifier for the 22:00 JST julia c_dd=0 control: if the orbital-Zeeman mechanism is correct, then c_dd=0 should still produce the asymmetry (because L_z+F_z conservation comes from the trap rotation, not from DDI per se). If c_dd=0 KILLS the asymmetry, then full-DDI off-diagonal Q_{αβ} is the actual mechanism. Both are testable with a single julia run pair.

*What moves it*: **theorist** with brief specifying: extend T17 eq(T1) to spin × orbital Hilbert (truncated at first few |L_z⟩ modes, say |0⟩, |±1⟩, |±2⟩); add -Ω(L_z+F_z) coupling; derive Δ⟨F_z⟩(±Ω) closed form OR identify the conservation law(s) that lock L_z+F_z; predict the dependence on (Ω, p_z, c_dd, c_1, F); state where T11 §2.8 full-DDI off-diagonal Q_{αβ} fits as alternative or complementary mechanism; produce 3-bin julia-falsifier predictions (γ_dr=0 alone, c_dd=0 alone, BOTH zero) that the 22:00 JST runs can cleanly test.

### B-2: researcher — pull Stamper-Kurn / Kawaguchi-Ueda / Hamada-Kambe rotating-frame results on orbital-spin coupling in trapped condensates

*Issue*: seed.md line 51-53 explicitly named this as a researcher role. T17 §2.4 cited Sakurai spin-coherent and KU 2012 / SKU 2013 rank-2 cascade. But the **trapped + rotating-frame + orbital-spin coupling** literature is not yet pulled (T14 covered Born-Markov + Hensler Cr); T11 §2.4 wrote -Ω(L_z+F_z) without a citation chain.

*Category*: docs/research gap.

*Leverage*: **3**. Cost ~1.0M. Value: provides citations + analytical scaffolding for theorist. But theorist can derive -Ω(L_z+F_z) from first principles (it's just lab-frame H = -p·F_z - Ω·L_z transformed by R(t)=exp(-iΩt(L_z+F_z))). The literature pull is value-add but NOT blocking — theorist B-1 can proceed without it and call out specific researcher queries for T20 if needed.

### B-3: critic — audit T18 §7 mechanism claim "physical sign-flip requires the orbital rotating-frame energetic bias -Ω(L_z+F_z) to actively protect the +Ω side"

*Issue*: T18 §7 stated this claim but did not derive it. T17 §8 Q8.3 originally raised the orbital mechanism as a hand-wave and T16 critic flagged that hand-waves require derivation. T18 has now numerically confirmed the spin-only Lindblad is insufficient — but the "orbital protection" mechanism is still asserted, not derived.

*Category*: verification gap.

*Leverage*: **2**. Cost ~0.8M. Risk: critic alone cannot derive the orbital extension — it can only flag the mechanism claim as still unaudited. **The actual move is for theorist to derive it (B-1); critic can audit the derivation post-hoc on T20 if needed.** Lower leverage than B-1.

### B-4: implementer_sympy — numerically extend T18 with orbital DOF (spin ⊗ first few |L_z⟩ modes)

*Issue*: Could numerically integrate a spin ⊗ orbital Hilbert (e.g. 13 × 5 = 65 dim) with -Ω(L_z+F_z) coupling and see if the sign-flip emerges.

*Category*: physics gap (D1 next iteration).

*Leverage*: **2**. Risks: (a) without a theorist directive on which orbital modes matter and what couples L_z to F_z (the DDI off-diagonal Q_{αβ}? the trap rotation alone? the single-particle Coriolis?), implementer is guessing — wrong tool for the question. (b) Implementer_sympy ran T13+T15+T18 (3 of last 6 turns); rotation is fine but theorist intervention is the right next step. **Reject — wrong sequencing**, equivalent to a Phase-2 attempt at numerical without the Phase-1 framework. The right shape is: theorist B-1 → implementer_sympy B-1' (T20) → julia confirmation (T21+ after 22:00 JST).

### B-5: noop until 22:00 JST

*Issue*: 4.7h wait until julia window opens. The decisive empirical test (γ_dr=0 + c_dd=0 controls in julia) is the gold standard.

*Category*: meta.

*Leverage*: **1**. Wastes 4.7h. Critically, without a theorist closed-form prediction for the orbital-extended framework, julia data is ambiguous (γ_dr=0 alone confirms T17 spin-only Rabi but not orbital; c_dd=0 alone is uninterpretable without prediction). **B-1 theorist for 4h MAXIMIZES the value of the 22:00 JST julia run by producing falsifiable predictions for it.** Reject noop.

### B-6: implementer_text (premature docstring or `@info` advisory)

*Issue*: Could add `@info` in `src/hamiltonian/interactions/secular_ddi.jl` documenting the Klaus-secular-suppresses-Barnett regime boundary.

*Category*: docs gap.

*Leverage*: **0**. seed.md line 54-58: "implementer_text turns: write a docstring ... documenting the Klaus-secular-suppresses-Barnett regime boundary — but ONLY after the theory is in place — not as the primary turn shape." Theory is NOT in place (T18 just refuted half of it). Premature. seed.md line 60 + feedback_manuscript_is_not_the_essence.md — explicit deferral. **Reject.**

## 4. Strategic options for THIS turn

| # | Move | Subagent | Now-or-later | Cost |
|---|---|---|---|---|
| 1 | **theorist: extend T17 (T1) with orbital -Ω(L_z+F_z) coupling; derive closed-form Δ⟨F_z⟩(±Ω) with L_z DOF; produce 3-bin julia-falsifier predictions for γ_dr=0 / c_dd=0 / both-zero** | **theorist** | NOW — must precede 22:00 JST julia window for julia to be decisive | ≤ 30 min, ≤ 1.7M |
| 2 | researcher Stamper-Kurn / KU / Hamada-Kambe orbital-spin coupling | researcher | LATER if B-1 calls specific queries | ≤ 15 min, ≤ 1.0M |
| 3 | critic audit T18 §7 mechanism claim | critic | LATER (T20 post-theorist) | ≤ 15 min, ≤ 0.8M |
| 4 | implementer_sympy 65-dim spin⊗orbital integration | implementer | rejected — needs theorist framework first | ≤ 20 min, ≤ 1.3M |
| 5 | noop until 22:00 JST | n/a | rejected — wastes window pre-prediction value | 0 |
| 6 | implementer_text docstring | implementer | rejected — theory not in place per seed.md | ≤ 10 min |

**Pick: Option 1 (theorist).**

Why:

- **§A5 axis (c) — research-grounded new theory**: T11 §2.4 and §2.8 named two candidate mechanisms (orbital -Ω(L_z+F_z) bias; full-DDI Q_{αβ} coherent coupling). T17 deferred both to "later turn." T18 Scenario C now demands one. T19 theorist closed-form derivation IS axis-(c). ✓
- **§A5 axis (a) — verify existing-implementation claim**: T11 §2.4 eq(2) is in the theorist record without a derivation chain (T16 critic flagged it as "assertion not derivation"; T17 §8 Q8.3 re-imported it as a hand-wave; T18 numerically confirmed spin-only insufficient). T19 either derives -Ω(L_z+F_z) cleanly or refutes it (driving the campaign to full-DDI Q_{αβ}, T11 §2.8). Either is verification depth lift. ✓
- **§B3 theorist dispatch rule (verbatim)**: "dispatch when the bottleneck needs a new derivation / sign argument / BCH analysis / sympy-backed closed form that no existing source provides." T11 §2.4 stated the rotating-frame Hamiltonian H_rot = -Ω(L_z+F_z) but never derived the resulting Δ⟨F_z⟩(±Ω). T17 §8 Q8.2 admitted this is open. Theorist is the right tool. ✓
- **§B4 rotation favorable**: theorist last ran T17 (2 turns ago — T18 implementer_sympy intervened). NOT 2-in-a-row. Implementer_sympy has run T13+T15+T18 (3 of last 6); critic T12+T16 (2 of last 8); researcher T14 (4 ago). Theorist is the natural sequencing per T17 §8 Q8.2 deferral and T18 §7 mechanism claim. ✓
- **§B6 DRIFT_MANUSCRIPT_DELTA_ZERO**: acknowledged. Defer to physics — seed.md line 60 explicit "Manuscript polish remains OUT of scope." Campaign deliverable per seed.md "essence" §1 is closed-form τ_Barnett that data validates (Tier 1→2 lift), NOT manuscript drafting. Theorist B-1 IS the campaign's primary value axis. ✓
- **§B6 DRIFT_COST_INFLATION**: yellow advisory at 1.165. Theorist typical 1.5M effective. Within cap (3M hard, 1.7M expected). ✓
- **§B7 quota budget**: theorist ~1.5-1.7M. Window seconds left 16910 = 4h 41m → plenty for ≤30-min turn. ✓
- **§B8 scheduler compliance**: theorist ∈ allowed_workloads; TEXT_ONLY (pure derivation, no julia, no GPU). ✓
- **§D1 dominant (verification depth axis)**: Tier-1.5 → Tier-2 lift on the orbital mechanism specifically. T18 ruled out spin-only-only; T19 derives orbital extension; 22:00 JST julia (Tier-3 candidate) tests it.
- **§D3 (research-grounded new theory)**: this is the campaign's theoretical core — what mechanism produces the empirical 4.60 sign-asymmetry? T19 derivation is the load-bearing theory step.
- **seed.md fit**: priority Q1 (closed-form τ_Barnett — orbital-extended) + Q2 (mechanism audit — orbital vs full-DDI alternatives derived as separable hypotheses) + Q3 (cross-validation framework — Yan-Li-Saito m+v=ℓ analog for trapped). seed.md line 49-52 "theorist turns: derive τ_Barnett scaling, write down the DDI off-diagonal coupling matrix element that drives the pumping, predict signatures at other (Ω, p) values." T19 IS this turn shape, now informed by T18 Scenario C.
- **`feedback_manuscript_is_not_the_essence.md`**: real bug-finding + theory tied to anko's experiments — exactly the right shape. ✓
- **`feedback_cost_overhead_is_the_cost.md`**: don't deliberate, dispatch. ✓
- **Drift-escalation `director_must_address` for DRIFT_MANUSCRIPT_DELTA_ZERO**: addressed in §1 last bullet and §3 B-1 leverage justification (manuscript is explicitly out of scope per seed.md, deferral is correct).

Why NOT Option 2 (researcher): theorist can derive -Ω(L_z+F_z) and its implications from first principles (it's a standard rotating-frame transformation of H = -p·F_z + H_trap with R(t) = exp(-iΩt(L_z+F_z))). Researcher would provide citations but not unblock. If theorist needs literature backing, T20 can be researcher.

Why NOT Option 3 (critic): cannot derive the orbital mechanism, only audit. If T19 theorist produces a load-bearing closed form, T20 critic can audit it. Right sequencing is theorist FIRST.

Why NOT Option 4 (implementer_sympy 65-dim): needs framework input. Theorist B-1 specifies which orbital modes matter (likely L_z ∈ {0, ±1, ±2}), what couples them to F_z (the DDI Q_{αβ} off-diagonal? trap rotation alone? Coriolis?), and the conservation law to test (L_z + F_z?). Without theorist, implementer guesses → wrong tool.

Why NOT Option 5 (noop): wastes 4.7h before julia. Without a theorist prediction, the 22:00 JST julia run can only validate T17 spin-only at γ_dr=0 (which T18 already established) — the c_dd=0 control becomes uninterpretable. Theorist B-1 produces falsifiable predictions for BOTH controls so julia is decisive.

## 5. Calibrated progress check

| Axis | Status | Evidence |
|---|---|---|
| Physics completeness (D1+D3) | **at risk — but T19 theorist closes the gap** | T18 refuted spin-only single-particle Lindblad for sign-asymmetry direction at γ_dr=0.02 (Scenario C). T11 §2.4 + §2.8 named two mechanism candidates (orbital -Ω(L_z+F_z); full-DDI Q_{αβ}) — neither derived. T19 theorist closes this. |
| Verification depth (D1 dominant) | **lifted from Tier-1.5 → Tier-1.5 with refined boundary**. T18 produced cleanest D1 result of campaign | spin-only Lindblad: numerically validated within scope (γ_dr=0 max dev 2.68e-11), refuted in extended-regime (γ_dr=0.02 sign wrong). Boundary now precisely known. Tier-3 lift comes after T19 derivation + 22:00 JST julia confirmation. |
| Manuscript (de-prioritized per seed.md L60 + feedback_manuscript_is_not_the_essence.md) | **deliberately deferred (drift advisory acknowledged)** | DRIFT_MANUSCRIPT_DELTA_ZERO escalation = director_must_address; addressed by explicit deferral per seed.md (essence is physics not manuscript polish). 3 unmerged T7-T10 branches still await anko-merge but not this turn. |
| Reproducibility | **on track** | T18 produced reproducible artifact runs/_loop/sim/turn_18/{coupled_lindblad_integration.py, fz_trajectories.png, trajectories.csv} with 23 jump ops + 5×3=15 trajectory dataset. T19 will produce theorist md following established convention. |
| Loop infrastructure | **healthy** | verdict_drift 0.6 yellow but T18 FAIL_PHYSICS broke 3-turn NOOP/WEAK_PASS streak with verdict-class outcome. T19 theorist with falsifiable directive → another verdict-class outcome likely. cost_inflation 1.165 yellow but T19 theorist 1.5-1.7M is within cap. Rotation: theorist clean (T17→T18 intervention). seed.md "≤2 same-subagent in a row" honored. |

**Mark**: T19 is the pivot from "spin-only Lindblad numerical-refuted" to "orbital-extended theory derived". After T19:
- If theorist produces clean closed-form Δ⟨F_z⟩(±Ω; Ω, p_z, c_dd, F) with the orbital channel → PASS, T20 = implementer_sympy 65-dim numerical confirm OR noop until 22:00 JST → julia γ_dr=0 + c_dd=0 controls tests.
- If theorist finds the orbital -Ω(L_z+F_z) bias is insufficient (e.g., the sign-flip requires full-DDI Q_{αβ} too) → useful FAIL_PHYSICS, T20 = theorist for T11 §2.8 full-DDI version OR researcher for Yan-Li-Saito m+v=ℓ adaptation.
- If theorist refuses to derive without literature backing → researcher T20.

## 6. Dispatch decision

```json
{
  "subagent_type": "theorist",
  "rationale": "T18 numerical integration (`runs/_loop/sim/turn_18.md` §5 + `runs/_loop/judge/turn_18.json` physical_red_flags) definitively established SCENARIO C: T17 spin-only single-particle Lindblad (eq T1) gives Δ⟨F_z⟩(±Ω)=+4.82 at γ_dr=0.02 (wrong sign — empirical is -4.60), with individual endpoint errors 100% / 1032%. T17 §8 Q8.3 explicitly identified the missing channel as 'rotating-frame energetic bias -Ω(L_z+F_z) protects +Ω side from cascading' but did NOT derive it; T11 §2.4 wrote this Hamiltonian without a derivation chain (T16 critic flagged as 'assertion not derivation'). T18 §7 stated the mechanism claim but only as interpretation. T11 §2.8 also named full-DDI off-diagonal Q_{αβ} coherent coupling as an alternative. The 22:00 JST julia window opens in 4.7h (scheduler T19: 16910s until PROBE_DRIVEN); for the planned julia γ_dr=0 + c_dd=0 control runs to be DECISIVELY interpretable (Tier 2→3 lift), a falsifiable closed-form prediction under EACH candidate mechanism must exist BEFORE julia fires. T19 theorist is the only subagent that can produce this. §A5 axis (c) research-grounded new theory + axis (a) verify existing-implementation claim. §B3 theorist dispatch rule: 'new derivation / sign argument that no existing source provides' — exactly this. §B4 rotation clean: theorist T17 was 2 turns ago, T18 implementer_sympy intervened. §B6 DRIFT_MANUSCRIPT_DELTA_ZERO addressed by explicit seed.md L60 + feedback_manuscript_is_not_the_essence.md deferral (campaign essence is closed-form τ_Barnett that data validates, not manuscript polish). §B7 cost: theorist ~1.5-1.7M within 3M cap, cost_inflation 1.165 yellow advisory but mid-cost is acceptable for highest-leverage move. §B8 scheduler: theorist ∈ allowed_workloads, TEXT_ONLY (pure derivation). §D1+D3 dominant. seed.md priority Q1+Q2+Q3 all addressed by orbital-extended τ_Barnett closed form with mechanism-discriminating predictions.",
  "brief": "## Goal\n\nExtend T17's single-particle spin-only Lindblad (eq T1) to include the trapped-orbital DOF that T11 §2.4 named (-Ω(L_z+F_z) rotating-frame Zeeman bias) AND/OR the full-DDI off-diagonal coherent coupling that T11 §2.8 named (Q_{xy/xz/yz} terms). Derive a closed-form prediction for Δ⟨F_z⟩(±Ω) in the extended framework that EXPLAINS the empirical sign-asymmetry (+Ω→5.02, -Ω→0.42, Δ=-4.60) which T18 proved spin-only-only cannot.\n\nPRIMARY DELIVERABLE: a falsifiable 3-bin prediction table for the 22:00 JST julia control runs:\n\n| Run | γ_dr | c_dd | Predicted Δ⟨F_z⟩(±Ω) under mechanism M1 (orbital) | Predicted Δ under M2 (full-DDI) | Predicted Δ under both (M1+M2) |\n|---|---|---|---|---|---|\n| empirical | 0.02 | nominal | -4.60 (observed) | -4.60 | -4.60 |\n| γ_dr=0 control | 0 | nominal | ? | ? | ? |\n| c_dd=0 control | 0.02 | 0 | ? | ? | ? |\n| both zero | 0 | 0 | ? | ? | ? |\n\nThis lets the julia data on the night of 2026-05-16/17 cleanly discriminate between mechanism candidates.\n\n## Context to read (in priority order)\n\n1. `runs/_loop/sim/turn_18.md` §5 Headline + §7 Falsification + §8 Pre-predictions — the new numerical constraint set. NOTE the T17 eq(12) sign error T18 §6 documented; do not re-import it.\n2. `runs/_loop/theorist/turn_17.md` — entire derivation, especially:\n   - §0 conventions (units, spinor ordering, p_z=0.315, p_perp=0.220, Ω=±0.5, γ_dr=0.02)\n   - §2.3 eq (T1) — the spin-only Lindblad you are extending\n   - §2.4 — rotating-frame quantization-axis tilt angle β(Ω); the angle that controls the asymmetry\n   - §4.3 — what T17 admitted was missing (state-conditioned dissipator AND trap orbital coupling)\n   - §8 Q8.2 + Q8.3 — the orbital-channel deferral and mechanism hand-wave\n   - §12 Scenario A/B/C — Scenario C now confirmed by T18\n3. `runs/_loop/theorist/turn_11.md` §2.4 eq (2) (rotating-frame Hamiltonian -Ω(L_z+F_z)) and §2.8 (full-DDI off-diagonal Q_{αβ} coherent coupling) — the two mechanism candidates. These were T11 hand-waves; you derive at least one rigorously.\n4. `runs/_loop/judge/turn_16_critic_audit.md` §3 Findings — what T16 critic flagged as needing derivation (assertion-grade claims about γ_dr being THE rate and rotating-frame bias).\n5. `runs/_loop/seed.md` — campaign priorities Q1/Q2/Q3 + §'Specific data targets for julia phase (≥22:00 JST)' line 77-82.\n6. Memory file `barnett_spin_pumping_observed_2026_05_16.md` — empirical observation context (Bz~2.6nT weak-field, ε_dd≈0.55 trapped regime, mechanism hypothesis).\n7. Memory file `yan_li_saito_2026_barnett_paper.md` — droplet m+v=ℓ free-space analog (different regime, but conceptual scaffold).\n8. T18 eq(12) verification: T18 §6 found T17 eq(12) wrote dFy/dt|_0 = -p_perp·F where correct is +p_perp·F (a sign error). Do not propagate it.\n9. `runs/_loop/judge/turn_12_critic_audit.md` — the rank-2 CG vs rank-1 lesson (T11's first mechanism error). Use T15 §5 Target 1 W^CG table as canonical.\n\n## Derivation targets (priority order)\n\n### Target T1 (HIGH): Lab-frame Hamiltonian with trap + orbital + DDI\n\nWrite the lab-frame Hamiltonian for the single-atom problem (trap + Zeeman):\n$$H^{\\rm lab}(t) = -p_z F_z - p_\\perp[\\cos(\\Omega t) F_x + \\sin(\\Omega t) F_y] + H_{\\rm trap}(\\vec r) + V_{\\rm DDI} + \\text{LHY} + K_3\\text{loss}$$\nwhere H_trap is the harmonic trap (isotropic or anisotropic — anko's config). State the trap frequency parameter clearly (specify ω_trap vs ω_ref).\n\n### Target T2 (HIGH): Rotating-frame transformation including orbital DOF\n\nUse R(t) = exp(-iΩt(L_z + F_z)) (NOT just exp(-iΩtF_z) as T17 did). Show that this transformation:\n- Cancels the rotating Bx/By terms in the lab Zeeman (same as T17 §2.2).\n- Adds -Ω(L_z + F_z) to the rotating-frame Hamiltonian (T11 §2.4 eq 2).\n- Leaves H_trap invariant ONLY if trap is rotationally symmetric about z (or transforms it for anisotropic traps).\n- Acts on DDI Q_αβ kernel: V_DDI ~ c_dd ∫∫ Q_αβ(r-r') F_α F_β / |r-r'|³. Show how Q_αβ transforms under R(t). Specifically write out which Q_αβ components are STATIC in rotating frame (Q_zz, secular sum), which OSCILLATE at ±Ω (Q_xz, Q_yz), and which OSCILLATE at ±2Ω (Q_xy, Q_xx-Q_yy). This is the full-DDI off-diagonal coupling T11 §2.8 referenced.\n\n### Target T3 (HIGH): Identify conservation laws in rotating frame\n\nIn the rotating frame, the coherent generator is\n$$H^{\\rm rot} = -(p_z - \\Omega) F_z - p_\\perp F_x - \\Omega L_z + H_{\\rm trap} + V^{\\rm rot}_{\\rm DDI}$$\nIdentify which quantities are conserved:\n- [H^rot, L_z + F_z] = ? In the strict secular DDI limit (only Q_zz contributes), the off-diagonal F_x perturbation breaks this for c_1≠0 → not conserved exactly, but approximately conserved for p_perp small.\n- In the full-DDI limit, the off-diagonal Q_αβ couples L_z to F_z via dipolar relaxation → exact conservation only for total J_z = L_z + F_z (the rotation generator).\n- Compare to Yan-Li-Saito's m+v=ℓ which is angular momentum conservation in a free-space droplet (different geometry but same physical content).\n\n### Target T4 (CRITICAL): derive Δ⟨F_z⟩(±Ω) under each candidate mechanism\n\n**Mechanism M1 — orbital -Ω(L_z+F_z) bias alone (no full-DDI off-diag, just secular)**:\n- Compute the rotating-frame ground state |GS_M1(Ω)⟩ as a competition between -(p_z - Ω)F_z, -p_perp F_x, -Ω L_z, and H_trap.\n- For +Ω: the -Ω L_z term lowers energy of |L_z=+ℓ⟩ states. Coupled to F_z via DDI secular term Q_zz ∝ (3 cos²θ - 1) F_z² — this DOES NOT mix L_z and F_z directly. So in secular-DDI-only limit, L_z and F_z are independently optimized: F_z optimizes to fully-polarized along -(p_z-Ω) direction tilted by p_perp, L_z optimizes to ground orbital state. Asymmetry comes from the cos²β_+ vs cos²β_- factor T17 already had.\n- Implication: secular-DDI-only mechanism gives SAME spin-only-Lindblad answer T18 already refuted. So M1 alone is INSUFFICIENT.\n- BUT: the dynamics matters, not just GS. In the dissipative dynamics with γ_dr (cascade), the -Ω L_z term gates which orbital states the system can populate, which gates which F_z trajectories are energetically allowed. This is a SUBTLE many-body effect. Derive (or argue rigorously) whether secular-DDI dissipative dynamics with -Ω L_z bias can flip the sign.\n\n**Mechanism M2 — full-DDI off-diagonal Q_{αβ} coherent coupling**:\n- In rotating frame, Q_{xz}, Q_{yz} oscillate at ±Ω (single-frequency); Q_{xy}, Q_{xx}-Q_{yy} oscillate at ±2Ω. The ±Ω terms can resonate with the F_+ ⊗ |L_z+1⟩⟨L_z| structure to drive coherent L_z ↔ F_z transfer.\n- Yan-Li-Saito 2026 has this in free-space droplet form: m+v=ℓ kinetic relation (v = orbital quantum number, m = spin) under DDI. Adapt to trapped harmonic + ε_dd<1.\n- Derive the Q_{xz}F_z F_x matrix element between |L_z=0, m=+F⟩ and |L_z=+1, m=+F-1⟩. Scale ∝ c_dd · F · sqrt(2F) · (trap-geometry factor).\n- This Q_{αβ} mechanism is c_dd-dependent: c_dd=0 → killed. This is the c_dd=0 control falsifier discriminant.\n\n**Mechanism M1+M2 combined**: most physically realistic. Orbital bias + DDI coupling work together. State the dominant scaling τ ~ ?.\n\n### Target T5 (HIGH): 3-bin julia falsifier prediction table\n\nFor each candidate mechanism (M1 alone, M2 alone, M1+M2), predict numerical values of Δ⟨F_z⟩(±Ω) at t=30 ω⁻¹ for:\n- γ_dr=0, c_dd nominal (the 22:00 JST run already queued)\n- γ_dr=0.02, c_dd=0 (the c_dd=0 control)\n- γ_dr=0, c_dd=0 (both zero — full coherent control; if asymmetry survives here, it's from trap geometry + p_perp alone)\n\nProvide concrete numbers (or analytical bounds) for each cell. This is the seed.md essence §2 (falsifiable parameter dependence) deliverable.\n\n### Target T6 (MEDIUM): τ_Barnett(Ω, p_z, p_perp, c_dd, F) scaling\n\nUnder the dominant mechanism (M1, M2, or M1+M2 — your choice based on T4 analysis), derive the closed-form scaling of τ_Barnett. Compare to:\n- T17 spin-only τ_asym = (6/(F·p_perp²·p_z·|Ω|))^(1/4) = 4.9 ms (T17 §4.3) — known incomplete\n- Empirical 7-14 ms\n- Yan-Li-Saito droplet τ scaling (if recoverable)\n\nDoes the orbital-extended τ_Barnett match 7-14 ms? If yes, this is the closed-form τ_Barnett seed.md priority Q1 asked for.\n\n### Target T7 (MEDIUM): Identify residual research queries Q19.x\n\nIf certain matrix elements (e.g. specific F=6 reduced-matrix-element values for DDI Q_{αβ} between trap eigenstates) need literature backing, list them as Q19.1, Q19.2, etc. — these become T20 researcher targets if you can't close them yourself.\n\n## Output\n\nProduce `runs/_loop/theorist/turn_19.md` with:\n0. Convention declaration (units, spinor ordering, p_z=0.315, p_perp=0.220, Ω=±0.5, γ_dr=0.02; AND state explicitly: T17 eq(12) sign error noted and not propagated)\n1. Context summary (T18 Scenario C result; T17 §8 Q8.2/Q8.3 deferral cashed in)\n2. Target T1: lab-frame H including trap + orbital + DDI\n3. Target T2: rotating-frame transformation with R = exp(-iΩt(L_z+F_z)); Q_{αβ} components in rotating frame (static / ±Ω / ±2Ω)\n4. Target T3: conservation laws (L_z+F_z=J_z; secular vs full-DDI)\n5. Target T4: Δ⟨F_z⟩(±Ω) derivation under M1, M2, M1+M2\n6. Target T5: 3-bin julia falsifier prediction table\n7. Target T6: τ_Barnett(Ω, p_z, p_perp, c_dd, F) closed form\n8. Target T7: residual research queries Q19.x\n9. Consistency checks (T17 limits recovered when L_z DOF frozen / c_dd→0 / γ_dr→∞)\n10. Sequencing for T20: (a) implementer_sympy numerical confirm of M1 / M2 / M1+M2 in spin⊗orbital truncated Hilbert; (b) researcher Q19.x if literature backing needed; (c) noop until 22:00 JST julia\n11. Dispatcher output (`subagent_type: 'noop'` for self-directive; or specify the recommended next action)\n\n## Constraints\n\n- Pure derivation. NO julia, NO GPU, NO file writes outside `runs/_loop/theorist/turn_19.md`. NO src/ modifications.\n- sympy / pencil-and-paper algebra fine; if sympy reproducible artifact helps, include as a separate dispatcher line item for implementer_sympy T20 follow-up.\n- Use rational arithmetic where possible (W^CG values per T15 table; CG coefficients exact where computable).\n- Be HONEST: if M1 alone is insufficient (likely — secular-DDI-only doesn't mix L_z F_z, as you'll find), state it. If M2 alone reproduces empirical Δ=-4.60 with correct sign, state that mechanism wins.\n- Document any sign errors found in T11 or T17 explicitly.\n- DRIFT_MANUSCRIPT_DELTA_ZERO note: this turn does NOT touch manuscript (per seed.md L60 + feedback_manuscript_is_not_the_essence.md). State this in your turn header to acknowledge director's deferral.\n\n## Out-of-scope (DO NOT)\n\n- Full GPE / many-body numerical simulation (defer to 22:00 JST julia run).\n- Yan-Li-Saito droplet reproduction in detail (ε_dd>1 regime; just note the analogous structure).\n- Manuscript / docstring polish.\n- src/ modifications.\n- 3000× FullBdGLHY closed form (contained bug per seed.md L60).\n- Pursuing T17 eq(12) sign-error fix beyond noting it (T18 already documented; T17 author can fix in revision).\n\n## Sanity checks before submitting\n\n- Did you reproduce T17 spin-only limit by setting L_z=0 (orbital frozen)? Should give T17 eq(T1) and Δ=+5.96 at γ_dr=0.\n- Does c_dd=0 in M2 give Δ that matches T17 spin-only? (Should: if M2 is full-DDI, killing DDI returns to spin-only.)\n- Are your numerical predictions for the 3 julia controls (γ_dr=0, c_dd=0, both zero) physically reasonable (|Δ| ≤ 2F = 12)?\n- Does the L_z+F_z = J_z conservation argument hold under secular DDI? (Should: secular Q_zz commutes with J_z trivially.)\n- For mechanism M2, did you scale the Q_{αβ} off-diagonal coupling correctly with trap geometry? (Anisotropic trap modifies the L_z↔L_z±1 matrix elements.)\n\n## Expected outcome shape\n\nMost likely (60% probability per director estimate): theorist concludes M2 (full-DDI Q_{αβ}) is necessary; M1 (orbital -Ω L_z bias alone, secular DDI only) gives same spin-only answer T18 refuted. M1+M2 needed for empirical Δ=-4.60. c_dd=0 control kills asymmetry → falsifies M2 if julia c_dd=0 still shows asymmetry. γ_dr=0 control isolates coherent vs dissipative.\n\nLess likely (25%): theorist finds the orbital bias alone (M1) suffices via subtle dissipative+orbital coupling.\n\n15%: theorist finds neither M1 nor M2 alone is the mechanism; suggests Candidate D (e.g., trap-driven Berry-phase mechanism not yet considered) and produces detailed Q19.x list for T20 researcher.\n\nAll three outcomes produce a verdict-class result (PASS for closed form derived; FAIL_PHYSICS if mechanism fails to close; NOOP-like if requires researcher backstop). Cost target ≤1.7M effective tokens (matches T17 baseline 1.5-1.7M).",
  "expected_outcome": "(1) `runs/_loop/theorist/turn_19.md` with 11 sections including the 3-bin julia falsifier prediction table (Target T5) — the central deliverable for the 22:00 JST julia window. (2) Closed-form τ_Barnett(Ω, p_z, p_perp, c_dd, F) for the dominant mechanism with comparison to empirical 7-14 ms. (3) Identification of which mechanism (M1 orbital alone, M2 full-DDI alone, M1+M2 combined) drives the sign-asymmetry — with conservation law (L_z+F_z=J_z under secular DDI; explicit DDI Q_{αβ} mixing under full DDI) explicitly stated. (4) Sequencing for T20 (likely implementer_sympy 65-dim numerical confirm if framework derived cleanly, or researcher if literature backing needed for specific matrix elements). (5) T17 sign-error and T11 §2.4/§2.8 hand-wave status explicitly addressed and either derived rigorously or refuted. Most likely (60%): M1+M2 derived as compatible; c_dd=0 julia control identified as decisive discriminator. Less likely (25%): clean M1-only or M2-only theory. 15%: needs Candidate D + researcher backstop.",
  "expected_cost": "≤ 30 min wall-clock, ≤ 1.7M effective tokens. Matches T17 baseline (T17 turn cost not separately tracked but theorist-class typical 1.5-1.7M per recent campaign). Pure derivation, no numerical simulation, no julia.",
  "if_fails_next_step": "If M1+M2 closed form derived cleanly with 3-bin julia falsifier table: T20 = noop until 22:00 JST (then julia γ_dr=0 + c_dd=0 controls test the predictions) OR implementer_sympy 65-dim numerical confirm of the predictions in spin⊗orbital truncated Hilbert (10 modes × 13 spin = 130 dim — still tractable in scipy). If theorist concludes M2 (full-DDI Q_{αβ}) is necessary but requires specific matrix elements (e.g., F=6 reduced-matrix-element values for L_z = 0 ↔ 1 trap eigenstates) from literature: T20 = researcher pull Stamper-Kurn / Kawaguchi-Ueda / Yan-Li-Saito for those specific elements. If theorist finds Candidate D (Berry phase, Coriolis cross-term, or other unconsidered mechanism): T20 = theorist Candidate D derivation OR critic to audit the abandonment of M1+M2. If theorist refuses to derive or produces a hand-wave: T20 = critic audit explicitly demanding a closed form OR director re-issues with narrower scope (just M2 alone, just the c_dd=0 control prediction). If T19 theorist cost exceeds 2M (signaling derivation more complex than expected): split into T19=M2 only, T20=M1+combined.",
  "consumed_seed_md": true
}
```

`consumed_seed_md: true`. seed.md 2026-05-16 priority Q1 ("Closed-form derivation of τ_Barnett(Ω, p, c_dd, c_1, F) for the trapped + weak-field + full-DDI regime. Does it match the empirical 7-14 ms? What's the conserved quantity replacing m+v=ℓ?") — T19 directly addresses by deriving the orbital-extended τ_Barnett and identifying L_z+F_z=J_z conservation under secular DDI. seed.md priority Q2 ("Mechanism audit: is the 'secular-DDI suppression' picture rigorous? Could the activation come from c_1 effect, K3 dissipation, trap geometry, or resonance crossing instead?") — T19 separates M1 (orbital bias under secular DDI) from M2 (full-DDI Q_{αβ}) as discriminable hypotheses with the c_dd=0 julia control as discriminant. seed.md priority Q3 ("Cross-validation with Yan-Li-Saito droplet / Stamper-Kurn / Kawaguchi-Ueda") — T19 connects M2 to Yan-Li-Saito m+v=ℓ trapped analog and identifies any specific reduced-matrix-element queries for T20 researcher. seed.md line 49-52 verbatim "theorist turns: derive τ_Barnett scaling, write down the DDI off-diagonal coupling matrix element that drives the pumping, predict signatures at other (Ω, p) values" — exactly T19's brief. seed.md line 96-108 "essence" §1 closed-form τ_Barnett (T19 Target T6) + §2 falsifiable parameter dependence (T19 Target T5 3-bin table for the 22:00 JST julia controls) + §3 bug-discovery (T17 eq(12) sign error already discovered T18, T19 documents and avoids propagating) — all three essence axes addressed.

## E. Self-review checklist

- [x] Read `runs/_loop/state.json` (turn=19, last_judge=FAIL_PHYSICS for T18, drift_escalation=director_must_address for DRIFT_MANUSCRIPT_DELTA_ZERO, cost_inflation=1.165 yellow).
- [x] Read `runs/_loop/seed.md` (Barnett campaign Q1/Q2/Q3 priorities; ≤2 same-subagent in a row; theorist role explicit at line 49-52; manuscript explicit out-of-scope at line 60).
- [x] Read `runs/_loop/_local/scheduler_19.json` (TEXT_ONLY, theorist ∈ allowed_workloads, julia rejected; 16910s = 4h 41m window remaining; 22:00 JST → PROBE_DRIVEN).
- [x] Read `runs/_loop/director/turn_18.md` (continuity: T18 dispatched implementer_sympy with verdict-class deliverable expectation).
- [x] Read `runs/_loop/sim/turn_18.md` (T18 numerical: Scenario C confirmed, spin-only Lindblad refuted at γ_dr=0.02; T17 eq(12) sign error documented).
- [x] Read `runs/_loop/judge/turn_18.json` (FAIL_PHYSICS verdict, falsification_result REFUTED, physical_red_flags = sign mismatch and endpoint errors).
- [x] Read `runs/_loop/theorist/turn_17.md` (relevant sections: §0 conventions, §2.3 eq T1, §4.3 admissions, §8 Q8.2/Q8.3 orbital and sign-reversal deferrals, §12 Scenario C path).
- [x] Read `runs/_loop/schedule.yaml` (TEXT_ONLY until 22:00 JST; PROBE_DRIVEN overnight).
- [x] Read memory `barnett_spin_pumping_observed_2026_05_16.md` (empirical observation, mechanism hypothesis, open questions Q1-Q5).
- [x] Read `runs/_loop/judge/turn_15.json` (cascade Candidate C refuted at γ_dr=0.02).
- [x] Considered NOT dispatching theorist: challenged with researcher (B-2: literature pull is value-add but theorist can derive from first principles), critic (B-3: cannot derive, only audit; right after theorist), implementer_sympy (B-4: needs theorist framework first), noop (B-5: wastes 4.7h pre-julia preparation), implementer_text (B-6: theory not in place per seed.md L60). Theorist wins on §A5 axis-c + §B3 dispatch rule (new derivation) + §B4 rotation clean (T17 was 2 turns ago, T18 intervened) + §B6 DRIFT_MANUSCRIPT_DELTA_ZERO addressed by explicit deferral + §B7 cost OK + §B8 scheduler-allowed + §D1+D3 dominant.
- [x] §6 brief is specific: 9 numbered context files, 7 priority-ordered derivation targets, output schema (11 sections), constraints (no julia/GPU/src), sanity-check list, out-of-scope list, expected outcome 60/25/15% distribution, cost target ≤1.7M, T20 sequencing for each scenario. Theorist needs no clarifying questions.
- [x] Justified why THIS turn: T18 just refuted spin-only at γ_dr=0.02 (Scenario C); 22:00 JST julia window opens in 4.7h; without theorist closed-form predictions, the julia γ_dr=0 + c_dd=0 controls are NOT decisive; seed.md priority Q1+Q2 explicit at this campaign turn; theorist rotation clean.
- [x] `consumed_seed_md: true` — Q1 (closed-form τ_Barnett with orbital extension), Q2 (mechanism audit M1 vs M2 with c_dd=0 discriminant), Q3 (Yan-Li-Saito cross-validation explicit), line 49-52 (theorist role), line 96-108 (essence §1/§2/§3) all addressed.
- [x] DRIFT_MANUSCRIPT_DELTA_ZERO acknowledged in §1, §3 B-1 leverage, and the brief's explicit deferral note (per seed.md L60 + feedback_manuscript_is_not_the_essence.md). Director justifies non-manuscript move as essence-aligned.
