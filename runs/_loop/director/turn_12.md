---
turn: 12
subagent: director
topic_tags: [barnett, tau-closed-form, critic-audit, rotating-frame, gamma-dr, refuted-secular-hypothesis, d1-tier-lift]
paper_section: null
depends_on: [11, "theorist/turn_11.md", "seed.md (2026-05-16 Barnett-pumping)", "memory:rotating_basis_loss_support.md", "memory:yan_li_saito_2026_barnett_paper.md"]
produces: "Critic audit of T11 theorist deliverable: independent reading of the rotating-frame derivation (§2.4), the closed-form τ_Barnett ≈ 1/(F(F+1) γ_dr) prediction (§2.5-2.6) vs empirical 7-14 ms, and the [Refuted] verdict against anko's secular-DDI-suppression hypothesis (§2.7-2.9, §4 claim 7). Output is a PASS / WEAK_PASS / FAIL with concrete findings, so anko can bank or reject the mechanism before the 22:00 JST julia window opens and γ_dr=0 falsifier dispatch is queued."
---

# Turn 12 — Director Report

## 1. Project state snapshot

- **Active research thread: Barnett spin-pumping campaign (seed.md 2026-05-16).** Other-session ran trapped F=6 Eu-151 weak-Bz Klaus-style rotating B at Ω=±0.5; observed ΔF_z/N = 4.60 between ±Ω (co-rotating preserves m=+F, counter-rotating relaxes to uniform). τ_Barnett ≈ 7-14 ms. Data at `runs/eu151_barnett_spin/`. Seed prioritises D1+D3: closed-form τ_Barnett + mechanism audit + cross-validation.
- **T11 (theorist, NOOP-judged) delivered substantive content** at `runs/_loop/theorist/turn_11.md` (796 lines). Closed-form prediction $\tau_{\rm Barnett}^{-1}\approx F(F+1)\,\gamma_{\rm dr}\cdot\Theta(-\Omega\,{\rm sgn}\,g_F)$, giving ~6 ms; matches empirical 7-14 ms within factor 2. **Critically: T11 [Refuted] anko's seed hypothesis** that secular-DDI suppression is the load-bearing mechanism — instead identifies *rotating-frame energetic bias $-\Omega(L_z+F_z)$ + dipolar-relaxation cascade* as the driver. T11 was NOOP not for lack of content but because its §6 was "Open questions" not directive-JSON; effective routing went to §8 Option B (noop).
- **Scheduler T12 (`runs/_loop/_local/scheduler_12.json`):** policy `TEXT_ONLY` until 22:00 JST (~6.5h remaining, 23291s left). All julia workloads rejected. `allowed_workloads` = theorist / researcher / critic / implementer_text / implementer_sympy / noop. **GPU probe is clean (12 GB VRAM free, 1% util)** but the window forbids dispatching — anko is presumed-using.
- **Recent unmerged work piling up:** T8 paper4 §IV.E branch `2bceceb` (audited T9 weak-pass, not merged), T4 paper3 F=14 `be6a472` (deferred), T10 split_step.jl + propagators.jl docstrings (auto-committed `0d6f1a6`). Today's main → T11 has produced no commits (theorist-only).
- **Drift signals all green** (T11 history entry): topic_repetition=0.0, subagent_repetition=0.333, cost_inflation=0.608 (down from 1.5x trend in T8-T10), novel_claim_zero=0.0. Escalation: `none`. T11 contributed a [Refuted] novel claim, breaking the 3-turn DRIFT_COST_INFLATION advisory streak.
- **What's blocked:** the strongest falsification test (γ_dr=0 control run) requires julia — locked behind 22:00 JST window. Until then, the only forward motion is reading-shaped work.

## 2. Recent-turn audit (last 3)

| Turn | Topic | Verdict | Value delivered | Was it right? |
|---|---|---|---|---|
| T9 | Critic audit of T8 paper4 §IV.E vs T7 theorist source | CRITIC_WEAK_PASS | 4 findings, manuscript merge-ready, dispatch.jl docstring narrative-fix surfaced. Cascade audit-asymmetry closed. | Yes — the right cheap critic move at the right moment; pre-merge gate on a [Plausible] mechanism going into a paper. |
| T10 | implementer_text: BCH-leak docstring + Option-γ cure assertion on split_step.jl + propagators.jl | PASS | Auto-commit `0d6f1a6`, +59/-7 lines, scaling formula + dt≲1e-5 threshold landed in docstrings. Cheap, julia-safe, drift-compliant. | Yes for what it was — but it operates on a *prior* theorist's Klaus thread, not on this session's Barnett thread. Manuscript-shaped, mid-priority per §D redirect. |
| T11 | theorist: closed-form τ_Barnett + mechanism audit of seed.md secular hypothesis | NOOP (procedural — §6 mislabeled) | 796 lines, factor-2 numerical match with empirical, [Refuted] anko's stated mechanism, 5 falsifiable predictions for julia phase, 3 deferred research queries. Substantively the highest-value turn this session. | YES on substance, NO on routing. The theorist produced a major D1+D3 deliverable and it sits **un-audited and un-banked**. The NOOP verdict means the loop has not yet acknowledged the [Refuted] claim. This is exactly the §B3 "load-bearing claim, single-source, shared priors" critic case. |

**Trajectory check (§B4):** T9 critic / T10 implementer_text / T11 theorist — clean rotation, no 3-in-a-row trap. T11's theorist output is a *new* topic (Barnett ≠ FullBdGLHY ≠ Klaus BCH). Theorist could fire again, but the highest-leverage move is closing the audit gate on T11's contrarian claim BEFORE the 22:00 JST window opens and a julia γ_dr=0 dispatch goes out on the basis of unaudited theory. If T11's §2.4 rotating-frame argument has a flaw, the γ_dr=0 julia turn would be wasted spend (~45 min GPU, ~3M tokens).

## 3. Bottleneck analysis

Filtered to the TEXT_ONLY workload set, ranked by leverage = value × p(this turn moves it) / cost.

### B-1: Critic audit of T11's Barnett mechanism derivation — gate the [Refuted] claim before julia commit

*Issue*: T11 produced a paper-scale contrarian claim ($\tau_{\rm Barnett}^{-1}\sim F(F+1)\gamma_{\rm dr}$, mechanism = rotating-frame bias + γ_dr cascade, [Refuted] secular-DDI hypothesis) with a factor-2 numerical match. The derivation is theorist-self-audited only — no independent reading exists. The [Refuted] claim against anko's own seed hypothesis raises the audit bar: a wrong refutation propagates two ways (kills a correct hypothesis AND commits to a wrong replacement).

*Category*: verification gap (audit of load-bearing single-source claim) + D1 dominant (Tier 1 derivation → Tier 2 lift gate).

*Leverage*: **5**. Cheap (critic Read-only ~0.7-1.0M effective per T9 baseline), aligns with seed.md "**critic** turns: independently audit theorist's claims against the empirical data + Yan-Li-Saito framework", aligns with T11 §8 Option A (theorist's own self-recommendation FIRST before julia commit). Closes the load-bearing audit-asymmetry before resource commitment to the γ_dr=0 julia falsifier at 22:00. §B3 textbook fit: paper-scale, single-source, contrarian — exactly the spec.

*What moves it*: **critic**. Brief: read T11 + cross-check the rotating-frame Hamiltonian derivation, the F(F+1) CG-weight, the Yan-Li-Saito picture parallel, and the [Refuted] secular-DDI logic. ~15-20 min wall-clock, ~1.0M effective.

### B-2: Researcher dispatch for T11's Q1/Q2/Q3 — fill the deferred-data gaps

*Issue*: T11 §7 has 3 research queries. Q1 (reconcile p=0.69 vs my-derived 0.315), Q2 (per-m CG weighting in `losses.jl` — I verified the file exists with rank-2 CG at `losses.jl:144-176`, so Q2 is partly readable now), Q3 (Born-Markov master equation in KU2012 / SKU2013 / Pasquiou 2011).

*Category*: docs / verification gap (literature ground).

*Leverage*: **3**. Useful but the queries are not blocking the audit. Q1 is a numerical reconciliation that critic can flag-or-not-flag in passing; Q2 has a codebase answer the critic can also read directly (`src/hamiltonian/interactions/losses.jl:144-176` — the rank-2 CG weighting confirms theorist's F(F+1) approach is structurally consistent); Q3 is a refinement-grade query, T11 itself marked it "not blocking". Better routed AFTER critic audit identifies which queries became decision-relevant. Parallel-dispatch with critic is tempting but §B8 picks one workload.

*What moves it*: researcher. ~10-15 min, ~0.8-1.5M effective. Deferred to T13 unless T12 critic surfaces a Q-blocker.

### B-3: Theorist re-derivation on a sibling thread (Yan-Li-Saito droplet ε_dd<1 mapping, or F-δ Goldstone)

*Issue*: T11 §3.5 sketched the Yan-Li-Saito connection but didn't push the mapping from free-space ε_dd>1 droplet to trapped ε_dd≈0.55 rotating regime. Memory `yan_li_saito_2026_barnett_paper.md` is a fresh Tier-3 verification target.

*Category*: physics gap (D3 new theory).

*Leverage*: **2**. Two-theorist-in-a-row on the Barnett thread is the §B4 rotation-trap that birthed the T11 NOOP procedurally. T11 is also un-audited; layering a second derivation on top of un-audited theory compounds risk. **Defer to post-audit**.

### B-4: implementer_text — write `@info` advisory at `make_workspace.jl:154` (the secular-DDI threshold) documenting the Barnett mechanism

*Issue*: seed.md mentions an implementer_text option to write a docstring or @info at the secular-DDI boundary documenting the Klaus-secular-suppresses-Barnett regime. But T11 §4-claim-7 says the standard explanation in terms of "secular DDI averages off-diagonal terms" is *incomplete/misleading*. Writing such an advisory NOW before the audit confirms or rejects T11's mechanism would commit a possibly-wrong narrative into the codebase.

*Category*: docs gap (downstream artifact).

*Leverage*: **1**. Per seed.md "A docstring or @info advisory at the secular_ddi boundary IS acceptable as a downstream artifact, but ONLY after the theory is in place — not as the primary turn shape." Audit must happen first.

### B-5: noop — wait until 22:00 JST window

*Issue*: All julia paths are locked; the most informative experiment (γ_dr=0 control, T11 §5.3, §3.2) requires julia. Doing nothing for 6.5h until window opens.

*Category*: meta.

*Leverage*: **1**. Fails §A5: there is at least one cheap, leveraged, julia-free move available (B-1). Per anko's "Cost overhead IS the cost" memory: do the audit now, sequence the julia turn behind it. Noop is the wrong choice.

### B-6: implementer_sympy — symbolic verification of the rotating-frame Hamiltonian transformation

*Issue*: T11 §2.4 derives $\tilde H = U_R^\dagger H U_R + i(\partial_t U_R^\dagger)U_R$ for $U_R = e^{-i\Omega t(L_z+F_z)}$. The $-\Omega(L_z+F_z)$ term comes from the gauge piece. This is textbook but symbolically verifiable.

*Category*: verification gap.

*Leverage*: **2**. Sympy can confirm a manipulation but cannot validate the physics interpretation (which is what's load-bearing). Critic does more in one shot. Better as a critic sub-task ("if you doubt §2.4, request a sympy verification") rather than a standalone turn.

## 4. Strategic options for THIS turn

| # | Move | Subagent | Now-or-later | Cost |
|---|---|---|---|---|
| 1 | **Critic audits T11 theorist deliverable** | **critic** | NOW — gates julia spend at 22:00 JST, theorist self-recommended (§8 Option A), seed.md "critic turns" call-out | ≤ 20 min, ≤ 1.0M |
| 2 | Researcher answers Q1/Q2/Q3 | researcher | T13 if critic flags a Q-blocker | ≤ 15 min, ≤ 1.5M |
| 3 | Theorist Yan-Li-Saito mapping or F-δ Goldstone | theorist | DEFER — §B4 rotation trap, audit must happen first | ≤ 25 min, ≤ 2.0M |
| 4 | implementer_text secular-DDI @info advisory | implementer | DEFER — premature without audit | ≤ 10 min, ≤ 0.5M |
| 5 | implementer_sympy rotating-frame check | implementer | subordinate to critic | ≤ 15 min, ≤ 1.0M |
| 6 | noop until 22:00 JST | n/a | rejected — cheap leveraged move exists | 0 |

**Pick: Option 1 (critic audit of T11).**

Why:

- **§A5 axis (a) — verify existing-implementation claim**: T11's $\tau_{\rm Barnett}\approx 1/(F(F+1)\gamma_{\rm dr})$ is a closed-form *prediction* about the simulator's behavior at given parameters. Audit checks the derivation. ✓
- **§A5 axis (c) — new theory grounded in research**: T11 §3.5 connects to Yan-Li-Saito; critic checks the mapping. ✓
- **§B3 critic dispatch rule (textbook fit)**: "Dispatch when the last N turns may have agreed on a wrong answer because they share priors. Costly; only invoke when a load-bearing claim from prior 3 turns is paper-scale." T11 is single-source, paper-scale (T11 §9 explicitly proposes Paper #5/#6 candidate "Rotating-frame Barnett pumping in trapped dipolar spinor BECs"), and contrarian against anko's seed hypothesis. This is the textbook case.
- **§B4 rotation favorable**: theorist last fired T11, critic last fired T9 (3 turns ago). Critic is most-fresh subagent. Dispatching it now improves rotation health.
- **§B6 drift compliance**: all drift signals clean (advisories empty). The audit produces a [Refuted]-or-not verdict — high novel-claim density at low cost.
- **§B8 scheduler compliance**: critic ∈ allowed_workloads, TEXT_ONLY policy honored, no julia, no GPU, 23291s window-left is >> critic typical_duration (300-1200s).
- **§D1 dominant**: this is the gate that decides whether T11's [Refuted]-mechanism claim banks or rolls back. If PASS: anko can route julia at 22:00 JST to test the *correct* falsifier (γ_dr=0 control). If FAIL: T13 theorist re-derives without wasting julia spend on a wrong mechanism. Either way, julia spend is preserved for the *right* experiment.
- **seed.md explicit fit**: "**critic** turns: independently audit theorist's claims against the empirical data + Yan-Li-Saito framework." Exact wording.
- **T11 §8 Option A self-recommendation**: theorist explicitly said "RECOMMENDED FIRST". Trust the theorist's routing read.

Why NOT Option 2 (researcher) primary: queries can be folded into critic's check; researcher is better-routed after critic identifies which Q surfaces matter. Specifically, Q2 has a codebase answer at `src/hamiltonian/interactions/losses.jl:144-176` (rank-2 CG: γ_m = Γ_dr·Σ_{q∈{-1,-2}}|CG(F,m;2,q|F,m+q)|²/Z) — the critic can read this directly and either confirm or refute T11 §2.6's F(F+1)=12 top-rung scaling without needing a researcher external pull. Q1 is a single-line numerical reconciliation. Q3 is "not blocking" by T11's own marking.

Why NOT Option 3 (theorist again): §B4 same-subagent rotation rule + un-audited theory shouldn't be deepened by more theory.

Why NOT Option 4 (advisory write): seed.md explicitly says "ONLY after the theory is in place". Theory is NOT in place until critic audit confirms.

Why NOT Option 5 (sympy): single mechanism check is narrower than critic's full reading.

Why NOT Option 6 (noop): leveraged move available, anko's "Cost overhead IS the cost" memory says execute don't deliberate.

## 5. Calibrated progress check

| Axis | Status | Evidence |
|---|---|---|
| Physics completeness (D1) | **at risk** — pending T12 audit | T11 derived closed-form τ_Barnett that matches data within factor 2 (Tier 1 → Tier 2 lift candidate), but the [Refuted] against anko's seed hypothesis is single-source. Tier 2 promotion requires audit pass. |
| Verification depth (D1 dominant) | **at risk → on track if T12 PASS** | Tier ladder: T11 = Tier 1 (theorist self-derived). T12 critic = Tier 2 (independent reading). T13+ julia γ_dr=0 (≥22:00 JST) = Tier 3 (empirical falsifier). T12 is the gate between Tier 1 and Tier 2. |
| Manuscript (D non-primary, per redirect) | **on track** | Three unmerged branches (T4 F=14, T8 paper4-IV.E, T10 docstrings) sit awaiting anko-merge. T11 explicitly proposes Paper #5/#6 candidate. Manuscript is downstream artifact — not this turn's concern. |
| Reproducibility | **on track** | seed.md, schedule.yaml, runs/eu151_barnett_spin/ data archived. T11 docs the parameter extraction in §2.1 with full SI chain. |
| Loop infrastructure | **healthy** | Drift signals all green. T11 NOOP was procedural (§6 mislabel) not strategic. T12 will exercise the "critic audits theorist deliverable from same-session loop" cascade pattern. |

**Mark**: Verification axis is the load-bearing axis this session. T12 critic is the precise move that lifts it from at-risk to on-track. Julia γ_dr=0 falsifier (Tier 3) is queued behind T12 and the 22:00 JST window — sequenced correctly.

## 6. Dispatch decision

```json
{
  "subagent_type": "critic",
  "rationale": "T11 (theorist, runs/_loop/theorist/turn_11.md, 796 lines) delivered a paper-scale contrarian claim: closed-form tau_Barnett ~ 1/(F(F+1) gamma_dr) ≈ 6 ms matching empirical 7-14 ms within factor 2, AND a [Refuted] verdict against anko's stated secular-DDI-suppression mechanism, replacing it with rotating-frame energetic bias -Omega(L_z+F_z) + dipolar-relaxation cascade. This is single-source (theorist self-audited) and paper-scale (T11 §9 proposes Paper #5/#6 candidate). The §B3 critic-dispatch rule fits textbook: load-bearing claim from prior 3 turns, paper-scale, shared priors (only theorist's reading exists). Theorist itself recommends critic_audit FIRST in §8 Option A. seed.md 2026-05-16 lines 50-51 explicitly call out 'critic turns: independently audit theorist's claims against the empirical data + Yan-Li-Saito framework' as a campaign workload. Scheduler T12 (runs/_loop/_local/scheduler_12.json) restricts to TEXT_ONLY — critic is in allowed_workloads. Gating this audit BEFORE the 22:00 JST julia window opens preserves julia spend for the *correct* falsifier (gamma_dr=0 control, T11 §5.3 §3.2): if T11's mechanism is right, the julia turn is decisive; if wrong, the julia turn would waste ~45 min GPU + ~3M tokens on a wrong-mechanism experiment.",
  "brief": "## Goal\n\nIndependently audit T11's theorist deliverable (`runs/_loop/theorist/turn_11.md`) on the trapped-weak-Bz-full-DDI Barnett-pumping mechanism. Produce a PASS / WEAK_PASS / FAIL verdict with concrete findings, so the orchestrator can decide whether to (a) bank T11's closed-form prediction + [Refuted] secular-DDI verdict, and (b) queue the gamma_dr=0 julia falsifier (T11 §5.3) for the 22:00 JST window opening.\n\nThis is a Read-only critic per protocol. No julia execution. No file modification. Output to `runs/_loop/critic/turn_12.md`.\n\n## Materials to read (in order)\n\n1. `runs/_loop/theorist/turn_11.md` — the source. Pay close attention to:\n   - §0 conventions (Larmor sign, DDI conventions, gamma_dr definition).\n   - §1 context + §2.1 parameter extraction at empirical data point (including the p=0.69 vs 0.315 discrepancy and how it's handled).\n   - §2.2 transverse DDI mean field vanishes at t=0+ for m=+F stretched (this is the structural argument that the secular-vs-full-DDI distinction is NOT load-bearing at t=0+).\n   - §2.3 single-particle Rabi picture and why it gives the OPPOSITE asymmetry sign vs anko's data.\n   - **§2.4 the load-bearing mechanism** — rotating-frame transformation $\\tilde\\psi = U_R^\\dagger\\psi$ with $U_R = \\exp[-i\\Omega t(L_z + F_z)]$; the gauge term $-\\Omega(L_z + F_z)$; the claim that $H_{\\rm DDI}$ is rotation-invariant about $\\hat z$ when both real-space and spin are rotated together. THIS IS THE LOAD-BEARING DERIVATION.\n   - §2.5-2.6 closed-form tau_Barnett scaling and numerical evaluation (eq 5-6).\n   - §2.7 c_1=0 isolation argument (only gamma_dr breaks F_z symmetry in this run).\n   - §2.8 conservation law replacement vs Yan-Li-Saito (J̃_z = L_z + F_z conserved in rotating frame modulo dissipation).\n   - §2.9 why Klaus strong-Bz shows no asymmetry ($\\Omega/\\omega_L \\sim 10^{-8}$ suppression).\n   - §3.1-3.6 sanity checks.\n   - **§4 calibrated claims** — 8 items at [Established]/[Plausible]/[Speculative]/[Refuted] tiers. The [Refuted] claim against anko's hypothesis is the highest-stakes item.\n   - §5 falsifiable predictions (relevant for the post-22:00 julia phase).\n   - §6-7 open questions + research queries Q1-Q3.\n   - §8 directive options (theorist's own routing recommendation).\n\n2. `runs/_loop/seed.md` — anko's hypothesis statement is at lines 18-22: 'Klaus *strong-field* regime had secular DDI averaging out the off-diagonal terms responsible for orbital→spin AM transfer; weak-field full DDI revives the mechanism.' This is the hypothesis T11 [Refuted]s.\n\n3. `/home/suzume/.claude/projects/-home-suzume-workspace-BEC-simulation/memory/yan_li_saito_2026_barnett_paper.md` — for the conservation-law analogue T11 §2.8 builds on (free-space droplet $m+v=\\ell$ vs trapped $\\tilde J_z = L_z+F_z$).\n\n4. `/home/suzume/.claude/projects/-home-suzume-workspace-BEC-simulation/memory/rotating_basis_loss_support.md` — for the loss-channel integration that enables this experiment.\n\n5. `src/hamiltonian/interactions/losses.jl:24-26, 144-176` — the actual codebase implementation of per-m gamma_dr CG weights. **CRITICAL: verify whether T11 §2.6 eq (6)'s factor F(F+1) = 12 at m=+F is consistent with the rank-2 CG weighting $\\gamma_m = \\Gamma_{\\rm dr}\\cdot\\sum_{q\\in\\{-1,-2\\}}|\\mathrm{CG}(F,m;2,q|F,m+q)|^2 / Z$.** Note rank-2 (not rank-1) — the codebase implements $\\Delta m=\\{-1,-2\\}$ via a rank-2 spherical-tensor matrix element, NOT $F_-$ alone ($F_-$ would give $\\sqrt{F(F+1)-m(m-1)}$, $F(F+1)$ squared = 12 at m=+F). T11's claim that the top-rung rate is enhanced by $F(F+1)-m(m-1)|_{m=F}=12$ uses the rank-1 ($F_-$) intuition. The codebase uses rank-2. Compute the actual rank-2 enhancement (it may differ by an order-of-magnitude factor) — this is a load-bearing numerical check.\n\n6. `src/foundation/types/ddi_loss.jl:73-78` — the LossParams definition cited in T11 §0.\n\n7. (Optional) `runs/eu151_barnett_spin/config.yaml` for parameter sanity check on T11 §2.1.\n\n## Audit checklist\n\nFor each item, write PASS / WEAK_PASS / FAIL with 2-3 sentence justification quoting specific lines/sections.\n\n### Audit-1: Parameter regime identification (T11 §2.1)\n\n- Is the dimensionless conversion chain for $c_{dd}^{\\rm dimless}\\approx 1.5\\times 10^3$ self-consistent (SI computation lines (1)-(3))?\n- Is the secular ratio $p/(c_{dd}\\langle n\\rangle)\\sim 2\\times 10^{-3}$ correctly interpreted as 'full-DDI regime by 5 orders'?\n- Is the p=0.69 vs computed p=0.315 discrepancy handled rigorously (Q1 flag), or does it propagate undetected into the rest of the derivation?\n\n### Audit-2: Transverse DDI mean field at t=0+ (T11 §2.2)\n\n- Is the claim '$\\Phi_x = \\Phi_y = 0$ at $t=0^+$ for $m=+F$ stretched, regardless of secular or full DDI' correct?\n- This is the structural argument that refutes the secular-vs-full distinction as load-bearing. Verify: full-DDI's $Q_{xy}, Q_{xz}, Q_{yz}$ being present doesn't help when $\\langle F_x\\rangle = \\langle F_y\\rangle = 0$ everywhere?\n\n### Audit-3: Single-particle Rabi gives wrong sign (T11 §2.3)\n\n- Is the rotating-frame quantization-axis tilt calculation $\\beta = \\arctan(p_\\perp / (p-\\Omega))$ correct?\n- Does the conclusion 'counter-rotating keeps spin close to ẑ' indeed predict the OPPOSITE asymmetry vs anko's data?\n- This rules out the trivial Rabi picture as the mechanism — confirm the logic is airtight.\n\n### Audit-4: Rotating-frame transformation and DDI invariance (T11 §2.4) — LOAD-BEARING\n\n- Is the rotating-frame Hamiltonian $\\tilde H = U_R^\\dagger H U_R + i(\\partial_t U_R^\\dagger)U_R$ derivation correct for $U_R = e^{-i\\Omega t(L_z+F_z)}$?\n- Does the gauge piece really give $-\\Omega(L_z+F_z)$? (Standard textbook, but verify the sign and the joint-rotation requirement.)\n- **Critical**: is $H_{\\rm DDI}$ actually rotation-invariant about $\\hat z$ in this codebase's implementation? T11 §2.4 paragraph starting 'H_DDI: the **scalar** dipolar-density form is rotation-invariant about $\\hat z$ because both real-space rotation and spin rotation are applied **together**' — verify this against `src/hamiltonian/interactions/ddi/`. Note T11 itself flags this in §6 question 4 as 'is the DDI rotation-invariance argument exact at the discrete-grid level?' The FFT grid is not exactly rotation-invariant — does this matter for the audit?\n- If §2.4 is wrong, the entire mechanism collapses. Make a careful call.\n\n### Audit-5: Closed-form tau_Barnett numerical match (T11 §2.5-2.6) — LOAD-BEARING\n\n- T11 eq (6): $\\tau^{(-)} \\approx 1/(12\\gamma_{\\rm dr}) = 50/12\\cdot \\omega^{-1}\\approx 4\\,\\omega^{-1}\\approx 6\\,$ms vs empirical 7-14 ms. The factor 12 comes from T11's claim that $F_-$ matrix element squared = $F(F+1)-m(m-1)|_{m=F} = 12$. **But the codebase uses rank-2 CG (not rank-1 $F_-$)**, per `src/hamiltonian/interactions/losses.jl:24-26, 144-176`. Compute the actual rank-2 enhancement factor at m=+F=6:\n  - $\\gamma_{m=6} = \\Gamma_{\\rm dr}\\cdot\\sum_{q\\in\\{-1,-2\\}}|\\mathrm{CG}(6,6;2,q|6,6+q)|^2 / Z$ where Z is the m-averaging normalization.\n  - Look up or compute the CG values: $\\langle 6,5|F=6,m=6;F'=2,q=-1\\rangle$ and $\\langle 6,4|F=6,m=6;F'=2,q=-2\\rangle$.\n  - Is the numerical match $\\sim 6$ ms preserved with the correct rank-2 factor, or does it shift to e.g. 30 ms (out of empirical window)?\n- If T11 §2.6's factor 12 is wrong, the factor-2 numerical match degrades. Does it still match within order of magnitude, or does it break?\n\n### Audit-6: c_1=0 isolation logic (T11 §2.7)\n\n- Is the argument 'all m-ladder motion comes from gamma_dr' correct given c_1=0 in the run and K3 being F_z-blind?\n- DDI does conserve total F_z (long-wavelength limit) — verify this for the full-DDI lab-frame implementation.\n\n### Audit-7: Yan-Li-Saito conservation-law analogy (T11 §2.8, §3.5)\n\n- Is the mapping $m+v=\\ell$ (free-space, no dissipation) → $\\tilde J_z = L_z+F_z$ (trapped, rotating-frame, dissipation-broken) substantively correct?\n- Is the [Plausible] tier on this claim appropriate, or is the trapped-vs-free distinction stronger than T11 acknowledges?\n\n### Audit-8: The [Refuted] verdict against anko's secular-DDI hypothesis (T11 §4 claim 7) — LOAD-BEARING\n\n- Is the [Refuted] verdict well-supported by §2.2 + §2.7 + §2.9?\n- Is there any path on which anko's secular-DDI hypothesis could still be right (e.g., at a different time-window than t=0+ when transverse magnetization has built up)?\n- Could a wrong [Refuted] kill a correct hypothesis? Steelman anko's view: at later t (after transverse magnetization develops), does secular DDI 'average out' the off-diagonal coupling in a way that full DDI does not? Even if t=0+ is symmetric, the *cumulative* effect over time might differ. Test whether T11's analysis covers this case or stops at $t=0^+$.\n\n### Audit-9: Falsifiability of the predictions (T11 §5)\n\n- Is the gamma_dr=0 control (T11 §5.3, §3.2) genuinely decisive as T11 claims? Yes-No-and-why.\n- Is the c_dd=0 prediction (T11 §5.4 + §3.3) genuinely the load-bearing falsifier of §2.4?\n- Are the predictions in T11 §5.1 ($\\Omega$-sweep) numerically reasonable, or are there qualitative issues (e.g., should the $\\Omega\\to 0$ limit really give zero asymmetry as eq 5's Heaviside implies, even though the smooth physics should be smooth)?\n\n### Audit-10: Overall lift-tier verdict\n\n- T11 self-declares 'Tier-1 → Tier-2 lift: WEAK_PASS' in §4. Is this an appropriate self-assessment, or should it be stronger (PASS) / weaker (FAIL)?\n- Concrete recommendation: should the orchestrator queue the gamma_dr=0 julia falsifier for the 22:00 JST window (the next strongest evidence step), or does T11's derivation need rework first?\n\n## Output format\n\nWrite to `runs/_loop/critic/turn_12.md`.\n\nStructure:\n\n1. **Verdict**: PASS / WEAK_PASS / FAIL with one-paragraph overall summary.\n2. **Audit-1 through Audit-10**: per-item verdict + 1-3 sentence justification + quoted evidence.\n3. **Specific findings**: numbered list of any concrete issues (mathematical errors, framing concerns, missing nuances). Empty list if none.\n4. **Recommendation to director (T13)**: explicit guidance on:\n   - Whether T11's closed-form $\\tau_{\\rm Barnett}\\sim 1/(F(F+1)\\gamma_{\\rm dr})$ should bank as a [Plausible] claim.\n   - Whether the [Refuted] verdict against anko's secular-DDI hypothesis is sound.\n   - Whether to queue the $\\gamma_{\\rm dr}=0$ julia falsifier (T11 §5.3) for the 22:00 JST window.\n   - Whether any T11 derivation step needs theorist re-work BEFORE julia spend.\n5. **Out-of-scope notes** (optional): related concerns that fall outside this audit (e.g., research queries Q1/Q3 that warrant a follow-on researcher turn).\n\n## Out-of-scope\n\n- DO NOT WebFetch Yan-Li-Saito 2026, Kawaguchi-Ueda 2012, etc. The audit is internal-consistency of T11 vs codebase + seed.md hypothesis. If T11 misattributes a paper, that's a [research-query Q3] follow-up not a critic-stopping issue.\n- DO NOT run julia, execute experiments, or modify files. Critic is Read-only.\n- DO NOT propose new derivations — only critique T11's.\n- DO NOT audit T11 §6 / §7 / §8 / §9 substantively — they are open-questions / queries / directive options, not load-bearing derivation content. §8 Option A/B routing recommendations are this turn's input, not its audit target.\n- DO NOT make the dispatch decision for T13 unilaterally — recommend in the output, but the orchestrator/director ratify.\n- If a critical math step in §2.4 or §2.6 cannot be verified at the audit-time check level, STOP and flag rather than fall back to plausible-sounding generic agreement.",
  "expected_outcome": "(1) `runs/_loop/critic/turn_12.md` with explicit PASS / WEAK_PASS / FAIL verdict + Audit-1 through Audit-10 per-item assessments + numbered specific findings + concrete T13 recommendation. (2) Critical numerical result: confirm or refute T11 §2.6 eq (6)'s factor 12 enhancement, using the rank-2 CG weighting actually implemented in `src/hamiltonian/interactions/losses.jl:144-176`. (3) Verdict on whether the [Refuted] against anko's secular-DDI-suppression hypothesis is sound or premature. (4) Explicit go/no-go signal for the 22:00 JST gamma_dr=0 julia falsifier. (5) If PASS or WEAK_PASS: T11's closed-form scaling and [Refuted] verdict bank as [Plausible]; T13 queues julia. (6) If FAIL: T13 dispatches theorist re-work on the specific failed step, no julia commit. Cost stays under 1.0M effective.",
  "expected_cost": "≤ 20 min wall-clock, ≤ 1.0M effective tokens. Critic reads T11 (796 lines) + seed.md (~110 lines) + 2 memory files (~25 lines each) + 2 codebase files (~50 lines relevant) + (optional) `runs/eu151_barnett_spin/config.yaml`. No WebFetch, no julia, no sympy execution. Comparable to T9 critic budget (1.0M, weak-pass case).",
  "if_fails_next_step": "If T12 returns FAIL with specific revision findings (e.g., §2.4 rotation-invariance argument has a fatal flaw, or §2.6 factor 12 is off by orders of magnitude under rank-2 CG): T13 dispatches theorist (T11 substantively, on the failed step only — NOT a fresh re-derivation) within the TEXT_ONLY window before 22:00 JST. Do NOT queue julia until corrected derivation is in place. If T12 returns WEAK_PASS with refinements (e.g., factor 12 should be a smaller-O(1) number from rank-2 CG, but order-of-magnitude survives, OR §2.8 Yan-Li-Saito mapping has caveats): T13 queues the gamma_dr=0 julia falsifier for 22:00 JST window AS THE FIRST julia dispatch (per T11 §8 Option B's own recommendation), with the falsification criterion explicit ($\\Delta\\langle F_z\\rangle/N < 1$ confirms mechanism; > 1 refutes). The Omega-sweep and p-sweep (T11 §5.1, §5.2) come AFTER the gamma_dr=0 result. If T12 returns PASS: T13 may parallel-dispatch researcher for Q1/Q3 + queue julia γ_dr=0 control. If T12 itself fails (critic cannot retrieve T11 file, or critic returns INCONCLUSIVE): T13 escalates to anko since the loop's own derivation is the audit target — this would be the second loop infrastructure escalation in 6 turns and warrants anko review.",
  "consumed_seed_md": true
}
```

`consumed_seed_md: true`. seed.md 2026-05-16 lines 50-51 explicitly call out "**critic** turns: independently audit theorist's claims against the empirical data + Yan-Li-Saito framework" as a campaign workload. T11's [Refuted] secular-DDI verdict directly contradicts seed.md's own stated hypothesis (lines 18-22) — auditing this contradiction is the precise leveraged D1 move, and the cheapest possible escalation step before the 22:00 JST julia window opens for the gamma_dr=0 falsifier. The seed's "what 'essence' looks like" section lines 96-104 lists "bug-discovery if reproduction or audit reveals SpinorBEC.jl framework error (highest individual D1 leverage)" — T11 §2.4's rotation-invariance argument is exactly the kind of claim where audit could either confirm or surface a code/derivation inconsistency.

## E. Self-review checklist

- [x] Read `runs/_loop/state.json` (turn=12, history through T11 NOOP, drift all green, last_judge=NOOP).
- [x] Read `runs/_loop/seed.md` (2026-05-16 afternoon Barnett-pumping campaign; D1+D3 dominant; critic role explicit).
- [x] Read `runs/_loop/theorist/turn_11.md` (full content in two reads; §1-§2.9, §3-§4, §5-§9).
- [x] Read `runs/_loop/director/turn_9.md` (T9 critic-audit pattern as reference for this turn's structure).
- [x] Read `runs/_loop/sim/turn_10.md` (T10 docstring landing, BCH leak thread).
- [x] Read `runs/_loop/judge/turn_10.json` (T10 PASS, no issues).
- [x] Read `runs/_loop/_local/scheduler_12.json` (TEXT_ONLY, julia rejected, critic in allowed_workloads, 23291s window-left).
- [x] Read ≥1 memory: `rotating_basis_loss_support.md` + cross-checked `src/hamiltonian/interactions/losses.jl` and `src/foundation/types/ddi_loss.jl` to verify rank-2 CG implementation exists (critical for Audit-5 numerical check).
- [x] Considered NOT dispatching critic — challenged with researcher (B-2: deferrable until critic flags Q-blocker), theorist (B-3: §B4 rotation trap + un-audited compounding), implementer_text (B-4: premature without audit), implementer_sympy (B-5: subordinate to critic), noop (B-6: §A5 fail with cheap-leveraged-move available). Critic wins on §B3 textbook fit + §B6 drift compliance + §B8 scheduler compliance + seed.md explicit + T11 §8 Option A self-recommendation.
- [x] §6 brief is specific: 7 numbered materials in read-order, 10 numbered audit items including a load-bearing numerical check (rank-2 CG vs T11's factor 12), explicit out-of-scope list, output format with concrete T13 recommendation. Critic needs no clarifying questions.
- [x] Justified why THIS turn — the audit gates julia spend at 22:00 JST window (~6.5h out). Waiting would either delay the falsifier or commit julia to an un-audited mechanism. Critic NOW is the precise moment of leverage.
- [x] `consumed_seed_md: true` — seed.md "critic turns ... audit theorist's claims" is honored verbatim. seed.md's own hypothesis is the audit target (T11 [Refuted]s it); the audit gates whether anko's hypothesis or T11's replacement banks.
