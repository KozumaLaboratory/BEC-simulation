---
turn: 18
subagent: director
topic_tags: [barnett, coupled-lindblad, numerical-integration, sign-reversal-audit, gamma-dr-falsifier, pre-julia-quantitative-pred, d1-tier-lift, implementer-sympy]
paper_section: null
depends_on: [11, 13, 14, 15, 16, 17, "theorist/turn_17.md §4.3 §5.1 §8 Q8.3", "judge/turn_16_critic_audit.md §3 Findings 1-3"]
produces: "implementer_sympy numerical integration of T17's coupled coherent+dissipative Lindblad master equation (T1) in the 13-dim F=6 spin Hilbert space. Trajectory ⟨F_z⟩(t; ±Ω) at three γ_dr values (0, 0.005, 0.02), checking (a) whether closed-form τ_asym=4.9 ms agrees with full ODE, (b) whether the sign-reversal claim in T17 §8 Q8.3 holds (γ_dr cascade flips Δ⟨F_z⟩(±Ω) from +5.96 at γ=0 to -4.60 empirical at γ=0.02), (c) generating the *quantitative* pre-prediction for the 22:00 JST julia γ_dr=0 falsifier run. Plus 5 differential-Ω predictions on the t⁴ scaling law."
---

# Turn 18 — Director Report

## 1. Project state snapshot

- **Barnett campaign turn 8 of an ~8-turn arc.** T11 theorist (rank-1 wrong, τ=6 ms) → T12 critic (rank-2 fix) → T13 sympy (τ_cascade=78 ms) → T14 researcher (Born-Markov no enhancement; p_z=0.315 authoritative) → T15 sympy (Candidate C refuted, full ODE never reaches 5.02 within 30 ω⁻¹) → T16 critic WEAK_PASS (cascade framework correct kernel but missing coherent p_perp Rabi channel) → **T17 theorist coupled Lindblad derivation: τ_asym = (6/(F·p_perp²·p_z·|Ω|))^(1/4) = 4.9 ms — FAILS [7,14] ms by factor 1.5 (too fast); time-averaged Δ⟨F_z⟩ = 3.11 = 70% of empirical 4.60; coherent Rabi explains most of the asymmetry magnitude.** T17 NOOP verdict (no implementer dispatch this turn per theorist's own §9 directive).
- **T17 has a load-bearing unaudited claim (§8 Q8.3 / §5.1 Table)**: at γ_dr=0 the prediction is **+Ω → 0.030, −Ω → 5.993** (Δ = +5.96, *positive*). The EMPIRICAL is **+Ω → 5.02, −Ω → 0.42** (Δ = +4.60, but with the *sign of ⟨F_z⟩ for each Ω opposite to T17's prediction*). T17 hand-wave-rationalizes this as "γ_dr cascade pulls everything down + rotating-frame energetic bias -Ω(L_z+F_z) protects +Ω side" — this is **a re-import of T11's mechanism that T16 critic specifically FAILed as 'assertion not derivation'**. T17 §8 Q8.4 admits this is "rehabilitated as late-time picture" — but the rehabilitation chain is currently: coherent Rabi (early, t≤1.4 ms) → cascade pulls down (mid, t~50ms) → rotating-frame bias gates +Ω from cascading (late, t→∞). The middle and late steps are NOT derived in T17. So the prediction at γ_dr=0.02 (empirical condition) is *not* given by the T17 closed form — only the γ_dr=0 instantaneous value at t=30 ω⁻¹ is.
- **T17 §4.3 explicitly states** the closed-form τ_asym=4.9 ms misses [7,14] ms because (i) Taylor breaks down at t~1.2 ω⁻¹, (ii) state-conditioned dissipator action makes γ_dr asymmetric in ±Ω (lower-m rungs have higher W^CG), (iii) trap orbital coupling not captured. T17 §6 Q8.1 explicitly defers quantitative τ_asym to "T18 implementer_sympy numerical integration."
- **T17 5 consistency checks PASS** (T5.1-T5.5): reproduces T15 -3/140 ω⁻¹ exact rational, rank-2 jump operators, γ_dr→∞ recovers cascade, p_perp→0 recovers cascade, Ω→p_z resonance. The derivation framework is internally consistent.
- **Scheduler T18 (`runs/_loop/_local/scheduler_18.json`)**: policy `TEXT_ONLY`, `decision=go`, `window_seconds_left = 18026` (≈ 5h 0m until 22:00 JST opens PROBE_DRIVEN window for julia). `allowed_workloads` = theorist / researcher / critic / implementer_text / implementer_sympy / noop. Probe is clean (12.59 GB VRAM, 1% util, no foreign julia) but window-policy lock holds. Implementer_sympy `typical_duration` ~600-900s; 18026s window left = plenty. ✓
- **Drift signals (T17 history)**: topic_repetition=0.0 (each turn moved a sub-issue), subagent_repetition=0.333, manuscript_delta_zero=0.0, code_delta_zero=0.0, **verdict_drift=0.5** (yellow plateau — T17 was NOOP, did not reset the streak from T15 FAIL → T16 WEAK_PASS → T17 NOOP), cost_inflation=1.103 (yellow advisory at T17). DRIFT_COST_INFLATION advisory active. §B7: prefer cheap routes; implementer_sympy ~1.0-1.3M effective is mid-cost. **Need a verdict-class outcome that breaks the 0.5 drift.**
- **Subagent rotation last 7 turns**: T11 theorist / T12 critic / T13 implementer_sympy / T14 researcher / T15 implementer_sympy / T16 critic / T17 theorist. Implementer_sympy ran T13+T15 consecutively (2-in-a-row consumed); **T16 critic and T17 theorist intervened — rotation now reset, T18 implementer_sympy is rotation-clean.** Critic: T12 + T16 (2 ago) — close, but not adjacent. Theorist: T11 + T17 (just ran) — adjacent block at T18. Researcher: T14 (4 ago).

## 2. Recent-turn audit (last 3)

| Turn | Topic | Verdict | Value delivered | Was it right? |
|---|---|---|---|---|
| T15 | implementer_sympy cascade initial-slope Candidate C | FAIL_PHYSICS | τ_init_slope = 67.5 ms; full ODE never reaches 5.02 within 30 ω⁻¹; T11's claim 5 demoted to [Refuted] for cascade framework; all τ-definitions inside cascade-only land in 50-80 ms | YES — closed Candidate C decisively, established framework is incomplete |
| T16 | critic audit of cascade framework | CRITIC_WEAK_PASS | Kernel verification PASS, framework FAIL: missing coherent p_perp Rabi channel quantified (p_perp=0.220 → 1.05 rad by 7 ms = empirical onset); concrete theorist brief for T17; γ_dr=0 julia falsifier config | YES — exact §B4 anti-pattern intervention; named missing physics with numerical thresholds |
| T17 | theorist coupled Lindblad derivation | NOOP (theorist self-directive) | Coupled coherent+dissipative Lindblad in Ω-rotating frame; H^rot = -(p_z-Ω)F_z - p_perp F_x; t⁴ closed form Δ⟨F_z⟩ = F·p_perp²·p_z·Ω·t⁴/6; τ_asym = 4.9 ms; γ_dr=0 prediction Δ=5.96 at t=30; all 5 consistency checks PASS; explicit deferral to T18 implementer_sympy for quantitative τ at γ_dr=0.02 (§6 Q8.1) | MIXED — strong framework derivation; closed form FAILs [7,14] ms; sign-reversal claim (Q8.3) is unaudited; T11 mechanism partially rehabilitated without derivation chain |

**Trajectory check (§B4)**: T11→T13→T14→T15→T16→T17 = 6 different sub-issues. Topic_repetition 0.0-0.125. **Verdict_drift stuck at 0.5 since T15** (FAIL→WEAK_PASS→NOOP — none of these are PASS class). T17 NOOP did not reset because theorist refused to dispatch implementer (§9). **T18 must produce a verdict-class outcome that resolves the closed-form vs full-ODE gap.**

**Specific concern (T17 sign-reversal mystery)**: T17 derives γ_dr=0 prediction Δ⟨F_z⟩(±Ω) = +5.96 (positive — +Ω is the side that *drops*). Empirical at γ_dr=0.02 has +Ω=5.02 (the side that *stays high*). T17 §8 Q8.3 says γ_dr cascade pulls *everything* down over τ_dr~70 ms but rotating-frame energetic bias -Ω(L_z+F_z) for +Ω protects from cascade. This rehabilitates T11 mechanism but is hand-waved — not in the coupled Lindblad equation (which has no orbital DOF). **The full coupled Lindblad (T1) numerically integrated at γ_dr=0.02 should give Δ⟨F_z⟩(±Ω) with the WRONG empirical sign if the orbital channel is necessary** (because T1 has no L_z coupling — it's spin-only). This is the load-bearing test. If T18 numerical (T1) at γ_dr=0.02 reproduces empirical Δ=4.60 with correct sign, T17 framework is Tier-2. If T18 numerical (T1) gives wrong sign or Δ=-something at γ_dr=0.02, T17's spin-only single-particle Lindblad is INSUFFICIENT, and the orbital -Ω(L_z+F_z) channel must be added (next turn).

## 3. Bottleneck analysis

Filtered to TEXT_ONLY allowed_workloads, ranked by leverage = (value × p_this_turn_moves_it) / cost. The bottleneck after T17 is *not* "derive more theory" but "numerically test the T17 coupled equation against (a) the t⁴ closed form, (b) the empirical sign-asymmetry direction, (c) generate quantitative pre-prediction for the 22:00 JST julia run."

### B-1: implementer_sympy — numerically integrate T17's coupled Lindblad (T1) in 13-dim Hilbert

*Issue*: T17 §6 Q8.1 explicitly defers: "Quantitative τ_asym in the empirical window needs numerical integration of (T1) on 13-dim Hilbert space, not analytic. The t⁴ closed form (T3-Taylor) is only the leading asymptotic; the full curve needs to be integrated to find when Δ⟨F_z⟩/N = 1 at finite γ_dr. T18 implementer_sympy task."

*Category*: physics gap (D1 framework verification — Tier-1.5 → Tier-2 lift). Specifically, the test is whether the coupled Lindblad (spin-only, single-particle) reproduces:
(i) empirical Δ⟨F_z⟩/N = 4.60 at γ_dr=0.02, t=30 ω⁻¹, with **correct sign** (+Ω at 5.02, −Ω at 0.42 — opposite of T17's γ_dr=0 prediction!);
(ii) closed-form τ_asym = 4.9 ms at γ_dr=0 matches the full ODE Δ⟨F_z⟩=1 crossing;
(iii) the γ_dr=0 prediction Δ=5.96 instantaneous, Δ=3.11 time-averaged — for the 22:00 JST falsifier;
(iv) the t⁴ scaling τ_asym ∝ Ω^(-1/4) across Ω ∈ {0.1, 0.3, 0.5, 0.7, 1.0}.

*Leverage*: **5**. Cost ~1.0-1.3M effective (matches T13/T15 implementer_sympy turns). Value:
- Tests T17 closed form at multiple γ_dr (0, 0.005, 0.02) and multiple Ω — establishes the τ_asym formula's validity range.
- **Decides whether the spin-only single-particle Lindblad SUFFICES or whether orbital (L_z) channel must be added** (sign-reversal test): if T18 numerical at γ_dr=0.02 gives Δ⟨F_z⟩ with wrong sign or magnitude vs empirical 4.60, the spin-only framework is REFUTED and T19 theorist must add the orbital channel (T11 §2.4 -Ω(L_z+F_z) bias).
- Produces quantitative pre-prediction for the 22:00 JST julia γ_dr=0 run (converts T17 §5.2 thresholds from analytical to numerically-validated).
- Closes the §B6 DRIFT_VERDICT_DRIFT yellow plateau (T15 FAIL→T16 WEAK_PASS→T17 NOOP→**T18 PASS or FAIL_PHYSICS** — either is verdict-class).

*What moves it*: **implementer_sympy** with brief specifying: scipy `solve_ivp` (LSODA / Radau for the linear Lindblad), 13×13 density matrix flattened to 169-real-DOF, 3 γ_dr values × 2 Ω values × 5 Ω-sweep points, integrate to t=30 ω⁻¹, plot Δ⟨F_z⟩(t; ±Ω), output trajectories.csv + figure + report.

### B-2: critic — audit T17 §8 Q8.3 sign-reversal claim

*Issue*: T17 §8 Q8.3 reconciles "γ_dr=0 predicts +Ω→0.030, empirical at γ_dr=0.02 has +Ω→5.02" by re-importing T11's rotating-frame energetic bias. But T16 critic explicitly FAILed T11's "γ_dr is THE rate" as "assertion not derivation." T17 reintroduced this mechanism without re-deriving it.

*Category*: verification gap.

*Leverage*: **3**. Cost ~0.8-1.0M. Risk: this is meta-meta after T16; critic on T17 §8 alone could find more assertion-grade hand-waves but won't produce a quantitative test like B-1 does. **A critic audit + an implementer numerical test is the right pair**; the implementer numerical is the higher-leverage one because it produces a falsifiable trajectory that EITHER confirms T17's chain OR forces orbital-channel addition. Critic audit alone produces another WEAK_PASS at best, doesn't reset drift.

### B-3: theorist — derive next-order correction (state-conditioned dissipator action)

*Issue*: T17 §4.3 admits state-conditioned dissipator action (lower-m rungs have larger W^CG, asymmetric power between ±Ω) and trap orbital coupling are missing. Could derive analytically.

*Category*: physics gap.

*Leverage*: **2**. Risks: (a) theorist would be 2-in-a-row (T17 just ran). §B4 rotation rule + seed.md "no more than 2 same-subagent in a row" — block. (b) State-conditioned dissipator is a 13-dim Lindblad problem; an analytical closed form is unlikely (T17 already noted this requires numerical integration). Theorist on this is the wrong tool — implementer_sympy is. **Reject.**

### B-4: researcher Q17.1 / Q17.2 (spin-coherent-state literature + dissipator coherences)

*Issue*: T17 §10 raised two research queries. Q17.1 (citation for spin-coherent stretched-state Rabi formula) is housekeeping. Q17.2 (Lindblad off-diagonal dissipator coherences) is theoretical.

*Category*: docs/research gap.

*Leverage*: **1**. Q17.1 is citation-polish (de-prioritized per `feedback_manuscript_is_not_the_essence.md`). Q17.2 is potentially load-bearing but downstream of B-1 (if B-1 numerical shows the diagonal-in-|m⟩ dissipator suffices, Q17.2 is moot). **Reject — wrong sequencing.**

### B-5: noop until 22:00 JST (≈5h wait)

*Issue*: Just wait for julia window. T17 produced the falsifier prediction; theoretically the 22:00 JST julia run is the decisive test.

*Category*: meta.

*Leverage*: **1**. Wastes 5h. Critically, **the 22:00 JST julia run at γ_dr=0 tests ONE point (anko's parameters at γ_dr=0)**. T18 implementer_sympy can test:
- γ_dr=0 (matches the queued julia run)
- γ_dr=0.005 (intermediate, sanity check)
- γ_dr=0.02 (matches anko's empirical condition) — **critical: does T17 framework reproduce the empirical sign-asymmetry?**
- 5 Ω points for the τ ∝ Ω^(-1/4) scaling law

These give an **enriched comparison space** for the julia run when it eventually fires. If T17 closed form holds in 13-dim numerical, the julia run is a confirmation; if it doesn't, T18 gives a much earlier signal of which sub-channel is missing. **Reject noop — leverage exists.**

### B-6: implementer_text (premature docstring/manuscript)

*Issue*: Could write `@info` advisory in `src/hamiltonian/integrator/split_step.jl` about the rotating-frame coupled-Lindblad picture.

*Category*: docs gap.

*Leverage*: **0**. Premature (theory not yet quantitatively validated). seed.md explicitly de-prioritizes (`feedback_manuscript_is_not_the_essence.md`). **Reject.**

## 4. Strategic options for THIS turn

| # | Move | Subagent | Now-or-later | Cost |
|---|---|---|---|---|
| 1 | **implementer_sympy: numerically integrate T17 coupled Lindblad (T1) in 13-dim, 3 γ_dr × 5 Ω points, test sign-reversal + τ scaling** | **implementer_sympy** | NOW — T17 §6 Q8.1 explicit deferral; produces quantitative pre-prediction for 22:00 JST | ≤ 25 min, ≤ 1.3M |
| 2 | critic audit T17 §8 Q8.3 sign-reversal claim | critic | LATER if B-1 shows discrepancy | ≤ 15 min, ≤ 1.0M |
| 3 | theorist next-order correction | theorist | rejected — 2-in-a-row rule + wrong tool (numerical not analytical) | ≤ 25 min, ≤ 1.5M |
| 4 | researcher Q17.1/Q17.2 | researcher | rejected — Q17.1 housekeeping, Q17.2 downstream of B-1 | ≤ 15 min, ≤ 1.0M |
| 5 | noop until 22:00 JST | n/a | rejected — leveraged work exists | 0 |
| 6 | implementer_text docstring | implementer | rejected — premature | ≤ 10 min |

**Pick: Option 1 (implementer_sympy).**

Why:

- **§A5 axis (a) — verify existing-implementation claim**: T17 derived the coupled Lindblad framework but only verified the t=0+ first derivative and analytic limits. The closed-form τ_asym=4.9 ms FAILs [7,14] ms; T17 attributes this to truncation + state-conditioned dissipator. **The verification IS to numerically integrate (T1) and check whether the truncation alone or also the missing orbital channel is the FAIL cause.** T17 §4.3 itself names this as "the question." ✓
- **§A5 axis (c) — research-grounded theory**: T17 derivation was research-grounded (Sakurai spin coherent states, KU 2012 / SKU 2013 rank-2 cascade). T18 numerical validates that derivation against full 13-dim ODE. ✓
- **§B3 implementer dispatch rule (verbatim)**: "Dispatch when the bottleneck is 'code benchmark vs known reference' or 'add an effect whose theory is already settled' — no theorist directive needed first; you provide the directive in §6.brief." T17 just settled the theory; T18 implements the numerical integration. The "known reference" is the closed-form τ_asym=4.9 ms + the empirical 4.60 asymmetry. ✓
- **§B4 rotation favorable**: implementer_sympy ran T13+T15 (2-in-a-row); T16 critic + T17 theorist intervened (2 different subagents); rotation block is now reset. T18 implementer_sympy is the natural sequencing per T17 §9 self-directive. ✓
- **§B6 DRIFT_VERDICT_DRIFT (0.5 plateau)**: numerical integration produces a verdict-class outcome (PASS if reproduces empirical 4.60 with correct sign; FAIL_PHYSICS if wrong sign indicates orbital channel needed; either resets the streak). T17 NOOP didn't reset. ✓
- **§B7 quota budget**: cost_inflation 1.103 yellow (DRIFT_COST_INFLATION advisory). Implementer_sympy ~1.0-1.3M effective per T13/T15 baseline (1.36M and 1.11M respectively) — mid-cost, fits cap. Cheaper than theorist (~1.5M). ✓
- **§B8 scheduler compliance**: implementer_sympy ∈ allowed_workloads; TEXT_ONLY policy honored (sympy/scipy/numpy is python, not julia); 18026s window-left = 5h >> typical_duration ~900s. ✓
- **§D1 dominant (verification depth axis)**: This is the framework-level Tier-1.5 → Tier-2 lift. T17 was Tier-1.5 [coherent-Rabi-channel-derived, full-coupled-Lindblad-numerical-pending, γ_dr=0-julia-pending]. T18 closes the "full-coupled-Lindblad-numerical-pending" half. The "γ_dr=0-julia-pending" half is the 22:00 JST run. ✓
- **seed.md fit verbatim**: seed.md line 56-58 "implementer_sympy turns: verify identities (commutators, conservation laws)." T18 implementer_sympy verifies the τ_asym closed form, the sign-reversal, and the t⁴ scaling — all numerically. seed.md line 41-45 D1+D3 priority Q1 ("Closed-form derivation of τ_Barnett(Ω, p, c_dd, c_1, F)... Does it match the empirical 7-14 ms?") — T18 directly addresses Q1 by numerically integrating the closed form's parent equation.
- **`feedback_manuscript_is_not_the_essence.md`**: numerical integration of a theory derivation tied to anko's empirical data — pure D1 verification, not manuscript polish. ✓
- **`feedback_cost_overhead_is_the_cost.md`**: don't deliberate, dispatch. ✓

Why NOT Option 2 (critic): a critic audit of T17 §8 Q8.3 would re-examine the sign-reversal hand-wave but cannot *test* it. The numerical integration in B-1 tests it directly — if the sign-asymmetry direction at γ_dr=0.02 in T1 matches empirical, T17 §8 Q8.3 hand-wave is vindicated; if not, refuted. Numerical >> critic audit for this specific question. Critic can also wait for T19 if B-1 reveals new framework gaps.

Why NOT Option 3 (theorist next-order): theorist 2-in-a-row violates seed.md + the question is fundamentally numerical (state-conditioned dissipator with 13×13 ρ is a numerical problem not analytic).

Why NOT Option 5 (noop): a 5h wait gains nothing AND the julia run at 22:00 JST will only produce ONE data point (γ_dr=0 at anko's Ω=±0.5). T18 implementer_sympy produces 3×5=15 data points (3 γ_dr × 5 Ω) all at the closed-form prediction's claimed validity — much richer comparison space for julia.

## 5. Calibrated progress check

| Axis | Status | Evidence |
|---|---|---|
| Physics completeness (D1+D3) | **at risk → on track if T18 numerical lands** | T17 derived coupled Lindblad framework. Closed form τ_asym=4.9 ms is qualitative leading-order. T18 numerical tests whether the framework (spin-only single-particle Lindblad) reproduces empirical Δ=4.60 with correct sign at γ_dr=0.02. If yes → Tier-2. If no → spin-only insufficient, T19 must add orbital channel. |
| Verification depth (D1 dominant, Tier 1 → Tier 3) | **stuck at Tier-1.5 [coherent-Rabi-channel-derived, full-coupled-Lindblad-numerical-pending, γ_dr=0-julia-pending]** | T17 named two pending half-steps. T18 closes half #1 (numerical-pending). 22:00 JST julia closes half #2. Both gating for Tier-2. |
| Manuscript (de-prioritized) | **on track (de-prioritized)** | 3 unmerged branches T7-T10 awaiting anko-merge. Not this turn. |
| Reproducibility | **on track** | T13/T15 sympy scripts under runs/_loop/sim/turn_{13,15}/ as reproducible artifacts. T18 will produce runs/_loop/sim/turn_18/ with same convention. |
| Loop infrastructure | **one yellow** | verdict_drift=0.5 plateau (T15 FAIL→T16 WEAK_PASS→T17 NOOP). Per §B6: "surface for critic dispatch or noop" — T16 critic already surfaced, T17 NOOP didn't resolve. **T18 must produce verdict-class outcome (PASS or FAIL_PHYSICS) to break drift.** Implementer_sympy with numerical falsifiable target → verdict-class guaranteed. cost_inflation 1.103 yellow advisory — choose cost-efficient: implementer_sympy at ~1.2M fits. Rotation: implementer_sympy clean (T16+T17 intervened); theorist blocked (2-in-a-row); critic adjacent (T16, 2 ago — but redundant after T17 specific deferral); researcher 4-ago (fine, but wrong sequencing). |

**Mark**: D1 framework-level Tier-2 lift is the load-bearing axis. T18 numerical integration of T1 is the precise unblock between T17's analytical framework and the 22:00 JST julia γ_dr=0 falsifier. After T18:
- If numerical reproduces empirical Δ=4.60 within 30% with correct sign at γ_dr=0.02 → PASS. T19 = noop or critic audit. 22:00 JST julia tests γ_dr=0 prediction quantitatively.
- If numerical gives Δ=5.96 at γ_dr=0 + Δ=4.60 with correct sign at γ_dr=0.02 → PASS (T17 framework Tier-2 validated for both regimes).
- If numerical gives Δ=5.96 at γ_dr=0 but WRONG sign or magnitude at γ_dr=0.02 → FAIL_PHYSICS. T19 = theorist add orbital -Ω(L_z+F_z) channel. 22:00 JST julia still proceeds as γ_dr=0 confirmation (which would still match).
- If numerical fails to reproduce closed-form τ_asym=4.9 ms at γ_dr=0 → FAIL_PHYSICS, theorist re-derives.

## 6. Dispatch decision

```json
{
  "subagent_type": "implementer",
  "rationale": "T17 theorist (`runs/_loop/theorist/turn_17.md`) derived the coupled coherent+dissipative Lindblad master equation (T1) in the Ω-rotating frame with H^rot = -(p_z-Ω)F_z - p_perp F_x, and the closed-form τ_asym = (6/(F·p_perp²·p_z·|Ω|))^(1/4) = 4.9 ms (FAILs [7,14] ms by factor 1.5). T17 §6 Q8.1 explicitly defers quantitative τ_asym in the empirical window to 'T18 implementer_sympy task': numerical integration of (T1) on 13-dim Hilbert is required. Additionally, T17 §8 Q8.3 contains a load-bearing unaudited claim: the γ_dr=0 prediction is Δ⟨F_z⟩(±Ω) = +5.96 (instantaneous, +Ω→0.030, -Ω→5.993) but empirical at γ_dr=0.02 is Δ=+4.60 with +Ω→5.02, -Ω→0.42 — the *signs of individual ⟨F_z⟩(±Ω) are SWAPPED* between T17's γ_dr=0 prediction and the empirical γ_dr=0.02 observation. T17 rationalizes via 'rotating-frame energetic bias -Ω(L_z+F_z) protects +Ω from cascading' — but this re-imports T11's mechanism that T16 critic specifically FAILed as 'assertion not derivation,' and the spin-only single-particle Lindblad (T1) has no L_z DOF to encode this protection. The decisive test is: numerically integrate (T1) at γ_dr=0.02 with anko's parameters and check whether the empirical sign-asymmetry direction emerges. If yes, T17 framework is Tier-2 validated. If no, the spin-only single-particle Lindblad is INSUFFICIENT and T19 must add orbital channel. §B3 implementer dispatch rule: 'code benchmark vs known reference; theory already settled' — T17 settled theory, T18 benchmarks numerically against T17 closed form + empirical 4.60. §B4 rotation: implementer_sympy ran T13+T15 (2-in-a-row consumed); T16 critic + T17 theorist intervened → rotation reset. §B6 DRIFT_VERDICT_DRIFT 0.5 plateau (T15 FAIL → T16 WEAK_PASS → T17 NOOP, none verdict-class PASS); T18 numerical guarantees verdict-class (PASS if reproduces 4.60 with correct sign, FAIL_PHYSICS otherwise). §B7 cost_inflation 1.103 yellow → prefer cheap; implementer_sympy ~1.0-1.3M cheaper than theorist ~1.5M. §B8 scheduler: implementer_sympy ∈ allowed_workloads, TEXT_ONLY (python/scipy/numpy, no julia), 18026s window left = 5h. seed.md line 56-58 verbatim 'implementer_sympy turns: verify identities (commutators, conservation laws)' — T18 verifies the τ_asym closed-form vs full ODE + sign-reversal + t⁴ scaling. Produces enriched 3×5=15 quantitative pre-predictions for the 22:00 JST julia run (vs julia alone giving 1 binary data point at γ_dr=0).",
  "brief": "## Action: compute_sympy (numerical scipy/sympy integration, NOT julia)\n\n## Goal\n\nNumerically integrate T17's coupled coherent+dissipative Lindblad master equation (T17 eq T1) in the 13-dim F=6 spin Hilbert space, at multiple γ_dr and Ω values. Test three load-bearing claims:\n\n1. **T17 closed-form τ_asym = 4.9 ms (eq T3-closed-form)** — is it accurate within Taylor radius (t < 1.2 ω⁻¹)?\n2. **Sign-reversal claim (T17 §8 Q8.3)** — does the spin-only single-particle Lindblad alone reproduce the empirical asymmetry direction (+Ω→5.02, -Ω→0.42 at γ_dr=0.02, t=30 ω⁻¹)? Or is the orbital -Ω(L_z+F_z) channel necessary?\n3. **t⁴ scaling law τ_asym ∝ |Ω|^(-1/4)** — does the full ODE follow this scaling across Ω∈{0.1, 0.3, 0.5, 0.7, 1.0}?\n\nProduces the **quantitative pre-prediction** for the 22:00 JST julia γ_dr=0 falsifier run (T17 §5.2 thresholds expanded to full trajectory).\n\nThis is T17 §6 Q8.1 + §12 Scenario A/B/C deferral cashed in.\n\n## Materials to read (in priority order)\n\n1. `runs/_loop/theorist/turn_17.md` — entire file, but especially:\n   - §0 conventions (units, spinor ordering, authoritative p_z=0.315, p_perp=0.220, Ω=±0.5, γ_dr=0.02)\n   - §2.3 eq (T1) — the coupled master equation you must integrate\n   - §3 — first derivative -3/140 ω⁻¹ + second derivative +p_perp²·F at t=0+ (use as sanity checks)\n   - §4 — closed form τ_asym = 4.9 ms (validate against numerical)\n   - §5 — γ_dr=0 prediction Δ=5.96 instantaneous, Δ=3.11 time-average (validate)\n   - §8 Q8.3 — the sign-reversal mystery you are testing\n   - §12 Scenario A/B/C — your output should classify which scenario the numerical lands in\n2. `runs/_loop/sim/turn_15.md` §5 Target 1 — exact-rational W^CG_{m,q} 13×2 table (q ∈ {-1,-2}) which you need for the rank-2 jump operators. Verify your construction matches these values.\n3. `runs/_loop/sim/turn_13/rank2_cg_prefactor.py` — T13 reference implementation of the rank-2 CG cascade (read as a reference for the dissipator structure; expand to include the coherent H^rot term).\n4. `runs/_loop/judge/turn_16_critic_audit.md` §3 Findings 1-3 — the framework gap T17 closed; you're testing that closure numerically.\n5. `runs/_loop/sim/turn_15/cascade_initial_slope.py` — T15 reference for sympy + scipy integration pattern; reuse the IVP solver structure.\n6. `runs/_loop/seed.md` line 56-58 — your role is 'verify identities (commutators, conservation laws)' plus quantitative numerical integration.\n\n## Computation targets (priority order)\n\n### Target T1 (HIGH): Construct 13-dim spin-F=6 operators and Lindblad RHS\n\nBuild numerically (sympy or scipy, both OK — use numpy float64 for the actual ODE integration):\n\n- F_z = diag(6, 5, 4, ..., -5, -6), 13×13 real diagonal\n- F_x, F_y from the standard ladder F_± = sqrt((F∓m)(F±m+1)) recursion. Note: spinor ordering convention c=1↔m=+F, so the (1,1) entry of F_z is +6 (T17 §0).\n- Verify [F_x, F_y] = iF_z, [F_y, F_z] = iF_x, [F_z, F_x] = iF_y as sanity checks.\n- H^rot(Ω) = -(p_z - Ω)·F_z - p_perp·F_x as a 13×13 Hermitian matrix.\n- Rank-2 jump operators L_{m,q} = sqrt(γ_dr·W^CG_{m,q})·|m+q⟩⟨m| for q∈{-1,-2}, m s.t. m+q ≥ -F. So 12 jump ops for q=-1 (m∈{+6,+5,...,-5}) and 11 for q=-2 (m∈{+6,+5,...,-4}). Total 23 jump operators.\n- Use W^CG_{m,q} from T15 §5 Target 1 (NORMALIZED such that γ_dr is the average rate — Z=2/5 for F=6 per T17 §0). Reproduce W^CG_{+6,-1} = 11/14 and W^CG_{+6,-2} = 1/7 as a sanity check.\n- Lindblad super-operator: dρ/dt = -i[H^rot, ρ] + Σ_j (L_j ρ L_j† - {L_j†L_j, ρ}/2). Vectorize as flattened ρ ∈ R^{169} (real and imag parts of upper triangle, or full complex 169-vector — your choice).\n\n### Target T2 (HIGH): Integrate (T1) at γ_dr=0, Ω=±0.5, anko's p_z=0.315 p_perp=0.220, t∈[0, 30] ω⁻¹\n\nInitial state ρ(0) = |+F⟩⟨+F| = diag(1, 0, 0, ..., 0).\n\nIntegrate with scipy `solve_ivp` (LSODA or Radau, both fine; LSODA stiff-detection handles the cascade well at γ_dr=0.02). dt_max = 0.01 ω⁻¹ for resolved oscillations; rtol=1e-8, atol=1e-10.\n\nAt each saved t (say 300 points uniformly in [0, 30]), compute ⟨F_z⟩(t; ±Ω) = Tr(ρ·F_z).\n\nVerify:\n- t=0+: dF_z/dt = 0 (because γ_dr=0 and ⟨F_y⟩|_0=0).\n- t=0+: d²F_z/dt² = p_perp²·F = 0.0484·6 = 0.290 ω⁻². Check via finite difference of ⟨F_z⟩(t) at small t.\n- Time-average ⟨Δ⟨F_z⟩⟩_avg over [0, 30] should equal F·(cos²β_- - cos²β_+) = 6·(0.9330 - 0.4144) = 3.11 per T17 §4.1.\n- Instantaneous ⟨Δ⟨F_z⟩⟩(t=30) should be near 5.96 per T17 §5.1 (the t=30 happens to land near Rabi peak alignment).\n- Compare closed-form τ_asym = 4.9 ms (T3-Taylor) to the actual Δ⟨F_z⟩(t)=1 crossing time numerically.\n\n### Target T3 (HIGH): Integrate at γ_dr=0.02 (anko's empirical condition), same Ω=±0.5\n\n**This is the critical sign-reversal test.**\n\nIntegrate exactly as Target T2 but with γ_dr=0.02. Compute ⟨F_z⟩(t; ±Ω) for t ∈ [0, 30] ω⁻¹.\n\n**Check the empirical-comparison:**\n- ⟨F_z⟩/N at t=30 ω⁻¹ for +Ω — does it equal **5.02** (empirical) within 20%?\n- ⟨F_z⟩/N at t=30 ω⁻¹ for -Ω — does it equal **0.42** (empirical) within 20%?\n- Δ⟨F_z⟩/N at t=30 — does it equal **+4.60** (empirical) with **correct sign**?\n\nThree-bin classification (matches T17 §12 scenario):\n- **PASS (Scenario A confirmed)**: Δ matches 4.60 with correct sign (+Ω side at higher ⟨F_z⟩) within 30%. T17 framework spin-only single-particle Lindblad SUFFICES.\n- **PARTIAL (Scenario B)**: |Δ| ∈ [1, 3] with correct or wrong sign. Coupled Lindblad qualitatively captures but quantitatively misses; need orbital channel for full match.\n- **FAIL (Scenario C)**: |Δ| < 1 with wrong sign, OR magnitude very wrong. Spin-only Lindblad REFUTED for the late-t regime; T19 must add orbital -Ω(L_z+F_z) channel.\n\n### Target T4 (MEDIUM): Ω-sweep at γ_dr=0 — t⁴ scaling test\n\nAt γ_dr=0, integrate (T1) for Ω ∈ {0.1, 0.3, 0.5, 0.7, 1.0} (5 points, take both ±Ω is symmetric so just one per |Ω|). For each Ω compute the first time at which |Δ⟨F_z⟩(±Ω; t)| = 1. Call this τ_asym(Ω).\n\nFit τ_asym(Ω) ~ A·Ω^(-α). T17 §5.3 predicts α = 1/4 = 0.25.\n\nReport:\n- Fit α and A.\n- α within 0.05 of 0.25 → t⁴ scaling CONFIRMED.\n- α outside [0.20, 0.30] → t⁴ scaling REFUTED; either Taylor breaks down for some Ω or another mechanism interferes.\n\n### Target T5 (MEDIUM): Bridge — γ_dr=0.005 intermediate run\n\nIntegrate at γ_dr=0.005 (4× weaker than empirical) with Ω=±0.5. This bridges γ_dr=0 (Target T2) and γ_dr=0.02 (Target T3). If the empirical-mismatch in T3 is a γ_dr-dependent effect, γ_dr=0.005 should show an intermediate trajectory between T2 and T3 endpoints. Check.\n\n### Target T6 (LOW): Diagnostics\n\n- At each γ_dr×Ω combo, also save ⟨F_x⟩(t), ⟨F_y⟩(t), Tr(ρ), Tr(ρ²) (purity), per-m population diagonal ρ_{mm}(t).\n- Verify Tr(ρ) = 1 at all times (Lindblad form preserves trace).\n- Verify Tr(ρ²) starts at 1 (pure state) and decreases (decoherence).\n- Per-m populations vs T15 cascade-only — compare; at small p_perp/Ω limit they should agree.\n\n## Output\n\nProduce `runs/_loop/sim/turn_18/coupled_lindblad_integration.py` + `runs/_loop/sim/turn_18.md` report with structure mirroring `runs/_loop/sim/turn_15.md`.\n\n**Report sections** (mirror T15 structure):\n1. Directive received (paraphrase brief above).\n2. Branch / commit.\n3. Commands executed.\n4. Metrics (wall_time_sec, etc.).\n5. **Headline verdict**: did T17 framework reproduce empirical 4.60 with correct sign at γ_dr=0.02? (Scenario A / B / C per Target T3.)\n6. Target T1: operator construction sanity checks (commutators, W^CG values, jump-op count).\n7. Target T2: γ_dr=0 trajectory. Numerical Δ at t=30 vs T17 prediction 5.96; numerical τ_asym at Δ=1 crossing vs T17 closed-form 4.9 ms; numerical time-average Δ vs T17's 3.11. Include figure (matplotlib) showing ⟨F_z⟩(t; ±Ω) at γ_dr=0.\n8. Target T3: γ_dr=0.02 trajectory. ⟨F_z⟩(t=30; +Ω) vs 5.02 empirical; ⟨F_z⟩(t=30; -Ω) vs 0.42 empirical; Δ vs +4.60 empirical with SIGN check. Include figure.\n9. Target T4: Ω-sweep fit α value and A. PASS/FAIL on t⁴ scaling.\n10. Target T5: γ_dr=0.005 bridging trajectory.\n11. Target T6: diagnostics summary.\n12. Verdict classification per T17 §12 Scenario A/B/C.\n13. Sequencing for T19: 3 scenarios (A → noop or critic; B → theorist add orbital channel; C → theorist refute spin-only Lindblad, theorist Candidate B / full-DDI Q_{xy/xz/yz}). 22:00 JST julia γ_dr=0 run proceeds in all three scenarios.\n\nFigure: 2-panel matplotlib (one panel γ_dr=0, one γ_dr=0.02), both panels show ⟨F_z⟩(t; +Ω) and ⟨F_z⟩(t; -Ω) traces. Mark empirical endpoint values 5.02 and 0.42 as horizontal dashed lines on γ_dr=0.02 panel. Mark T17 closed-form 4.9 ms τ_asym as vertical dashed line on γ_dr=0 panel.\n\n## Tool usage\n\n- `Read` for materials.\n- `Write` for `runs/_loop/sim/turn_18/coupled_lindblad_integration.py` and `runs/_loop/sim/turn_18.md`.\n- `Bash` for running the python: `uv run --with sympy --with scipy --with numpy --with matplotlib python3 runs/_loop/sim/turn_18/coupled_lindblad_integration.py 2>&1 | tee runs/_loop/sim/turn_18/output.log`. Mirror T15 setup.\n- DO NOT use julia.\n- DO NOT use GPU.\n- DO NOT use WebFetch/WebSearch (T14 already covered the literature; T17 cites all needed sources).\n- DO NOT modify src/.\n- DO NOT commit until tests pass (git checkout -b auto/turn_18_coupled-lindblad-numerical-integration first).\n\n## Out-of-scope\n\n- Full-DDI Q_{xy/xz/yz} coupling (Candidate B; deferred to T19+ if T17 framework FAILs at T3 sign-test).\n- Many-body / spatial DDI / GPE coupling (single-particle approximation; spin-only).\n- Trap orbital -Ω(L_z+F_z) channel (T19 deferral if T3 FAIL).\n- K3 loss channel (F_z-blind).\n- Yan-Li-Saito droplet framework (ε_dd>1, different regime).\n- Manuscript / docstring polish.\n- Test-tier increase (don't run Pkg.test).\n\n## Sanity checks before submitting report\n\n- Did your t=0+ derivatives match T17 §3.1 (-3/140 at γ_dr=0.02) and §3.2 (+p_perp²·F at γ_dr=0)?\n- Did Tr(ρ) stay 1 (within 1e-10) throughout integration?\n- Did your W^CG_{+6,-1} match 11/14 ≈ 0.7857 and W^CG_{+6,-2} match 1/7 ≈ 0.1429?\n- Did γ_dr=0 Ω=+0.5 numerical ⟨F_z⟩ at t=30 land near 0.030 (T17 §5.1)?\n- Did γ_dr=0 Ω=-0.5 numerical ⟨F_z⟩ at t=30 land near 5.993 (T17 §5.1)?\n- Is the sign-test on γ_dr=0.02 explicitly classified as Scenario A/B/C?\n- Did you provide a τ_asym fit on the Ω-sweep with α explicit?\n- Is the figure saved as PNG/PDF for reproducibility?\n\n## Expected wall time\n\n13-dim Lindblad with 23 jump operators, 5 (γ_dr×Ω) combos × ~30000 RHS evals each + 5 more for Ω-sweep ≈ 10-20 sec compute. Total turn ~10-15 min including IO + report writing. Well within scheduler 18026s window.\n\n## Output dispatcher format\n\nReturn the standard implementer JSON in §13 of the report (`action`, `target_files`, `experiment_config`, `expected_outcome`, `falsification_criterion`, `estimated_cost`). Branch commit format `auto/turn_18_coupled-lindblad-numerical-integration`.",
  "expected_outcome": "(1) `runs/_loop/sim/turn_18/coupled_lindblad_integration.py` runnable script integrating T17 eq (T1) at 3 γ_dr × 5 Ω = 15+ trajectories. (2) `runs/_loop/sim/turn_18.md` report with 13 sections including headline Scenario A/B/C classification on the γ_dr=0.02 sign-reversal test. (3) Matplotlib figure showing ⟨F_z⟩(t;±Ω) at both γ_dr=0 and γ_dr=0.02 with empirical endpoints marked. (4) τ_asym fit on Ω-sweep with α explicit (T17 predicts 1/4=0.25; numerical should be within 0.05 if T17 closed form holds). Most-likely outcome (60% probability): Scenario B (partial) — coupled Lindblad reproduces magnitude qualitatively but sign-asymmetry at γ_dr=0.02 has wrong direction or attenuated magnitude (because the spin-only single-particle Lindblad has no orbital -Ω(L_z+F_z) channel to gate the +Ω-side cascade-protection that T11 hand-waved). Less likely (25%): Scenario A — full agreement, T17 framework Tier-2 confirmed. 15%: Scenario C — wrong sign and wrong magnitude, T17 spin-only insufficient, must add orbital. All three are verdict-class outcomes resetting the 0.5 drift plateau. The numerical also produces enriched comparison data for the 22:00 JST julia γ_dr=0 run.",
  "expected_cost": "≤ 25 min wall-clock, ≤ 1.3M effective tokens. Comparable to T13 (1.36M) and T15 (1.11M) implementer_sympy turns. Compute itself ~10-20 sec (13-dim Lindblad with scipy is fast); bulk of cost is reading materials + writing 13-section report + figure generation.",
  "if_fails_next_step": "If Scenario A (full agreement with empirical 4.60, correct sign): T19 = noop or critic audit T18 to validate scenario classification; 22:00 JST julia run proceeds as quantitative confirmation of T17 closed form Δ=5.96 at γ_dr=0. If Scenario B (partial match, magnitude OK sign wrong or attenuated): T19 = theorist add orbital -Ω(L_z+F_z) channel to (T1), extending Hilbert to spin × first few |L_z⟩ modes; 22:00 JST julia still proceeds as binary test. If Scenario C (spin-only refuted): T19 = theorist Candidate B (full-DDI Q_{xy/xz/yz} coherent coupling per T11 §2.8, Yan-Li-Saito m+v=ℓ-style for trapped). If the script fails to run (sympy/scipy error, divergence): T19 = implementer_sympy debug + theorist if framework issue; this happened on no prior turn so probability < 10%. If Tr(ρ) violations indicate Lindblad implementation bug: T19 = implementer_sympy debug + critic to audit T18 implementation correctness.",
  "consumed_seed_md": true
}
```

`consumed_seed_md: true`. seed.md 2026-05-16 priority Q1 ("Closed-form derivation of τ_Barnett... Does it match the empirical 7-14 ms?") — T18 directly tests whether T17 closed form τ_asym=4.9 ms agrees with full ODE; tests whether the framework predicts the empirical 4.60 with correct sign at γ_dr=0.02. seed.md priority Q2 ("Mechanism audit: is the secular-DDI suppression picture rigorous?") — T17 reintroduced T11's rotating-frame energetic bias; T18 numerical tests whether spin-only single-particle Lindblad reproduces empirical asymmetry direction WITHOUT that orbital channel (i.e., whether T17 §8 Q8.3 hand-wave is necessary or whether the spin-only mechanism suffices). seed.md priority Q3 ("Cross-validation with KU / SKU / Yan-Li-Saito") — addressed via T14 already; T18 doesn't re-engage. seed.md line 56-58 verbatim: "implementer_sympy turns: verify identities (commutators, conservation laws)" — T18 verifies T17 closed form + sign-reversal + t⁴ scaling. seed.md "essence" §1 (closed-form τ_Barnett prediction that data validates → Tier 1→2 lift) — T18 is the precise deliverable closing the Tier-1.5 → Tier-2 gap for the spin-only single-particle Lindblad framework. seed.md "essence" §2 (falsifiable parameter dependence Ω, p, c_dd) — T18 Ω-sweep tests the τ ∝ Ω^(-1/4) scaling falsifiably; 22:00 JST julia is the c_dd-side falsifier.

## E. Self-review checklist

- [x] Read `runs/_loop/state.json` (turn=18, last_judge=NOOP for T17, verdict_drift=0.5 yellow plateau, cost_inflation=1.103 yellow advisory, drift_advisory=DRIFT_COST_INFLATION).
- [x] Read `runs/_loop/seed.md` (Barnett campaign, ≤2 same-subagent rule, implementer_sympy role explicit at line 56-58 "verify identities").
- [x] Read `runs/_loop/_local/scheduler_18.json` (TEXT_ONLY, implementer_sympy ∈ allowed_workloads, julia rejected, 18026s window-left = 5h to 22:00 JST PROBE_DRIVEN).
- [x] Read `runs/_loop/director/turn_17.md` (rationale chain for T17 theorist dispatch).
- [x] Read `runs/_loop/theorist/turn_17.md` (all 14 sections — coupled Lindblad derivation, closed form τ_asym=4.9 ms, γ_dr=0 prediction Δ=5.96, sign-reversal mystery §8 Q8.3, explicit T18 deferral §6 Q8.1).
- [x] Read `runs/_loop/judge/turn_16_critic_audit.md` (framework FAIL on missing coherent channel; T11 mechanism FAILed as assertion).
- [x] Read memory: `barnett_spin_pumping_observed_2026_05_16.md` (empirical 5.02 / 0.42 endpoints, opposite of T17 γ_dr=0 prediction 0.030/5.993).
- [x] Read `runs/_loop/schedule.yaml` (TEXT_ONLY until 22:00 JST; PROBE_DRIVEN overnight).
- [x] Considered NOT dispatching implementer_sympy: challenged with critic (B-2: meta-meta after T16, can't test sign-reversal numerically), theorist (B-3: 2-in-a-row rule + wrong tool for numerical question), researcher (B-4: Q17.1 housekeeping, Q17.2 downstream), noop (B-5: 5h waste + julia gives 1 binary point vs 15-point comparison), implementer_text (B-6: premature). Implementer_sympy wins on §A5 axis-a + §B3 dispatch rule (code benchmark vs known reference) + §B4 rotation reset by T16/T17 + §B6 verdict-class outcome guaranteed + §B7 cost-efficient at 1.0-1.3M.
- [x] §6 brief is specific: 6 numbered materials, 6 priority-ordered computation targets, output schema (13 report sections), tool whitelist, sanity-check list, out-of-scope list, expected wall-time estimate, branch naming. Implementer needs no clarifying questions.
- [x] Justified why THIS turn: T17 §6 Q8.1 + §12 explicit "T18 implementer_sympy task" deferral; verdict_drift 0.5 plateau needs a PASS/FAIL_PHYSICS outcome (T17 NOOP didn't reset); 5h before julia window opens — produces enriched 15-point quantitative pre-prediction vs julia's 1 binary point alone; sign-reversal test from T17 §8 Q8.3 is unaudited and load-bearing, numerical integration is the only way to test it.
- [x] `consumed_seed_md: true` — Q1 numerical match to 7-14 ms tested; Q2 mechanism audit tested via sign-reversal at γ_dr=0.02 with vs without orbital channel; line 56-58 implementer_sympy role honored; essence §1 (Tier 1→2 lift) and §2 (falsifiable Ω dependence) both directly addressed.
