# `runs/` — YAML experiment configurations

Each subdirectory holds one experiment: a `config.yaml` describing the
pipeline (ground state → dynamics → analyzers), with results written
in-place (`result_legacy.jld2`, `data.json`, `dashboard_data.json`,
`figs/`, …).

Run a single experiment:

```bash
julia --project=. -e 'using SpinorBEC; run_yaml("runs/<name>/config.yaml")'
```

For GPU runs add `LD_LIBRARY_PATH=/usr/lib/wsl/lib` (WSL2). For long-
running batches use `setsid nohup …` (see `scripts/run_thesis_batch.sh`).

---

## Active / current

### Klaus 2022 reproduction (Option γ rotating-basis spinor GPE)

| dir | what it tests |
|---|---|
| `klaus_option_gamma_full/` | Klaus Dy164 main protocol — vertical bias GS → 20 ms tilt ramp → 50 ms freq chirp → steady stir. Full Klaus paper protocol on a 32×32×16 grid. |
| `klaus_eu151_spin_excitation/` | Spinor extension on Eu151. Same protocol, F=6 D=13. Showed m=+F: 0.77 → 0.83 (DDI-driven). |
| `klaus_eu151_weak_field/` | Adiabatic-following companion run (m=+F frozen at 0.998). |
| `klaus_dy164_weak_field/` | 4th corner of Berry / adiabatic comparison on Dy164. |
| `klaus_eu151_v2_{full,no_ddi,no_stir,baseline}/` | 4-corner mechanism map (proven Klaus protocol). |
| `klaus_eu151_p_{300,1000}/` | p-sweep — Berry-vs-adiabatic regime onset. |

### Thesis batch (24 runs, ~3h wall-clock w/ 3-parallel)

`thesis_batch/<name>/` — generated by `scripts/generate_thesis_batch.jl`:

- B1 thesis main: `dy164_main_500ms`, `eu151_full_500ms`, `eu151_no_ddi_500ms`,
  `eu151_no_stir_500ms`, `eu151_baseline_500ms`, `eu151_no_chirp_500ms`
- B2 p-sweep: `p_30`, `p_100`, `p_300`, `p_1000`, `p_3000`, `p_10000`
- B3 c1 / c_dd sweep: `c1_FM_strong`, `c1_FM_weak`, `c1_AFM_strong`,
  `c1_AFM_weak`, `cdd_half`, `cdd_2x`
- B4 protocol / numerical: `duration_1500ms`, `duration_2000ms`,
  `grid_48cube`, `epsilon_1e6`, `theta_25deg`, `theta_45deg`

Driver: `scripts/run_thesis_batch.sh` (sequential batches, 3-parallel chunks).

### Eu151 phase-diagram backbone (legacy spinor solver)

| dir | what it tests |
|---|---|
| `eu151_mz_scan/` | target_Mz ∈ [-6, 0], 13 points. Phase diagram baseline. |
| `eu151_mz_scan_ensemble/` | seed ensemble of `eu151_mz_scan`. |
| `eu151_phase_pq/`, `eu151_phase_pq_hires/` | (p, q) phase scan. |
| `eu151_phase_mz_q/` | (Mz, q) phase scan. |
| `eu151_phase_c1_q/`, `eu151_phase_c1_cdd/` | c1 / c_dd Cartesian scans. |
| `eu151_p_continuation/`, `eu151_gs_continuation/` | continuation / arc-length GS. |
| `eu151_b1_c1_sweep_template/` | template for B-1 (uniform vs FL polarization). |

### Eu151 EdH (Einstein–de Haas)

| dir | what it tests |
|---|---|
| `eu151_edh/`, `eu151_edh_3d/`, `eu151_edh_ext/` | EdH dynamics — Mz-Lz transfer. |
| `eu151_edh_smoke/` | quick smoke version. |
| `eu151_edh_scan/`, `eu151_edh_c1_scan/` | parameter scans. |
| `eu151_edh_tomography/` | EdH + per-m density tomography. |

### Eu151 specific physics

| dir | what it tests |
|---|---|
| `eu151_droplet/` | Lima-Pelster Q5 droplet stabilisation. |
| `eu151_rosensweig/` | Rosensweig instability surface pattern. |
| `eu151_kibble_zurek/`, `eu151_kz_slow/` | KZ defect counting. |
| `eu151_spontaneous_magnetization/` | SSB on quench. |
| `eu151_pancake_{fl_probe,fl_scan,strong_fl}/` | quasi-2D FL phase. |
| `eu151_fl_vortex/`, `eu151_faraday_vortex/` | FL / Faraday vortex states. |
| `eu151_klaus_freq_scan/` | Klaus 2022-style stir-frequency scan on Eu. |
| `eu151_sgpe_thermal/` | SGPE finite-T relaxation. |
| `eu151_c1_scan_comparison/`, `eu151_c1_scan_quasi2d/` | c1-scan continuation. |
| `eu151_gs_lhy/`, `eu151_gs_quasi2d/` | GS sub-experiments. |
| `eu151_convergence/` | grid / dt convergence study. |

### Standalone smokes / validations

| dir | what it tests |
|---|---|
| `option_gamma_micro/`, `option_gamma_smoke/` | minimal CI for Option γ pipeline path. |
| `klaus_dt_test/` | Klaus dt-stability micro. |
| `klaus_scalar_validation/` | scalar eGPE adiabatic-limit cross-check. |
| `klaus2022_smoke/`, `klaus2022_full/`, `klaus2022_freq_scan/` | early Klaus reproduction (legacy spinor path; superseded by Option γ). |
| `quasi_2d_eu151/` | quasi-2D DDI sanity. |

### Mechanism map v1 (DEPRECATED — instant theta jump → all null)

`klaus_eu151_mechanism_{baseline,full,no_ddi,no_stir}/` — kept for archival.
v1 used `theta_const=0.611` from t=0 (instant tilt jump), which silently put
all four corners in the deeply-adiabatic regime and produced m=+F: 1.0 → 1.0
across the board. Superseded by `klaus_eu151_v2_*` (proper ramp protocol).

---

## Layout / shared bits

- `_dashboard_cache/` — server-rebuildable atlas cache (gitignored).
- `samples/`, `scenarios/`, `shared/`, `tools/` — fixtures + helper YAML
  fragments referenced by other configs.

## Status conventions

- `result_legacy.jld2` present → run completed.
- `figs/figs.json` present → dashboard frames generated.
- `frames/` → per-step PNG dump (gitignored).
- `dashboard_data.json` → dashboard ingest payload (gitignored).

## Adding a new run

1. Pick the closest existing config as a template (most thesis work descends
   from `klaus_eu151_v2_full/config.yaml` or `klaus_eu151_spin_excitation/`).
2. Copy to `runs/<your_name>/config.yaml`, override only what changes.
3. Run via `run_yaml("runs/<your_name>/config.yaml")`.
4. For batch jobs, follow the pattern in `scripts/generate_thesis_batch.jl`
   + `scripts/run_thesis_batch.sh`.
