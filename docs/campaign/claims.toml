# Claim ledger — one row per physics claim, with its status as a FIELD, not as prose.
#
# WHY THIS FILE EXISTS
#
# `docs/campaign/edh_quench_polarisation_decision.md` is an excellent change log and
# a bad place to look up what is currently true. It records, in order, that
# `|Ω|/ω_⊥ = 0.468 ± 0.003` was the prescription, then that it is not re-derivable,
# then that `0.68 ± 0.04` replaces it. Reading it front to back gives the right
# answer. Reading any one section does not, and nothing in the tree says which
# section is the live one.
#
# The measured consequence is in `docs/manuscript/klaus_protocol_sheet.md`. That
# file carries a correct, detailed retraction at lines 17-70 — and then states the
# retracted numbers bare at lines 105, 125, 227, 232-234, 255 and 293, including
# inside the numbered lab protocol an experimentalist would actually follow. The
# retraction is 160 lines above the point of use. Both PR #401 and PR #403 updated
# the retraction block and left the protocol steps alone. Nothing was careless:
# the status of a claim was not a thing the tree could hold, so it lived in
# whichever paragraph someone last remembered to edit.
#
# So: status is a field. `superseded` and `refuted` are different values, `refuted
# with no replacement` is representable, and `uncertainty` is REQUIRED — a row that
# cannot state what bounds it is a row nobody has bounded, and that is information
# rather than an omission to be defaulted away.
#
# WHAT A ROW IS
#
#   id                  stable kebab identifier; referenced by supersedes/superseded_by
#   claim               the assertion, one sentence, in the form it should be quoted
#   status              live | scoped | superseded | refuted | suggested | open | closed
#                       `closed` = an `open` gap shut by doing the work; it keeps the
#                       row rather than deleting it, because "this was looked at" is
#                       the part a reader needs, and it requires a `note` saying what
#                       shut it.
#   type                A (code correctness) | B (physics agreement) | C (model fidelity)
#                       — the taxonomy in CAMPAIGN.md §5, unchanged
#   scope               what the claim is true OF: grid, seed count, closure, field,
#                       preparation, duration. A claim with no scope is a claim
#                       nobody has bounded.
#   uncertainty         REQUIRED, non-empty. If nothing bounds it, say so in those
#                       words — `unbounded: <why>` — do not leave it out.
#   uncertainty_basis   fit | grid | dt | seed | control | none — how the number above
#                       was obtained. `none` and a filled `uncertainty` is a
#                       contradiction the gate rejects.
#   evidence            run configs / arm sets the claim rests on
#   evidence_status     in_tree | partial | absent — whether `evidence` resolves in
#                       this repo. `absent` is common and is not a defect by itself;
#                       it is the L1 hook, and it is what makes a re-derivation
#                       cost visible before someone promises one.
#   commit              producing commit
#   doc, section        where the claim is written out (the L3 anchor)
#   pr                  the PR that last moved this row
#
# Optional:
#   supersedes / superseded_by   ids. Both directions are declared; the gate checks
#                                they agree, so a half-migrated retraction is red.
#   window / reduction / boundary REQUIRED when `quantity` names a reduction
#                                (peak / argmax / optimum / endpoint). A peak is a
#                                peak OF something OVER something, and at 10.4 nT
#                                three readings of the SAME cached arms put the
#                                argmax at three different points while 16 of 20
#                                arms had theirs on the hold's first frame -- a
#                                decaying pre-hold transient ranked as the cascade.
#                                `boundary` says what was done when the argmax hit
#                                the edge: reject / accept / unchecked / n/a.
#                                `accept` records the defect rather than the fix and
#                                is used where that is what happened. `unchecked` is
#                                NOT `reject`.
#                                The detector is a WORD LIST and unknown forms are
#                                innocent: a quantity naming no reduction is not
#                                required to carry a window, because a rule that
#                                reddens `eta_start` gets deleted.
#   retraction_evidence          REQUIRED on superseded/refuted rows. WHICH AXIS
#                                the killing measurement moved, and both values.
#                                NOT "the same convergence ladder" -- a refutation
#                                usually differs deliberately on exactly one axis
#                                (32^3 -> 64^3, 8 ms -> 16 ms, 1 seed -> 3), and
#                                demanding sameness would forbid the move that does
#                                the work. What is wanted is that a reader can tell
#                                a refinement from a different experiment. Use
#                                `premise_dissolved: <why>` when the claim never
#                                rested on a measurement -- that is a real outcome
#                                and must not look like a blank.
#                                WHY: eight times in this repo a retraction was
#                                itself wrong. The shape was never haste; it was
#                                that the killed claim rested on a run and the
#                                killing one was allowed to rest on prose.
#   retired_literal              REQUIRED on superseded/refuted rows. String or
#                                array — a retracted claim usually survives in
#                                more than one wording, and the lab instruction
#                                and the mechanism sentence are different points
#                                of use. This is what the gates scan for.
#   replacement_literal          String or array. What must appear instead.
#                                DELIBERATELY ABSENT on a row refuted with no
#                                replacement — `edh-window-1p3nt-0p3` is the case:
#                                at 1.3 nT there is no operating window to
#                                relocate the number to, and an empty field says
#                                that where a placeholder value would hide it.
#   prediction                   a statement fixed BEFORE the measurement
#   prediction_registered        before | after — `after` is not a prediction and the
#                                gate refuses to count it as one
#   prediction_outcome           hit | miss | pending
#   note
#
# THE FIRST DOCUMENT POURED IN is the rotation-assisted EdH quench decision doc
# (#343 → #392 → #401 → #403). It was chosen because it contains every shape a
# claim can have: a number with a fit uncertainty, the number it replaced, a
# prescription refuted with no replacement, a claim whose replacement is a SHAPE
# rather than a number, a hypothesis with a registered test, a gate that failed,
# and a scope restriction that applies to all of them at once.

schema_version = 1

# --- Type A: the instrument -------------------------------------------------

[[claim]]
id = "eu-field-sign-knob-live"
claim = "Under `H = -p·F_z`, `p ≡ -g_F μ_B B`, a ¹⁵¹Eu ground state at B_z = +0.01 G lands at m = -6 and at B_z = -0.01 G at m = +6, on current code."
status = "live"
type = "A"
scope = "16³, DDI off, spin-coherent θ=π/2 seed, GPU. Positive control for every null in the same document."
uncertainty = "⟨F_z⟩ = ±6.0 exactly; the observable is discrete (argmax m), so there is nothing to bound."
uncertainty_basis = "control"
evidence = ["16³ DDI-off pair, Bz = ±0.01 G, spin_coherent init_theta = 1.5708"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "2"
pr = 392
note = "Reproduces bce2068f's own 2026-07-29 measurement. Exists because this repo has shipped a table of nulls taken through a dead knob (mistake_null_from_a_degenerate_knob_2026_07_31)."

[[claim]]
id = "edh-mirror-flips-every-axial-quantity"
claim = "A mirror arm is defined by the mirror operation: Ω, the spin projection AND B_z must all flip. Negating only the knob being scanned does not produce a mirror."
status = "live"
type = "A"
scope = "General. Instantiated here for the klaus_quench pair; the same rule caught #338 (Barnett), where the missing partner was B_y rather than B_z."
uncertainty = "Not a measured quantity. The measured consequence — the repaired pair agrees to < 1e-5 — is claim `edh-mirror-pair-repaired`."
uncertainty_basis = "none"
evidence = ["runs/klaus_quench/klaus_quench_omm0p5_keeprot.yaml", "runs/klaus_quench/klaus_quench_omp0p5_keeprot_mirror.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "5"
pr = 392
note = "Gated by test/workflow/test_mirror_pairs_flip_every_axial_quantity.jl (fast tier), with a canary that reconstructs the post-bce2068f broken pair and requires the B_z clause to reject it."

[[claim]]
id = "edh-mirror-pair-repaired"
claim = "The klaus_quench mirror pair, broken by `bce2068f`, is repaired, and the repair is confirmed by measurement: arm G reproduces its partner's peak P_adj = 0.24311 to five digits."
status = "live"
type = "A"
scope = "32³, CPU, one seed, `lhy: none`, the two `*_keeprot*` arms only."
uncertainty = "< 1e-5 relative between the two arms (G4). Bounded by the comparison itself, not by a fit."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench/klaus_quench_omm0p5_keeprot.yaml", "runs/klaus_quench/klaus_quench_omp0p5_keeprot_mirror.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "5.1"
pr = 392

# --- Type B: the preparation ------------------------------------------------

[[claim]]
id = "edh-requires-anti-aligned-preparation"
claim = "The rotation-assisted EdH quench must prepare the stretched state ANTI-ALIGNED with B (the Zeeman-highest state). Every other Eu arc in this project prepares a Zeeman ground state and is aligned."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, one hold duration, ¹⁵¹Eu F=6 klaus_quench protocol. Stated as a relative orientation, never as an m label — `m = +F` means the opposite thing at the opposite field sign, which is the error that produced #343."
uncertainty = "The DIRECTION is far outside any plausible scatter: ×2.2 in peak P_adj and +16.5 % vs -0.45 % in rotation contrast, against a seed floor of 0.000 % over 5 live seeds. The VALUES are not converged — grid systematic +2.47 % (G3), one seed, one duration."
uncertainty_basis = "control"
evidence = ["§3.2 four arms (klaus_quench_om0p0.yaml, klaus_quench_omm0p5_keeprot.yaml)", "§3.6 three arms E/F/G"]
evidence_status = "partial"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "4"
pr = 392
prediction = "If the controlling variable is which end of the Zeeman ladder the prepared state sits on — rather than the sign of B_z — then m = +F at B_z > 0 must show the rotation enhancement return."
prediction_registered = "before"
prediction_outcome = "hit"
note = "Registered in §3.5 and tested in §3.6: +16.5 % returned against -0.45 % at the same field. This is the standard Kawaguchi-Saito-Ueda EdH configuration (PRL 96, 080405) and should have been looked up before it was measured."

[[claim]]
id = "edh-enhancement-absent-at-aligned-preparation"
claim = "At the ALIGNED (Zeeman-ground) preparation the rotation enhancement is absent: -0.45 % on peak P_adj, of the wrong sign, while the Ω knob is demonstrably acting on the state (overlap 0.95, not 1)."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, B_z = +0.01 G, m = -F, Ω ∈ {0, -0.5}."
uncertainty = "-0.45 % against a dt/2 reproducibility floor of 0.029 % and a seed floor of 0.000 %; the sign is resolved, the magnitude is at the level of the grid systematic (2.5 %) and should not be quoted as a number."
uncertainty_basis = "dt"
evidence = ["runs/klaus_quench/klaus_quench_om0p0.yaml", "runs/klaus_quench/klaus_quench_omm0p5_keeprot.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "3.4"
pr = 392
note = "This is why `0.468 ± 0.003` is not re-derivable AS POSED rather than merely stale — a parabolic optimum cannot be refined out of a flat, slightly negative response."

# --- The Ω operating window: the superseded row and its replacement ---------

[[claim]]
id = "edh-omega-window-0p468"
claim = "|Ω|/ω_⊥ = 0.468 ± 0.003 at B = 2.6 nT, from a 7-point parabolic fit."
status = "superseded"
retraction_evidence = """preparation, ALIGNED -> ANTI-ALIGNED (the axis the original never varied), plus sampling 7 points -> 20 cells: 0.468 +- 0.003 -> 0.68 +- 0.04. Not a refinement. At the aligned preparation the response is flat and slightly negative (-0.45 %), so there was no optimum to fit and the third significant figure was never supportable."""
superseded_by = "edh-omega-window-2p6nt"
retired_literal = ["0.468 ± 0.003"]
replacement_literal = ["0.68 ± 0.04", "0.68 +- 0.04"]
type = "B"
scope = "Was: 32³, one seed, hold-only, 2 ms delay — at the ALIGNED preparation, which is the regime where the enhancement does not exist."
uncertainty = "±0.003 was quoted from the fit covariance alone. It omitted the grid systematic (2.5 %) and rested on a preparation that produces no optimum to fit, so the third significant figure was never supportable."
uncertainty_basis = "fit"
evidence = ["7-point Ω refinement, 2026-05-27"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/archive/klaus_quench_protocol_pivot_2026-05-26.md"
section = "Ω refinement"
pr = 392
note = "The old value is not recovered by the re-derivation. It is not a less precise version of 0.68 — it was measured at the wrong preparation."

[[claim]]
id = "edh-omega-window-2p6nt"
claim = "|Ω*|/ω_⊥ = 0.68 ± 0.04 at B = 2.6 nT, +24.9 % over Ω = 0. Two significant figures, not three."
status = "live"
supersedes = ["edh-omega-window-0p468"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, hold-only + 2 ms delay, ANTI-ALIGNED preparation, 20 Ω cells."
uncertainty = "±0.04 on the vertex from a 5-point parabolic fit over |Ω| ∈ [0.58, 0.80], c₂ = -2.70, fit rms 4.7e-3. Separately, the OBSERVABLE carries a +2.47 % one-signed grid systematic (32³→64³); that does not move the vertex but does bound any absolute P_adj quoted with it."
uncertainty_basis = "fit"
evidence = ["runs/klaus_quench/klaus_quench_om*_holdonly_delay2ms_refine.yaml (20 cells)"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.1"
pr = 392
note = "Criteria C1-C4 were fixed before launch: interior maximum required, > 5 % enhancement over Ω=0, ≥5-point fit with negative curvature and an interior vertex, digits limited by the fit. All four applied."

# --- What actually acts -----------------------------------------------------

[[claim]]
id = "edh-chirality-matched"
claim = "The enhancement requires matched chirality: Ω·sign(m_init) < 0 lowers the mode energy by ΔE = -Ω·ℓ, so the trap must be rotated counter to the stretched-spin direction."
status = "refuted"
retraction_evidence = """knob parity, a one-sided Omega scan -> +-Omega at |Omega| in {0.1, 0.3, 0.5}: the response is EVEN to 0.071 / 0.113 / 0.124 % against a dt/2 reproducibility floor of 0.029 %. An effect even in Omega cannot come from -Omega*l, which is odd, so the mechanism is refuted by parity rather than by a better fit."""
superseded_by = "edh-response-even-in-omega"
retired_literal = ["chirality opposite", "Ω · sign(m_init)", "Ω·sign(m_init)"]
# NOT the bare `sign(m_init)`: that also matches the KINEMATICS (`ℓ = − sign(m_init) · k`),
# which is correct and was never retracted. The refuted claim is the ENERGY rule
# `ΔE = −Ω·ℓ ⇒ matched chirality wins`, so the literal carries the `Ω ·`. Both spacings
# are listed because the two documents write it differently and whitespace is normalised
# but `·` adjacency is not.
replacement_literal = ["even in Ω", "even in Omega"]
type = "B"
scope = "Was stated as the mechanism of the whole protocol."
uncertainty = "unbounded: never measured against ±Ω. It was a mechanism assumed from the Coriolis term, not a fitted result."
uncertainty_basis = "none"
evidence = ["klaus_protocol_sheet.md mechanism one-liner, step 5, and the falsification row"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/manuscript/klaus_protocol_sheet.md"
section = "Mechanism"
pr = 392

[[claim]]
id = "edh-response-even-in-omega"
claim = "The response is EVEN in Ω to ≤ 0.124 %: both senses of rotation enhance identically and Ω = 0 is the minimum. Chirality is irrelevant to this observable."
status = "live"
supersedes = ["edh-chirality-matched"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT, |Ω| ∈ {0.1, 0.3, 0.5}."
uncertainty = "0.071 / 0.113 / 0.124 % at |Ω| = 0.1 / 0.3 / 0.5, against a dt/2 reproducibility floor of 0.029 %. The residual asymmetry is ~4× the floor, i.e. small but resolved — it is not exactly zero."
uncertainty_basis = "dt"
evidence = [
    "runs/klaus_quench/klaus_quench_omm0p1_holdonly_delay2ms_refine.yaml",
    "runs/klaus_quench/klaus_quench_omm0p3_holdonly_delay2ms_refine.yaml",
    "runs/klaus_quench/klaus_quench_omm0p5_holdonly_delay2ms_refine.yaml",
    "runs/klaus_quench/klaus_quench_omp0p1_holdonly_delay2ms_refine.yaml",
    "runs/klaus_quench/klaus_quench_omp0p3_holdonly_delay2ms_refine.yaml",
    "runs/klaus_quench/klaus_quench_omp0p5_holdonly_delay2ms_refine.yaml",
]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.2"
pr = 392
note = "An effect even in Ω cannot come from -Ω·ℓ, which is odd. This is the knob-parity argument: a one-sided scan gives magnitude only, ±Ω identifies the term."

[[claim]]
id = "edh-mechanism-is-centrifugal"
claim = "The enhancement is CENTRIFUGAL, not Coriolis: a static, non-rotating trap weakened to ω_eff = √(ω_⊥² - Ω²) reproduces the entire effect. Rotation is not required."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT. Verified at 4 matched points across ω_eff ∈ [0.6, 1.0], not only at the optimum."
uncertainty = "Worst disagreement 0.06 % across the range (§10.1); 0.06-0.19 % at the two §9.3 points. Against a dt/2 floor of 0.029 %, so the substitution is exact to within about 2-6× the reproducibility floor."
uncertainty_basis = "control"
evidence = ["34 static-trap arms, §10; 2 matched arms, §9.3"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.3"
pr = 392
note = "The control carries its own positive control: had the per-step `potential:` override been silently ignored, these arms would have returned the Ω = 0 value 0.527, not 0.659."

[[claim]]
id = "edh-variable-is-radial-not-density"
claim = "The operative variable is the radial confinement specifically, not the density: a density-matched weakening along z (same ω̄) lands BELOW the baseline, recovering -36 % of the gain."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT, three arms."
uncertainty = "The three arms are 0.52748 / 0.65864 / 0.48073 — separations of 25 % and 9 %, against a dt/2 floor of 0.029 %. The SIGN is what the arm establishes; the -36 % figure inherits the 2.5 % grid systematic."
uncertainty_basis = "dt"
evidence = ["baseline (1.000, 1.000, 1.1818); radial (0.714, 0.714, 1.1818); z-weakened (1.000, 1.000, 0.6026)"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "10.4"
pr = 392
note = "Rules out the density explanation. The reading that ℓ ≠ 0 orbital modes have their cost set by ω_⊥ is an interpretation and is NOT established here — see `edh-microscopic-reason-orbital-cost`."

[[claim]]
id = "edh-microscopic-reason-orbital-cost"
claim = "A softer radial trap helps because the ℓ ≠ 0 orbital modes the spin flip must populate become cheaper (larger cloud, lower kinetic cost)."
status = "suggested"
type = "B"
scope = "Interpretation of `edh-variable-is-radial-not-density`. Not measured."
uncertainty = "unbounded: no measurement addresses the microscopic mechanism. The VARIABLE is established; the reason behind the variable is not."
uncertainty_basis = "none"
evidence = []
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.3"
pr = 392

# --- The three field rows: one prescription, one absence, one shape ----------

[[claim]]
id = "edh-lab-prescription-2p6nt"
claim = "At B = 2.6 nT the lab prescription is a static radial trap at ω_⊥,eff/ω_⊥ = 0.71 — weaken the radial confinement by ~27 %, with no trap rotation. +24.9 % over the unweakened trap."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, B = 2.6 nT ONLY. Does not transfer to 1.3 or 5.2 nT — see the two rows below."
uncertainty = "Optimum located at ω_eff = 0.714 by a 34-arm static scan and independently at 0.68 ± 0.04 in |Ω| by the fit; the two agree. Quote one digit on the ratio. Absolute P_adj carries the +2.47 % one-signed grid systematic."
uncertainty_basis = "fit"
evidence = ["34 static-trap arms, §10.1-10.2"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "10.2"
pr = 392
note = "Replaces the rotating-anisotropic-trap prescription and removes the 'sign-convention mapping from simulation Ω to lab rotation direction' caveat with it."

[[claim]]
id = "edh-window-1p3nt-0p3"
claim = "|Ω|/ω_⊥ ≈ 0.3 at B = 1.3 nT."
status = "refuted"
retraction_evidence = """sampling, a 3-point Omega scan -> a static omega_eff scan over [0.65, 1.0] at the same field: 0.505 -> 0.514, +1.8 % and flat. The axis is the same one; only the density changed, and it shows there is no interior maximum for the number to relocate to."""
retired_literal = ["≈ 0.3 at B = 1.3 nT"]
type = "B"
scope = "Was: the 1.3 nT row of the three-field prescription table."
uncertainty = "unbounded: quoted from a 3-point scan with no fit uncertainty."
uncertainty_basis = "none"
evidence = ["3-point Ω scan {0.3, 0.5, 0.7}, 2026-05-27"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/manuscript/klaus_protocol_sheet.md"
section = "Three-regime Ω operating window"
pr = 401
note = "REFUTED WITH NO REPLACEMENT, and that is the finding rather than a gap. At 1.3 nT the response is flat: 0.505 → 0.514 over ω_eff ∈ [0.65, 1.0], +1.8 %. There is no operating window to relocate the number to. Do not re-fit it."

[[claim]]
id = "edh-window-5p2nt-0p5-0p6"
claim = "|Ω|/ω_⊥ ∈ [0.5, 0.6] at B = 5.2 nT."
status = "refuted"
retraction_evidence = """sampling, 3 points -> 20 points in omega_eff at 5.2 nT. The old window [0.5, 0.6] maps to omega_eff [0.80, 0.87], which the 20-point scan resolves as a DECLINING shoulder (0.50070 -> 0.49290) lying below both maxima. At 7 points (#401) the honest verdict was still `neither confirmed nor replaced`; the status moved because the sampling changed, not because anyone re-read the prose."""
superseded_by = "edh-two-branches-5p2nt"
retired_literal = ["[0.5, 0.6]"]
replacement_literal = ["hold duration", "hold-duration"]
type = "B"
scope = "Was: the 5.2 nT row of the three-field prescription table."
uncertainty = "unbounded: quoted from a 3-point scan with no fit uncertainty."
uncertainty_basis = "none"
evidence = ["3-point Ω scan {0.3, 0.5, 0.7}, 2026-05-27"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/manuscript/klaus_protocol_sheet.md"
section = "Three-regime Ω operating window"
pr = 403
note = "In ω_eff the old window is [0.80, 0.87], which the 20-point scan resolves as a DECLINING shoulder (0.50070 → 0.49290) lying below BOTH maxima. #401 could only say 'neither confirmed nor replaced' at 7 points; #403 upgrades that to refuted. The status moved because the sampling changed, not because anyone re-read the prose."

[[claim]]
id = "edh-two-branches-5p2nt"
quantity = "peak_P_adj @ B=5.2nT, weff-ordering"
window = "hold, 8 ms"
reduction = "max"
boundary = "accept"
claim = "At B = 5.2 nT the ω_eff response has TWO branches: a global maximum at ω_eff ≈ 0.55 (+17.0 %), a secondary at ≈ 0.77, and a dip between them at ≈ 0.65."
status = "refuted"
retraction_evidence = """hold duration, 8 ms -> 16 ms at four matched omega_eff. 0.650 goes from 5.05 % BELOW 0.550 to 6.2 % ABOVE it. The grid axis WAS moved (32^3 -> 64^3, gate D4) and could not see it: at the 8 ms hold, omega_eff 0.42-0.62 all peak on the last streamed frame (42/42), so the reported structure compared boundary values against one resolved maximum (0.650, frame 38/42). A 20-arm re-run reproduced the published values to five digits and reproduced the same truncation with them."""
supersedes = ["edh-window-5p2nt-0p5-0p6"]
superseded_by = "edh-5p2nt-ordering-is-hold-dependent"
retired_literal = ["TWO BRANCHES", "ω_eff ≈ 0.55"]
# NOT the bare "two branches": it is ordinary English and matched git branches in
# unified_spec_architecture.md and code branches in parameter_contract_with_Ueda.md.
# The uppercase form and the numeral are specific to this claim.
replacement_literal = ["hold duration", "hold-duration"]
type = "B"
scope = "32³ for the 20-point shape, 64³ for the dip and the adjacent peak. CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, B = 5.2 nT."
uncertainty = "REFUTED 2026-08-21 on an axis the original never varied. The 64³ check (D4) moved the GRID; it could not move the TRUNCATION, and the truncation was the problem: at the 8 ms hold, ω_eff 0.42-0.62 all peak on the LAST streamed frame (42/42), so the reported structure compared boundary values against one resolved maximum (0.650, frame 38/42). Doubling the hold to 16 ms REVERSES the ordering — 0.650 goes from 5.05 % BELOW 0.550 to 6.2 % ABOVE it. An ordering that inverts with hold duration is not a property of the response."
uncertainty_basis = "grid"
evidence = ["20 static-trap ω_eff arms at 5.2 nT; 2 arms at 64³ (ω_eff = 0.550, 0.650)"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "11"
pr = 403
prediction = "D1: if two maxima are separated by a dip > 1 %, report two branches and quote no single optimum. D4: the structure must survive 64³ or it is a grid artifact."
prediction_registered = "before"
prediction_outcome = "hit"
note = """The values reproduce exactly — a 20-arm re-run from committed configs at
`c5ac5e67` returns 0.51387 / 0.48791 / 0.50252 / 0.43935 against the published
0.51390 / 0.48790 / 0.50252 / 0.43930. Reproducing it to five digits did not make it
true: it reproduced the same truncation. That is the useful lesson here — an exact
re-run confirms the pipeline and says nothing about whether the observable was
resolved. D1 and D4 were both fixed before launch and both fired correctly on the
axes they covered; the axis nobody registered was hold duration."""


[[claim]]
id = "edh-5p2nt-ordering-is-hold-dependent"
quantity = "peak_P_adj @ B=5.2nT, weff-ordering"
window = "hold, 8 ms and 16 ms"
reduction = "max"
boundary = "accept"
claim = "At B = 5.2 nT the ordering of peak P_adj across ω_eff INVERTS between an 8 ms and a 16 ms hold: ω_eff = 0.650 goes from 5.05 % below 0.550 to 6.2 % above it. No ω_eff optimum is quotable at 5.2 nT."
status = "live"
supersedes = ["edh-two-branches-5p2nt", "edh-5p2nt-second-branch-has-no-mechanism"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, B = 5.2 nT, four ω_eff points (0.550 / 0.650 / 0.770 / 1.000) at each of two hold durations."
uncertainty = "The INVERSION is far outside any floor: 0.48791 → 0.62804 at ω_eff = 0.650 while 0.51387 → 0.59152 at 0.550, against a dt/2 floor of 0.029 % and a seed floor of 0.000 %. The 16 ms VALUES are not converged either — three of the four still peak at the last streamed frame (53/53), only ω_eff = 1.000 resolves (50/53). So this row bounds what is NOT true and deliberately quotes no optimum."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/manifest_hold2x.txt", "runs/klaus_quench_weff/klaus_weff0p650_B5p2nT_hold2p0x.yaml", "runs/klaus_quench_weff/klaus_weff0p550_B5p2nT_hold2p0x.yaml"]
evidence_status = "in_tree"
commit = "c5ac5e67"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "11.5"
pr = 0
note = """The diagnostic was the `frame` column, which the original analysis printed and
nobody read: a peak reported at frame 42 of 42 is a truncation, not a peak. Both sides
of the published dip had to be compared at the same resolution before the dip meant
anything, and they never were. Generalises past this instance: when an observable is
`maximum over the trajectory`, the argmax index IS part of the measurement, and an
argmax at the boundary is a statement that the run ended too early."""

[[claim]]
id = "edh-104nt-bump-at-0p65"
quantity = "peak_P_adj @ B=10.4nT, weff-scan, 32cubed, hold=8ms"
window = "whole_trajectory"
reduction = "max"
boundary = "accept"
claim = "At B = 10.4 nT there is a resolved local maximum at ω_eff ≈ 0.65, +22.4 % over a flat baseline, and it peaks INSIDE the hold window."
status = "superseded"
retraction_evidence = """observable window, whole trajectory -> inside the hold, on the SAME cached arms and with no new run: baseline 0.27574 -> 0.20139, enhancement +22.4 % -> +49.5 %. 16 of 20 arms had their argmax on frame 32, the hold's first frame, where the decaying pre-hold transient still dominates. The correction was already in the tree when the row was written (section 12.1, 97ec124e)."""
superseded_by = "edh-104nt-single-peak-in-hold"
retired_literal = ["0.27574"]
# NOT "+22.4 %": that percentage also names an unrelated quantity in the same
# document — static vs rotating at long time, §17 — and a literal that matches a
# different measurement is a predicate wider than the thing it hunts. The baseline
# VALUE is unique to this claim.
replacement_literal = ["+49.5 %", "0.20139"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, B = 10.4 nT. 20 points at 8 ms, two of them repeated at 16 ms."
uncertainty = "WRONG OBSERVABLE. The peak was taken over the WHOLE trajectory; at 10.4 nT that reads the pre-hold transient, which §12.1 had established a day earlier. The `flat baseline at 0.27574` is the transient, not a saturated cascade. Read in the hold the baseline is 0.20139 and the enhancement is +49.5 %."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff0p650_B10p4nT_hold2p0x.yaml", "runs/klaus_quench_weff/klaus_weff1p000_B10p4nT_hold2p0x.yaml"]
evidence_status = "in_tree"
commit = "c5ac5e67"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "11.5"
pr = 0
note = """SUPERSEDED 2026-08-21, and the correction was ALREADY IN THE TREE when this row
was written. §12.1 (`97ec124e`, 2026-08-20) measured that a whole-trajectory peak reads
the pre-hold transient at 10.4 nT — four arms differing only in the hold returned 0.26050
to five decimals, argmax frame 29, hold starting 32 — and stated the rule: the claim is
about the hold, so the peak must be taken inside it. I merged that section into my branch
and then wrote a contradicting one, because I read main for CONFLICTS and not for CONTENT.
git reported no conflict, and there was none: two sections of one document disagreeing is
not a textual conflict. What makes this preventable rather than regrettable is that §12
was NOT IN THE LEDGER — 47 of the document's 70 sections were unpoured — so there was no
row for a new row to collide with. Hence `quantity` and the coverage ratchet."""


[[claim]]
id = "edh-5p2nt-dip-is-a-resonance"
claim = "The 5.2 nT dip is a resonance entering the sampled window — plausibly between the Zeeman splitting and the radial mode spacing, both of which the field and ω_eff set."
status = "refuted"
retraction_evidence = """field, 5.2 -> 2.6 and 10.4 nT, which is the test registered before the measurement. It fails twice over: on the informative arm (2.6 nT) the predicted dip near omega_eff ~ 0.325 is absent, the curve rising monotonically 0.45976 / 0.46412 / 0.46681 / 0.47068 / 0.47682 over 0.250-0.375; and the structure the hypothesis explained is itself a hold-duration truncation, so there is no dip at 5.2 nT to move."""
retired_literal = ["dip is a resonance", "resonance entering the sampled window"]
type = "B"
scope = "Hypothesis. The observation it explains is that 2.6 nT is a clean single peak and doubling the field introduces a dip."
uncertainty = "unbounded: not measured. Two fields is not a trend."
uncertainty_basis = "none"
evidence = []
evidence_status = "absent"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "11.4"
pr = 403
prediction = "If the dip is a resonance, its ω_eff position must move again at B = 10.4 nT. If it does not move, the resonance reading is wrong."
prediction_registered = "before"
prediction_outcome = "miss"
note = """REFUTED 2026-08-21 BECAUSE ITS PREMISE DISSOLVED, not because the test disagreed. The 10.4 nT arm was run, and the answer is that there is no dip at 5.2 nT to move: the two-branch structure was a truncation artifact (see `edh-5p2nt-ordering-is-hold-dependent`). `prediction_outcome = "miss"`, and it took two corrections to get there. §12.3
(2026-08-20) had ALREADY tested this properly and found it fails: the prediction was
ω_eff,dip ∝ B, whose informative arm is 2.6 nT — predicted dip near ω_eff ≈ 0.325,
measured 0.250→0.375 rising monotonically (0.45976, 0.46412, 0.46681, 0.47068, 0.47682).
10.4 nT predicts "no dip in range", which is the easy half: a null that confirms nothing.
I ran the easy half, did not read §12.3, and concluded the question was unaskable — then
added a `moot` value to the schema to describe that, and REMOVED it again the same day
once the misdiagnosis was clear. The lesson is sharper than the word: I
extended a schema to fit a situation I had misdiagnosed, which is how a taxonomy acquires
categories that describe the analyst rather than the world."""

# --- Acceptance gates and the precision claims they touch --------------------

[[claim]]
id = "edh-grid-convergence-4-digit"
claim = "32³ and 64³ agree to 4 digits on the klaus_quench observables."
status = "refuted"
retraction_evidence = """grid, 32^3 -> 64^3 with the residual quoted for the first time: peak P_adj moves +2.47 % and peak P_exc +2.05 %, one-signed, and one-signed again at 5.2 nT (+2.15 %, +2.54 %). That is 2-digit agreement, two orders of magnitude looser than the claimed 4 digits. The original quoted no residual at all, which is why the comparison had never been made."""
superseded_by = "edh-grid-convergence-2-digit"
retired_literal = ["4-digit match"]
replacement_literal = ["2.47 %", "+2.47%"]
type = "A"
scope = "Was: the grid-convergence row of the sheet's acceptance chain."
uncertainty = "unbounded: no residual was ever quoted with it."
uncertainty_basis = "none"
evidence = ["klaus_protocol_sheet.md validation chain"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/manuscript/klaus_protocol_sheet.md"
section = "validation chain"
pr = 392

[[claim]]
id = "edh-grid-convergence-2-digit"
claim = "32³ → 64³ moves peak P_adj by +2.47 % and peak P_exc by +2.05 %, one-signed. That is 2-digit agreement, two orders of magnitude looser than 4-digit."
status = "live"
supersedes = ["edh-grid-convergence-4-digit"]
type = "A"
scope = "klaus_quench protocol, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT. Confirmed one-signed at 5.2 nT too (+2.15 %, +2.54 %)."
uncertainty = "The residual is the uncertainty: ±2.5 % is the floor on any absolute observable quoted from a 32³ arm in this family. It dwarfs the seed (0.000 %) and dt/2 (0.029 %) floors."
uncertainty_basis = "grid"
evidence = ["runs/klaus_quench/klaus_quench_omm0p5_keeprot_n64.yaml", "runs/klaus_quench/klaus_quench_om0p0_n64.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.4"
pr = 392
note = "One-signed means 32³ under-resolves the cascade rather than scattering about it, which is why the 5.2 nT dip survives refinement: a uniform offset cannot fill a dip. Cost, for whoever runs it next: 3026 s at 64³ against ~590 s at 32³ on the same core — 5.1×, not 8×, because the run is partly memory-bound (3.0 GB RSS)."

[[claim]]
id = "edh-g6-pexc-rises-at-matsui-n"
claim = "At N = 5×10⁴ the rotation raises P_exc by ~30 %."
status = "refuted"
retraction_evidence = """observable, P_exc -> P_adj at N = 5e4. P_exc measures 0.81531 against 0.81613, i.e. -0.10 %, resolved against the 0.029 % dt/2 floor and therefore a genuine null rather than an unresolved one, while P_adj rises +6.4 %. The cascade is already saturated at P_exc = 0.816 so that observable has no headroom: the gate was mis-posed, and it also failed against the criterion fixed before launch. Both facts are kept."""
superseded_by = "edh-g6-omega-concentrates-not-creates"
retired_literal = ["P_exc +30%"]
replacement_literal = ["concentrates", "saturated"]
type = "B"
scope = "Was: acceptance gate G6 of the 2026-05-26 protocol spec."
uncertainty = "unbounded. The sheet's own row carried '(with metric caveat)'; the caveat was load-bearing and was not read as one."
uncertainty_basis = "none"
evidence = ["klaus_quench_protocol_spec_2026_05_26.md gate G6"]
evidence_status = "absent"
commit = "unknown"
doc = "docs/manuscript/klaus_quench_protocol_spec_2026_05_26.md"
section = "acceptance gates"
pr = 392
note = "`retired_literal` is deliberately the bare observable name and NOT a numeral: the retracted content here is the claim that P_exc is the observable the enhancement shows up in. Any live document quoting an enhancement in P_exc at Matsui N is quoting this row."

[[claim]]
id = "edh-g6-omega-concentrates-not-creates"
claim = "Ω does not create excitation, it concentrates it. Peak P_exc is flat to |Ω| ≈ 0.6 while P_adj rises 24 %; at N = 5×10⁴ the cascade is already saturated (P_exc = 0.816), so P_exc has no headroom and the enhancement appears on P_adj (+6.4 %)."
status = "live"
supersedes = ["edh-g6-pexc-rises-at-matsui-n"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT, N = 5×10⁴."
uncertainty = "P_exc measured at 0.81531 vs 0.81613, i.e. -0.10 % — resolved against the 0.029 % dt/2 floor and therefore a genuine null rather than an unresolved one. P_adj +6.4 % (0.36119 vs 0.33956) carries the 2.5 % grid systematic."
uncertainty_basis = "dt"
evidence = ["runs/klaus_quench/klaus_quench_om0p0_N50k.yaml", "runs/klaus_quench/klaus_quench_omm0p5_keeprot_N50k.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.4"
pr = 392
note = "G6 is recorded as FAIL against the criterion fixed before launch, and the row above explains why the criterion was the wrong one. Both facts are kept: the gate failed AND the gate was mis-posed."

# --- Scope restrictions that bind every row above ----------------------------

[[claim]]
id = "edh-observable-deterministic-in-seed"
claim = "On this protocol and duration peak P_adj is deterministic in the seed: 5 seeds agree to 5 decimals (sd = 0.00000), with the seed proved live (state overlap 0.9999997 at frame 1, growing to 1.9e-5 by frame 39)."
status = "scoped"
type = "A"
scope = "klaus_quench hold-only + 2 ms delay at 32³, ω_eff ∈ {1.000, 0.714}. Does NOT generalise to longer holds, where the cascade's amplification of the seed (2 orders over this run) may reach the observable."
uncertainty = "sd = 0.00000 over 5 seeds. This is a measured zero with a positive control, not an unmeasured one — checked precisely because an all-identical table is what a dead knob looks like."
uncertainty_basis = "seed"
evidence = ["5-seed arms at ω_eff = 1.000 and 0.714"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "10.3"
pr = 392
note = """Consequence: the seed axis carries no uncertainty to quote for this family, so every `uncertainty` field above is bounded by grid/dt instead. The `noise_seed` knob was initially wired to the wrong step and produced the same all-identical table for a wrong reason; that was found and fixed before these numbers.

CAVEAT NOW MEASURED, 2026-08-20. The scope's warning about longer holds was not hypothetical: at 100 ω_ref⁻¹ and 64³ a second seed moves the static arm's ENDPOINT by 34.2 % (0.27262 → 0.17952) while leaving its PEAK identical to five decimals (0.49081 both). So the determinism is a property of the peak, and the endpoint at long time carries a seed uncertainty large enough to swallow an 18.8 % arm separation — see `edh-longtime-endpoint-ordering-unresolved`. Rows bounded by grid/dt above remain correct for the short protocol; a long-hold endpoint must quote a seed bound."""

[[claim]]
id = "edh-corpus-is-one-seed-32cubed-lhy-none"
claim = "Every value in the rotation-assisted EdH quench re-derivation is one seed, 32³ (except where 64³ is stated), `lhy: none`, one hold duration, CPU."
status = "live"
type = "A"
scope = "Binds every claim in this file whose id starts `edh-`. Stated as its own row so a reader who quotes one number does not have to find the caveat in a different section of a different document."
uncertainty = "The directional findings (×2.2, +16.5 % vs -0.45 %, the 4.70 % dip) are far outside the measured floors and survive. The VALUES are not converged results and are not quotable as such."
uncertainty_basis = "grid"
evidence = ["§6.3"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "6.3"
pr = 392

# --- Open ------------------------------------------------------------------

[[claim]]
id = "klaus-sheet-frozen-but-prescriptive"
claim = "`docs/manuscript/klaus_protocol_sheet.md` carried a dated FROZEN header — the tree's declaration that a file need not be true — and also a numbered lab protocol instructing a reader which rotation frequency, chirality and field to set. Its label and its use disagreed."
status = "closed"
type = "A"
scope = "One document. The same shape may apply to `klaus_quench_protocol_spec_2026_05_26.md`, which is 1000 lines of frozen record containing the same prescriptions."
uncertainty = "unbounded: this is a classification question, not a measurement. What IS measured is the count — 9 sites in that file state a retracted claim with no marker within 6 lines (lines 105, 125, 227, 232, 233, 234, 255, 293, 296), against a retraction block at lines 17-70."
uncertainty_basis = "none"
evidence = ["docs/manuscript/klaus_protocol_sheet.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/claims.toml"
section = "Open"
pr = 0
note = """CLOSED 2026-08-20 by annotation, not by moving the file. The archive route was costed first and is the more expensive one: 15 inbound references, two of them CALIBRATION PROBES inside tests — `test_campaign_fix_list_gate.jl` uses this exact path as its negative control and would go blind if the file moved. Measuring the edges reversed the recommendation. What was done instead: (1) the nine point-of-use sites now carry SUPERSEDED / REFUTED at the line, with strikethrough on the values, so step 6 of the numbered protocol issues no instruction; (2) `docs/guides/edh_quench_lab_prescription.md` is a new LIVE document carrying only what survived, so a reader has somewhere true to go; (3) the retraction gates now bind LIVE docs PLUS every document this ledger names in its `doc` field, which reaches this file whether or not it is frozen. The asymmetry kept: a frozen document still need not be CORRECT, it must only not present a retracted claim as live. Rewriting history is not demanded; issuing orders from it is stopped."""


[[claim]]
id = "edh-phi-phys-config-anti-alignment"
claim = "`runs/eu151_klaus_phi_phys/config.yaml` needed `init_m_idx: 13` at p > 0 to be anti-aligned. It has it."
status = "closed"
type = "A"
scope = "One config. `init_m_idx: 1` there is the schema default at p > 0 (`run_step_rotating/ground_state.jl:162`), so nothing was ever chosen."
uncertainty = "unbounded: not measured. The sign bookkeeping is determinate; the consequence for that scan's results is not, because it has no quoted result."
uncertainty_basis = "none"
evidence = ["runs/eu151_klaus_phi_phys/config.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "4.2"
pr = 392
note = """CLOSED 2026-08-20, and it had been closed since 2026-08-19 without anyone noticing. `e8dafe8e` — the same commit that retargeted the quench corpus — set `init_m_idx: 13` on line 51, with the reasoning in the config header. The decision document's section 4.2 went on saying "not done here … on the re-derivation list" for a day, and this row was written faithfully FROM that paragraph. So the ledger inherited a false statement from its source and then caught it, which is the sequence the `evidence` column is for: the row was only checkable because it named a path, and the path resolved to a file that disagreed with the prose. A row whose evidence is prose could not have done this. Section 4.2 corrected in the same change."""

[[claim]]
id = "edh-longtime-static-sustains"
claim = "At 145 ms the rotating arm peaks and collapses back below the baseline while the static weakened trap holds 2.25× it, so rotation gives a transient and the static trap a SUSTAINED transfer."
status = "refuted"
retraction_evidence = """grid, 32^3 -> 64^3 at a 100 omega_ref^-1 hold, three arms. BOTH orderings invert: static-vs-rotating goes -3.2 % -> +21.6 % at the peak and 2.25x -> 0.68x at the endpoint, and per-arm the endpoints move -42.6 % (static) and +91.3 % (rotating). The original stated its own key number (3.21 %) INSIDE the 2.5 % resolution systematic and said so, which is what made the 64^3 run the obvious next step."""
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 2.6 nT, hold = 100 ω_ref⁻¹, three arms."
retired_literal = ["2.25×", "2.25x", "sustained transfer", "a transient"]
uncertainty = "The claim was stated with its own key number (3.21 % static-vs-rotating at the peak) INSIDE the resolution systematic (2.5 %), and said so. That overlap is what made the 64³ run the obvious next step."
uncertainty_basis = "grid"
evidence = ["32³: baseline 0.38532/0.23127, static 0.50790/0.47517, rotating 0.52475/0.21093 (hold-peak/end)"]
evidence_status = "absent"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "13"
pr = 410
note = """REFUTED 2026-08-20 by the 64³ re-run, with NO replacement ordering — see `edh-longtime-not-converged-at-32`. Both orderings invert: static-vs-rotating at the peak goes -3.2 % to +21.6 %, and at the endpoint 2.25× to 0.68×. Per-arm the endpoints move -42.6 % (static) and +91.3 % (rotating). This is not a 3 % divergence being resolved, it is 32³ failing to resolve the branch."""

[[claim]]
id = "edh-longtime-not-converged-at-32"
claim = "At a 100 ω_ref⁻¹ hold, 32³ is not resolution-converged: refining to 64³ inverts both the peak and the endpoint ordering of static vs rotating, so NEITHER resolution establishes the long-time ordering."
status = "live"
type = "B"
scope = "2.6 nT, anti-aligned, CPU. THREE arms at 64³ (baseline, static ω_eff=0.714, rotating Ω=−0.70), two seeds on the static and rotating arms."
uncertainty = "Bounds what 32³ cannot support; it does not by itself establish the 64³ answer. The two measurements it named as missing — a baseline arm and a second seed — were run on 2026-08-20 and are `edh-longtime-peak-ordering-at-64` (established) and `edh-longtime-endpoint-ordering-unresolved` (still not)."
uncertainty_basis = "grid"
evidence = ["64³: static 0.49081/0.27262, rotating 0.40358/0.40358 (hold-peak/end) against 32³ 0.50790/0.47517 and 0.52475/0.21093"]
evidence_status = "absent"
commit = "32aa5e50"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "15"
pr = 0
note = """The part that generalises: grid adequacy is a property of the grid AND the integration time. Under the same 32³→64³ refinement the SHORT protocol (14.5 ω⁻¹) moved by a uniform +2.3 % and the 5.2 nT dip kept 93 % of its depth — shape preserved — while the long one moves -3.4/-23.1/-42.6/+91.3 % and orderings invert. So sections 9-12 are unaffected and only the long-time branch is out of reach at 32³. A convergence check at one duration does not transfer to another."""

[[claim]]
id = "edh-longtime-peak-ordering-at-64"
claim = "At a 100 ω_ref⁻¹ hold and 64³ the hold-peak ordering is static (0.49081) > rotating (0.40102) > baseline (0.37973): the statically weakened trap beats no-intervention by +29.3 % and beats rotation by +22.4 %."
status = "live"
type = "B"
scope = "2.6 nT, anti-aligned, CPU, `lhy: none`, 64³, 100 ω_ref⁻¹ hold. The PEAK only — the endpoint ordering at the same duration is a separate and unresolved row."
uncertainty = "The static peak is seed-independent to five decimals (0.49081 at two seeds) and the rotating peak moves 0.63 %, so the 22.4 % and 29.3 % gaps are 35-100x the seed scatter. Not grid-bounded: this IS the refined grid, and 32³ gave the opposite static-vs-rotating sign, so no statement is carried over from there."
uncertainty_basis = "seed"
evidence = ["64³ hold-peak P_adj: baseline 0.37973, static 0.49081, rotating 0.40102; static seed 101 vs default both 0.49081"]
evidence_status = "absent"
commit = "6bb97bb2"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "17.2"
pr = 0
note = """Replaces the ordering `edh-longtime-static-sustains` asserted and `edh-longtime-not-converged-at-32` refused to restate — but only for the peak, and with the opposite reading of what the two arms do. The original claim was that the static arm SUSTAINS and the rotating one decays; at 64³ the rotating arm is the persistent one (peak ~= endpoint, still climbing at 145 ms) and the static one falls from 0.49081 to 0.17952. Larger cascade and more persistent cascade are different questions and 32³ answered neither."""

[[claim]]
id = "edh-longtime-endpoint-ordering-unresolved"
claim = "RESOLVED by the n=8 ensemble (#424): at 100 ω_ref⁻¹ and 64³ the statically weakened trap DOES beat no-intervention at the endpoint — mean 0.27869 against 0.15094, +84.6 %, at 5.01 SE_diff. The two-seed reading that could not separate them (+18.8 %) was a sample of a distribution whose pooled sd is 0.05098."
status = "live"
type = "B"
scope = "2.6 nT, anti-aligned, CPU, `lhy: none`, 64³. The endpoint at long hold only; the peak at the same points IS resolved (`edh-longtime-peak-ordering-at-64`)."
uncertainty = "n = 8 per arm, pooled sd 0.05098 (the pre-run n=2 estimate was 0.0658, so n=8 was sufficient rather than lucky), SE_diff 0.02549, |diff|/SE = 5.01 against the 2.0 threshold fixed before launch. The three groups have wildly different spreads and that is the result's shape: baseline sd 0.00020 (0.13 % of mean), rotating sd 0.00230 (0.57 %), static sd 0.07210 (26 %). ALL of the endpoint uncertainty lives in the static arm; the arms it is compared against are essentially deterministic."
uncertainty_basis = "seed"
evidence = ["64³ endpoint P_adj, n=8/8/4: baseline mean 0.15094 sd 0.00020, static mean 0.27869 sd 0.07210, rotating mean 0.40416 sd 0.00230", "runs/klaus_quench_long_time_ensemble/", "scripts/submit_lt64_endpoint_ensemble.sh", "scripts/validation/lt64_endpoint_verdict.jl"]
evidence_status = "in_tree"
commit = "6bb97bb2"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "17.2"
pr = 0
note = """Recorded as `open` rather than left out, because the absence of a statement here is exactly what a reader would otherwise supply from the peak row. The rotating-vs-static factor of 2.23 IS outside the scatter and is safe to quote; the static-vs-baseline one is not. Same shape as `edh-longtime-not-converged-at-32`: a quantity measured on one axis (peak) does not transfer to another (endpoint), just as one duration did not transfer to another.

THE RUN THAT WOULD HAVE CLOSED IT WAS PREPARED AND NOT SPENT (2026-08-21), AND WAS THEN RUN (2026-08-22, #424, TSUBAME 8459280, 20/20 arms rc=0). Kept verbatim because the sizing and the criterion were fixed before launch and that is what makes the verdict a measurement. 20 committed configs (8 baseline + 8 static + 4 rotating, seeds 1001-1008) and an array submit script. Sizing fixed BEFORE launch: sd = 0.0658 at n = 2, difference of means 0.0750, SE_diff = sd*sqrt(2/n), so n = 7 reaches 2 sigma and n = 8 is what is committed; fewer than 7 cannot answer the question at all. Rejection criterion also fixed before launch and written into `runs/klaus_quench_long_time_ensemble/README.md`: ESTABLISHED only if the difference of means is at least 2*SE_diff using the sd POOLED FROM THOSE RUNS, and if the pooled sd comes back much larger than 0.0658 the answer is "n = 8 does not reach 2 sigma, it would need N", not a re-fitted criterion.

CLOSED 2026-08-22. The criterion was applied as written, by a script that carries the 2.0 threshold as a constant so it could not be re-fitted: |diff|/SE = 5.01, ESTABLISHED, static ABOVE baseline by +84.6 %. The pooled sd came back SMALLER than the pre-run estimate (0.05098 against 0.0658), so the "n = 8 does not reach 2 sigma, it would need N" branch did not fire.

THE PEAK COLUMN OF THAT RUN IS NOT QUOTABLE AND THE ARGMAX SAYS WHY. With the window derived from the config (hold 100.0, dt 0.005, save.every 1000 -> 20 frames of 39, so the hold starts at frame 20), the baseline maximum lands at frame 20 and rotating at 21 — the first and second frames of the window, where the decaying pre-hold transient still dominates. Only static's is interior (frame 31), and only static's matches its recorded value (0.49081, exactly). This is the §12.1 condition again, and it is the reason the peak numbers in `runs/klaus_quench_long_time_ensemble/README.md` should not be compared against this run's peak column. It does not touch the verdict: the endpoint is `adj[end]` and needs no window.

NOT LAUNCHED because it is ~5.2 points of a 19.24 group balance (~27 %) with another session spending concurrently, and this is a secondary row — the peak ordering it might be confused with is already established and unaffected. anko made that call; the numbers are in the README so the next person does not re-derive the decision.

Two API facts were established the expensive way and are recorded so the next author does not invent them again: `cli.jl launch` takes `[<batch>] <run_name>` and has NO `--smoke`, and `run_yaml` has NO point selection. The second is why this is 20 single-arm configs rather than 3 seed-scans — a `scan:` cannot be split across array tasks."""

[[claim]]
id = "edh-phi-phys-anti-aligned-needs-field-reversal"
claim = "The anti-aligned preparation cannot be produced by ITP on the fast-Larmor rotating-basis path: at p = 26700 the Zeeman-shifted factor for the highest m is exp(-12·p·dt) = exp(-1602), which underflows in ONE step and returns ψ identically zero. It needs the state relaxed at the OPPOSITE field sign and the field reversed for the dynamics — a pipeline change, not a config key."
status = "live"
type = "A"
scope = "`runs/eu151_klaus_phi_phys/config.yaml`, p = 26700, dt = 0.005, F = 6. Quantitative, so it does NOT generalise: the klaus_quench spinor corpus has p ≈ 148, per-step factor exp(-8.9), renormalised every step and never underflowing."
uncertainty = "Four control arms — init_m_idx in {1, 13} x ITP in {100, 1500} — separate the candidate causes: both init_m_idx=13 arms give 0 of 212992 non-zero entries and ‖ψ‖² = 0.0, both init_m_idx=1 arms give ‖ψ‖² = 4.096 and E = -160177.6. The seed, not the ITP length, is what decides it."
uncertainty_basis = "control"
evidence = ["runs/eu151_klaus_phi_phys/config.yaml", "src/workflow/experiments/pipeline/run_step_rotating/ground_state.jl"]
evidence_status = "in_tree"
commit = "e11b0291"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "16.3"
pr = 0
note = """SUPERSEDES the deferral in `edh-phi-phys-config-anti-alignment`, which proposed `init_m_idx: 13` as the repair. That was set on 2026-08-19 and reverted on 2026-08-20 once the energy was reported at all — the run had been completing on ψ ≡ 0 with E = 0.0 and saying nothing. The underflow is a hard error naming the mechanism and the remedy.

IMPLEMENTED 2026-08-20 as `prepare_anti_aligned` — see `edh-anti-aligned-prep-implemented`. This row stays `live` because the claim it makes is about ITP, not about the config: imaginary time still cannot produce the anti-aligned state, and the new key does not change that. It relaxes in a DIFFERENT field."""

[[claim]]
id = "edh-anti-aligned-prep-implemented"
claim = "`prepare_anti_aligned: true` on a `kind: rotating_basis` ground_state relaxes at -p and hands the dynamics +p, producing the Zeeman-HIGHEST stretched state that the EdH quench needs. Only `p` reverses; `q ∝ |B|²` is even in the field and does not."
status = "live"
type = "A"
scope = "The `rotating_basis` ground_state step. Gated at F=1 on an 8³ grid; the mechanism is the sign of one Zeeman coefficient and does not depend on F. RUN AT PRODUCTION SCALE 2026-08-22 (#423): all 8 `phi.rate` points of `runs/eu151_klaus_phi_phys` at 32×32×16, F=6, D=13, DDI + LHY scalar, TSUBAME job 8461716, 82 min, rc=0 — every point `converged = true`, `energy = −160177.72` finite, and `mz_actual = −6.0000` on all eight, i.e. the runtime anti-aligned assertion held across the whole scan."
uncertainty = "Directional, not numeric: an ALIGNED control arm in the same field comes out with sign(⟨F_z⟩) = +sign(p) and the anti-aligned arm with -sign(p), at both signs of p, each stretched to |⟨F_z⟩| = F within 0.05. A single arm could not tell the preparation from the sign convention assumed while reading it."
uncertainty_basis = "control"
evidence = ["test/rotating_basis/test_anti_aligned_preparation.jl", "src/workflow/experiments/pipeline/run_step_rotating/ground_state.jl", "runs/eu151_klaus_phi_phys/config.yaml"]
evidence_status = "in_tree"
commit = "0"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "18"
pr = 0
note = """The step now ASSERTS the outcome at runtime — `sign(⟨F_z⟩) = -sign(p)` or it throws — rather than trusting the construction. That is deliberate: the defect this replaces was a run that COMPLETED and returned ψ ≡ 0, and a preparation that quietly hands back the ALIGNED state is the same class and would invert every conclusion drawn from it. `⟨F_z⟩` along B̂ is also recorded in every result now (`rotating_basis_fz_along_b`), because which end of the ladder a run started from was the controlling variable of this whole campaign and nothing was reporting it.

The canary needed strengthening: the first version used p = 400, where exp(-2·F·p·dt) = exp(-8) is renormalised away every step and the arm completes. Only a SINGLE-STEP underflow (exponent > 708) is the inexpressible case."""

[[claim]]
id = "rotating-basis-result-writer-omitted-energy"
claim = "`save_rotating_basis_result!` — the writer that owns result.jld2 for BOTH `kind: rotating_basis` and `kind: spinor`-with-dynamics — wrote neither `energy` nor `converged`, so both keys were ABSENT from every dynamics run's result file."
status = "live"
type = "A"
scope = "All runs written through `save_rotating_basis_result!` (runner.jl:294), which is every pipeline carrying a dynamics history."
uncertainty = "unbounded: this is a code-shape claim, not a measured quantity — the writer either emits the key or it does not, and it did not. What is NOT bounded is how many stored runs are affected, because runs/ is untracked."
uncertainty_basis = "none"
evidence = ["src/workflow/io/save_rotating_result.jl"]
evidence_status = "in_tree"
commit = "e11b0291"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "16.1"
pr = 0
note = """CORRECTS section 14.2, which attributed the missing energy to `run_registry.jl` reading absent keys through defaults. That writer is never reached on this path. The `conv = true` default was real and printed to stdout, but was never STORED — so a guard reading the file finds nothing, which is unknown rather than a pass, and section 14.2's guard-7 claim overstated. The #410 default fix stands on its own path; this is a third writer nobody had checked."""
# --- eu335 / #383 / #398: the 68.4 µG reading ---------------------------------
#
# The SECOND document poured in, and it was chosen for a shape the EdH rows do not
# have: a claim retracted not because its NUMBER moved but because its READING did.
# 68.4 ± 0.15 µG is the same measurement before and after; what changed is whether
# it is a property of the energy landscape or a property of the solver walking it.

[[claim]]
id = "eu335-68p4-is-a-mean-field-spinodal"
claim = "68.4 ± 0.15 µG is the flower branch's mean-field spinodal at κ = 1.8 — the branch stops existing there — so a rising B_z ramp must convert by then at ANY rate."
status = "superseded"
retraction_evidence = """not a value: the NUMBER is unchanged (68.4 +- 0.15 muG at 32^3, [68.0, 68.5] at 64^3, 0.2 % apart). What moved is what it is a number OF, and the measurement that moved it is #383's three-quantity test on the same cells: branch-tangent curvature linear in R^2 to rms 0.4 % with its zero at 78.2 muG, i.e. NOT vanishing at 68.4; escape direction <d,Ad> = +3.403, uphill; lowest omega flat to 1.4 %. A saddle-node takes all three to zero together and none softens."""
type = "B"
scope = "¹⁵¹Eu F = 6, κ = ω_z/ω_⊥ = 1.8, 32³ (64³ bracket [68.0, 68.5] µG), flower branch reached by warm-started L-BFGS continuation in B_z."
retired_literal = [
    "spinodal at 68.4 µG",
    "flower-branch spinodal at κ = 1.8",
    "flower branch stops existing",
    "ends at 68.4 ± 0.15 µG",
]
replacement_literal = [
    "loses the flower branch",
    "continuation loses the branch",
    "warm-started continuation loses the branch",
]
superseded_by = "eu335-68p4-is-where-continuation-loses-the-branch"
uncertainty = "±0.15 µG at 32³ and [68.0, 68.5] µG at 64³, 0.2 % apart. That bound is on the FIELD at which the continuation stops and was read as a bound on where the branch ends; the reading itself carried no uncertainty at all, which is how it survived."
uncertainty_basis = "grid"
evidence = ["docs/guides/eu_spinodal_spectrum.md", "docs/guides/eu_kappa_hysteresis_loop.md"]
evidence_status = "in_tree"
commit = "bace6f12"
doc = "docs/guides/eu_kappa_hysteresis_loop.md"
section = "5.1"
pr = 409
note = """SUPERSEDED 2026-08-20 (#398) by #383's three-quantity test, and the reason it is `superseded` rather than `refuted` is that the number did not move — only what it is a number OF. A saddle-node takes the branch-tangent curvature, the escape direction and the lowest ω to zero together; #383 measured all three on the same cells and none softens (R² linear to rms 0.4 % with its zero at 78.2 µG, ⟨d,Ad⟩ = +3.403 uphill, ω flat to 1.4 %). What is NOT established either way is that the branch survives: λ_min at 68.25 µG is unresolved by both solvers, tracked as #399. The generalisable part: "the continuation loses the branch" and "the branch ends" are different claims about different objects — one about a solver's basin, one about the energy landscape — and a continuation instrument can only ever measure the first."""

[[claim]]
id = "eu335-68p4-is-where-continuation-loses-the-branch"
claim = "68.4 ± 0.15 µG is the field at which a warm-started L-BFGS continuation loses the flower branch at κ = 1.8, agreeing to 0.2 % between 32³ and 64³. Nothing in the campaign establishes that the branch ENDS there."
status = "live"
type = "B"
scope = "Same cells as the superseded row: ¹⁵¹Eu F = 6, κ = 1.8, 32³ and 64³, warm-started continuation in B_z. Says nothing about any other κ, and nothing about the polarised branch."
supersedes = ["eu335-68p4-is-a-mean-field-spinodal"]
uncertainty = "±0.15 µG at 32³, bracket [68.0, 68.5] µG at 64³ — 0.2 % apart. This is a bound on a solver-dependent field, so a different warm-start schedule may move it; the grid agreement bounds the discretisation, not the algorithm."
uncertainty_basis = "grid"
evidence = ["docs/guides/eu_kappa_hysteresis_loop.md", "docs/guides/eu_spinodal_spectrum.md"]
evidence_status = "in_tree"
commit = "bace6f12"
doc = "docs/guides/eu_spinodal_spectrum.md"
section = "5.1"
pr = 409
note = """Everything #335 delivers that needs only this weaker reading stands unchanged, including the 32³↔64³ agreement of §5.8 and the deliverable itself — the Stern-Gerlach level count (6-7 populated m_F at κ = 1.8 against 3-4 at κ = 0.9, disjoint at all five rates), which never rested on the landscape reading. What is lost is the *at any rate* clause: behind a barrier a fast enough rising ramp carries the flower past 68.4 µG, which is what the predecessor campaign actually observed."""

# --- #407: the FFTW planner's memory, and the hypothesis it was not ------------
#
# A type-A pair, and the reason they are here rather than only in CLAUDE.md is
# that the RSS table has now been restated three times (the submit script's
# header, the issue body, this campaign) and each restatement is a copy. The
# `retired_literal` on the refuted row is what stops the killed hypothesis being
# quoted back as a candidate.

[[claim]]
id = "fftw-mixed-radix-planner-memory"
claim = "FFTW's MEASURE planner allocates gigabytes for a NON-POWER-OF-TWO transform when Julia has real threads: 1.66 GB at 48³ rising to 11.97 GB at 98³, against 0.35 GB at 64³ and 0.38 GB at 128³. The axis is the radix, not the grid size and not the thread count."
status = "live"
type = "A"
scope = "TSUBAME cpu_16 (16 of 384 cores), Julia 1.12.6, FFTW.jl, `julia -t 16`, FFTW 16 threads, cubic complex in-place 3D plans, one process per point. Not measured on other machines; the same corner did NOT appear on a 10-core local box in #405."
uncertainty = "One process per point and one point per (n, threads, flags) cell — no repeats, so the numbers carry no scatter estimate. What IS bounded is the ordering: the two power-of-two sizes are 4.7-34× below every mixed-radix neighbour, which no plausible run-to-run scatter closes. `ru_maxrss` is a high-water mark, so each figure is an upper bound on that process's peak and an exact one for the comparison being made."
uncertainty_basis = "control"
evidence = ["scripts/klaus2022/fftw_thread_probe.jl", "scripts/klaus2022/submit_fftw_probe.sh"]
evidence_status = "in_tree"
commit = "9d4ad07d"
doc = "CLAUDE.md"
section = "Cost model + execution discipline"
pr = 420
note = """Three conditions, each shown necessary by removing it: MEASURE (ESTIMATE gives 0.33-0.36 GB at every size), REAL Julia threads (`julia -t 1` gives 0.26-0.30 GB at every size EVEN WITH FFTW reporting 16 threads — FFTW.jl runs its parallel loop through Julia's threadpool), and a non-power-of-two length. The consequence for this repo is that `make_fft_plans` / `make_rfft_plans` DEFAULT to MEASURE and `src/analysis/dipole_field.jl` plans the DDI work shape that way, i.e. the path every eGPE ground state runs. Not auto-downgraded to ESTIMATE: that would change the throughput of every production run to fix a corner recognisable in advance, and one call is not a throughput measurement. `SPINORBEC_FFT_PLAN=estimate` is the deliberate opt-out; `fft_planning_memory_risk` is the predicate and `test_fft_planning_memory_risk.jl` gates it in both directions on all three axes."""

[[claim]]
id = "fftw-visible-cpu-count-hypothesis"
claim = "The 36.9 GB at 48³ is caused by a library sizing per-thread scratch from the node's CPU count (`/proc/cpuinfo` = 384) rather than from the job's cgroup allocation (16)."
status = "refuted"
retraction_evidence = """visible CPU count, 384 (from /proc/cpuinfo) -> 16 under `taskset`, at three grid sizes. 48^3 still allocates 2.26 GB with the affinity mask at 16, so the blow-up survives the variable the hypothesis blamed. A refutation by counterexample on the named axis, not a re-measurement of a quantity."""
type = "A"
scope = "Same cells as `fftw-mixed-radix-planner-memory`. The taskset arm narrows the VISIBLE set without changing the allocation, which was already 16."
retired_literal = [
    "visible-CPU count (384 on the node) is a candidate",
    "reads /proc/cpuinfo (384) rather than the cgroup (16)",
    "a library reads `/proc/cpuinfo` (384) rather than the cgroup (16)",
]
uncertainty = "unbounded: this is a refutation by counterexample, not a measurement of a quantity. Three sizes were run under `taskset`; two reproduce the blow-up with the affinity mask at 16 and the third (128³, a power of two) is cheap as always."
uncertainty_basis = "none"
evidence = ["scripts/klaus2022/submit_fftw_probe.sh"]
evidence_status = "in_tree"
commit = "9d4ad07d"
doc = "CLAUDE.md"
section = "Cost model + execution discipline"
pr = 420
note = """REFUTED 2026-08-21. Narrowing the visible CPU set to 16 with `taskset` leaves 48³ at 2.26 GB and 96³ at 8.38 GB — if anything slightly worse than the unnarrowed 1.66 and 6.73 GB. The count was never the mechanism. Worth keeping as a row rather than deleting, because it is a PLAUSIBLE hypothesis with a well-known general form (libraries that read the node instead of the cgroup) and it will be proposed again by anyone who meets the symptom without the radix table in hand. The first version of the probe could not have refuted it either: it ran at `julia -t 1`, where nothing blows up at all, so its `taskset` rows would have been a null on top of a null."""

# --- #397 / #399: lambda_min on the weak-field Eu branches ---------------------

[[claim]]
id = "eu-lambda-min-certified-on-polarised-branch"
claim = "With `precond=:combined` and `max_iter=2000` the constrained Hessian's λ_min converges with a solver certificate on 5 of 6 weak-field Eu cells, and its SIGN is resolved above the finite-difference floor at three of them — 100 µG (+7.1595e-02), 20 µG (+4.0e-07) and the 68.25 µG flower cell (+7.6e-08 to +8.4e-08). All three are minima along every direction the block resolves."
status = "live"
type = "B"
scope = "¹⁵¹Eu F=6, κ=1.8, 32³×13, box 24 a_ho, pin ε=0.002, unpadded DDI, dealiasing off, one seed, `nev=4` — so 'minimum' means 'along every direction a 4-dimensional block resolved'. 25 and 60 µG are NOT resolved; 5 µG does not converge at all."
uncertainty = "Two finite-difference steps (1e-5, 3e-5) bound the operator's own error by their difference: 6.7e-09 at 20 µG, 7.5e-09 at 68.25 µG, 1.4e-07 at 100 µG. The solver widths (1e-14 to 3e-10) are an order of magnitude or more BELOW that and are not the binding uncertainty. Two points give a sensitivity, not a convergence order."
uncertainty_basis = "control"
evidence = ["docs/guides/eu_lambda_min_resolved.md", "scripts/eu_spectrum/precond_ab.jl"]
evidence_status = "in_tree"
commit = "65f4da78"
doc = "docs/guides/eu_lambda_min_resolved.md"
section = "5"
pr = 420
note = """The load-bearing part is what the FD control REVOKED. The first pass reported `λ_min = -1.77e-09` at 25 µG and `-8.88e-09` at 60 µG, both `converged = true` with widths of ~1e-14, and the driver printed "SADDLE — proven" for each. Re-running the same arm at ε = 3e-5 gave +5.47e-09 and +8.46e-09: BOTH SIGNS INVERT. They were numbers below the instrument's floor read as though they were above it, and the `converged` flag was truthful — about the solver. What generalises: once an iterative eigensolver's width drops below the accuracy of the operator it is solving, `converged` stops being the thing to check and the discretisation control becomes it."""

[[claim]]
id = "eu335-68p4-flower-is-still-a-minimum"
claim = "At 68.25 µG — the last flower cell before #335's continuation loses the branch — λ_min is converged and POSITIVE (+7.62e-08 at ε=1e-5, +8.37e-08 at ε=3e-5), i.e. 11× its own finite-difference sensitivity. The state is an energetic minimum there, which a saddle-node forbids."
status = "live"
type = "B"
scope = "One cell, one seed, 32³, `nev=4`. Says nothing about fields above 68.5 µG, where no converged flower cell exists to measure."
uncertainty = "±3e-10 from the solver; ±7.5e-09 from the ε control, which is the binding one. λ/|Δ| = 11."
uncertainty_basis = "control"
evidence = ["docs/guides/eu_lambda_min_resolved.md"]
evidence_status = "in_tree"
commit = "65f4da78"
doc = "docs/guides/eu_lambda_min_resolved.md"
section = "5"
pr = 420
note = """This is the direct evidence #398 said would settle the reading and #383 could not obtain — both solvers there returned values one tenth of their own uncertainty. It supports `eu335-68p4-is-where-continuation-loses-the-branch` and closes off the saddle-node reading with a fourth independent quantity, after the branch-tangent curvature, the escape direction and the lowest ω. What it does NOT establish is where the flower branch actually ends: a minimum at 68.25 µG says the branch has not ended AT 68.25 µG, and the next converged cell up the field axis does not exist."""

# --- Second document poured in: docs/theory/lhy_scheme_selection_eu_f6.md (#337)
#
# Chosen because it is a DIFFERENT SUBSYSTEM. The column set was fixed by the EdH
# quench arc, and a schema that only fits the document it was designed from is not
# a schema. What it exercises that the EdH rows did not: a claim whose scope is a
# THRESHOLD rather than a grid ("above 105 µG the question does not arise"), a
# ratio quoted against a value in the code's own docstring, and two rows where the
# uncertainty IS the finding.
#
# What did not fit, recorded rather than smoothed: this document's claims are
# largely CONDITIONAL ("use X when |⟨F⟩|/F ≈ 1"), and `claim` is one sentence with
# no condition field, so the condition has to live inside the sentence. That works
# and reads badly. If a third document also arrives conditional, the schema wants
# a `applies_when` column rather than more prose.

[[claim]]
id = "lhy-eu-f6-ambiguity-is-1p3-percent"
claim = "The ε_LHY scheme ambiguity for ¹⁵¹Eu at F=6 is ≤ 1.3 %, not O(1): a beyond-mean-field result here IS quotable with an error bar."
status = "live"
type = "B"
scope = "¹⁵¹Eu F=6 at the campaign coupling. Measured by repairing the counterterm bookkeeping `full_bdg` warns about (S1 vs S3) rather than by asserting it."
uncertainty = "1.3 % is itself the uncertainty — this row exists to bound the ambiguity, so the number and the error bar are the same object. Negative control present: wherever the spectrum is real, S3 ≡ S1 identically, so the `c_dd = 0` rows prove the comparison can return zero."
uncertainty_basis = "control"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "3"
pr = 0
note = "Answers #337 against a premise that was too strong. The issue asked whether ε_LHY is ill-posed at F=6; it is not, and the way to find out was to repair the mismatch the code already warned about rather than to argue from the literature."

[[claim]]
id = "lhy-eu-f6-field-closes-the-question"
claim = "Above B ≈ 105 µG the m = −F mean field is dynamically stable and ε_LHY carries no scheme dependence at all, so the scheme question does not arise. It closes through p, not through q."
status = "live"
type = "B"
scope = "¹⁵¹Eu F=6, m = −F. A THRESHOLD, not a grid or a duration — the claim is about which side of 105 µG a run sits on."
uncertainty = "The threshold is read off a B-scan of the BdG spectrum (0 / 44 / 100 µG), so it is located to roughly the scan spacing and should be quoted as ≈ 105 µG, not 105. Bounded below by the 100 µG row still showing an unstable m = −F direction at 0.5530."
uncertainty_basis = "grid"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "2.1"
pr = 0
note = "Supersedes a verdict that was wrong for a UNIT reason: `linear_zeeman_p` takes tesla and the campaign YAML writes Gauss, so a dismissal table read p and q low by 10⁴ and 10⁸. The row printed as 1 G was in fact 100 µG. A dimensional slip that survives because every number in the table is slipped the same way is not visible from inside the table."

[[claim]]
id = "lhy-eps-varies-2p7x-with-polarisation"
claim = "ε_LHY varies by a factor 2.68 with the DDI (3.72 contact-only) between |⟨F⟩|/F = 1 and 0 at the campaign coupling — not the ~20 % `spatial.jl` records."
status = "live"
type = "B"
scope = "Along ζ(α) = cos α|m=+F⟩ + sin α|m=0⟩ at c1_ratio = 1/36. The ratio RIDES ON c1_ratio: it is 1.38 at c1_ratio = 0 and 2.68 at 1/36, so a measurement taken near zero coupling would have looked small and been quoted as general."
uncertainty = "unbounded: a single sweep at one coupling, with no grid or seed replication. What IS bounded is the direction and the order of magnitude — 2.68 against a recorded ~20 % is a factor 13, far outside anything replication would move."
uncertainty_basis = "none"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "7.2"
pr = 0
note = "The consequence is a rule, not a number: never give one ansatz's functional to a state of different |⟨F⟩|. At c1_ratio = 0 every g_S collapses to c₀ and the two contact closed forms coincide, so the residual 1.38 there is the DDI alone — which is why the docstring's figure is not merely stale but was measured in a regime where the effect is absent."

[[claim]]
id = "lhy-scheme-moves-the-boundary-21x"
claim = "The choice of LHY scheme moves the FM/polar phase boundary by a factor 21 in δc1*: 3.4e-4 for a single functional against 7.3e-3 when each branch uses its own ansatz. `spatial` lands with the latter (6.6e-3)."
status = "live"
type = "B"
scope = "At the reference point c1_ratio = 0.027, converted through each arm's own median |∂ΔE/∂c1| and the field slope 0.0311 per µG. In µG: +0.40 against +9.9."
uncertainty = "Positive control passes and is quantitative: ×30 on the scalar amplitude moves the gap 5.7× further than ×1 and converts to 4.1e-3 in c1_ratio, four times the 1e-3 pre-launch threshold. Sub-linear in amplitude because the cloud relaxes against the added repulsion — expected, and the reason a control has to be RUN rather than reasoned about."
uncertainty_basis = "control"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "5.4"
pr = 0

[[claim]]
id = "lhy-mean-field-fm-polar-line-at-1over36"
claim = "The mean-field FM/polar line at `c1_ratio ≈ 1/36` is not defensible: LHY does not displace that boundary, it CREATES it."
status = "live"
type = "B"
scope = "¹⁵¹Eu F=6 at the campaign coupling. A statement about which term is responsible for a boundary, not about where the boundary sits."
uncertainty = "unbounded as a location — this row asserts the mechanism, not a number. The boundary's POSITION is `lhy-scheme-moves-the-boundary-21x`, which does carry one."
uncertainty_basis = "none"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "5.3"
pr = 0
note = "Load-bearing for anything that quotes a mean-field FM/polar transition for Eu: there is no mean-field crossing to quote."

[[claim]]
id = "lhy-spatial-residual-was-1-minus-1-over-N"
claim = "`spatial_lhy_residual` compared an n_atoms = N table against an UNDIVIDED BdG reference, so it returned ≈ 1 − 1/N — exactly 1.0000 on converged Eu states."
status = "closed"
type = "A"
scope = "One function. Every existing test ran at the default n_atoms = 1, where the defect is invisible."
uncertainty = "Not a measured quantity: a scale factor, either present or not. The replacement gate asserts the residual is INVARIANT under n_atoms, which a scale bug cannot satisfy."
uncertainty_basis = "control"
evidence = ["docs/theory/lhy_scheme_selection_eu_f6.md"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
section = "6"
pr = 0
note = "CLOSED — fixed, and gated by a case that runs at n_atoms = 50000. Kept rather than deleted because the shape recurs: a defect invisible at every tested value of a parameter is not caught by more tests at that value. It would have been reported as `a 100 % residual`, i.e. the spatial approximation collapsing, which is a physics conclusion drawn from a missing division."

# --- §12, poured 2026-08-21 because its ABSENCE is what let a refuted method
#     be used again. These four rows are the correction to my own work.

[[claim]]
id = "edh-peak-must-be-taken-inside-the-hold"
claim = "The quoted observable is the peak of P_adj INSIDE the hold, not over the whole streamed trajectory. Over the whole trajectory the maximum can be the pre-hold transient."
status = "live"
type = "A"
scope = "Every EdH-quench arm. It BITES only where the hold suppresses the cascade — measured: 0.00 % change on all 43 arms at 1.3 / 2.6 / 5.2 nT, and 7 of 10 arms moved by up to −23 % at 10.4 nT."
uncertainty = "Not a measured quantity — a definition. Its violation IS measurable and was: four 10.4 nT arms differing only in the hold returned peak P_adj = 0.26050 to five decimals, which is impossible unless the maximum precedes the hold. Argmax at frame 29, hold starting at frame 32."
uncertainty_basis = "control"
evidence = ["scripts/validation/klaus_weff_extract.jl"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.1"
pr = 410
note = """Four identical five-decimal numbers across arms that differ is the signature, and it
is worth naming as a class: when a knob is varied and the observable does not move AT ALL,
the first hypothesis is that the observable is not looking where the knob acts. The
re-extraction was from cache — no recompute — which is why an instrument correction of this
size cost minutes. `klaus_weff_extract.jl` read the whole trajectory until 2026-08-21 and
now takes the window from `times`, printing both values with a marker when they differ."""

[[claim]]
id = "edh-104nt-single-peak-in-hold"
quantity = "peak_P_adj @ B=10.4nT, weff-scan, 32cubed, hold=8ms"
window = "hold, frames 34-42"
reduction = "max"
boundary = "accept"
claim = "At B = 10.4 nT, read inside the hold, the ω_eff response is a CLEAN SINGLE PEAK with its maximum at ω_eff = 0.650, +49.5 % over ω_eff = 1 — the largest relative enhancement of any field measured, on the smallest absolute transfer."
status = "scoped"
supersedes = ["edh-104nt-bump-at-0p65"]
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, B = 10.4 nT, 10 arms. **Peak over hold frames 34-42**, and the row is scoped to that window because the number depends on it — see `edh-104nt-observable-not-window-robust`."
uncertainty = "WINDOW-DEPENDENT, and that dominates everything else: the same arms give a baseline of 0.27574 over hold frames 32-42 and 0.20139 over 34-42, a 37 % difference, both being `the peak in the hold`. 0.30101 at ω_eff = 0.650 against 0.20139 at 1.000 on the 34-42 window. Absolute transfer is roughly half the 2.6 nT value (0.66), which is Zeeman re-pinning costing a factor 2 — real, but it does not close the channel. Grid unbounded: no 64³ arm at this field. Hold duration also unbounded at the corrected observable."
uncertainty_basis = "grid"
evidence = ["runs/klaus_quench_weff/klaus_weff0p650_B10p4nT.yaml", "runs/klaus_quench_weff/klaus_weff1p000_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 410
note = "Weakening the trap helps proportionally MORE where the channel is nearly closed — the largest relative gain sits on the smallest absolute signal. A prescription quoting the relative number without the absolute one would send an experimentalist to the worst field."

[[claim]]
id = "edh-resonance-reading-refuted-at-2p6nt"
claim = "The resonance reading of the 5.2 nT dip predicted ω_eff,dip ∝ B, and it FAILS on its informative arm: at 2.6 nT a dip was predicted near ω_eff ≈ 0.325 and the measured curve rises monotonically through it."
status = "live"
type = "B"
scope = "2.6 nT, ω_eff ∈ [0.250, 0.375], five points, read inside the hold."
uncertainty = "0.45976 / 0.46412 / 0.46681 / 0.47068 / 0.47682 — monotone, no interior minimum, against a dt/2 floor of 0.029 %. The prediction was registered BEFORE the runs."
uncertainty_basis = "dt"
evidence = ["docs/campaign/edh_quench_polarisation_decision.md"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.3"
pr = 410
prediction = "ω_eff,dip ∝ B: at 10.4 nT the dip moves to ω_eff ≈ 1.30 (out of range, so none observable); at 2.6 nT it moves to ω_eff ≈ 0.325."
prediction_registered = "before"
prediction_outcome = "miss"
note = """The two arms are not equally informative and the difference is the whole lesson. 10.4 nT
predicts an ABSENCE, which any number of mechanisms produce; 2.6 nT predicts a specific
interior minimum, which almost nothing produces by accident. Testing the absence first and
stopping there is how a prediction gets confirmed by a null. A registered prediction should
name WHICH arm is informative at the time it is registered, or the cheap arm will be run."""

[[claim]]
id = "edh-5p2nt-second-branch-has-no-mechanism"
claim = "Whatever produced the second branch at 5.2 nT is unexplained: it is a measured structure without a mechanism, not a mechanism awaiting confirmation."
status = "superseded"
retraction_evidence = """premise_dissolved: there is no second branch to explain. The structure this row was a statement ABOUT is a hold-duration truncation (see `edh-5p2nt-ordering-is-hold-dependent`), so `no mechanism is known` stopped being a fact about the world and became a fact about an artifact. Nothing was measured against this row because there was nothing left for a measurement to address."""
superseded_by = "edh-5p2nt-ordering-is-hold-dependent"
retired_literal = ["measured structure without a mechanism"]
replacement_literal = ["hold duration", "hold-duration"]
type = "B"
scope = "Was: the honest position after the resonance reading was refuted, while the structure itself was still believed."
uncertainty = "unbounded: a statement that no mechanism is known, which carries no number."
uncertainty_basis = "none"
evidence = ["docs/campaign/edh_quench_polarisation_decision.md"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.3"
pr = 410
note = "Superseded the next day: there is no structure to explain. The mechanism hunt was well-posed given what was believed, and the thing that dissolved it was varying the hold — an axis neither the structure nor the hunt for its mechanism had touched."

# --- Coverage ratchet ---------------------------------------------------------
#
# `covered` is how many numbered sections of each cited document have at least one
# row. It may RISE and must not FALL, so adding a section to a document this
# ledger cites, without pouring it, is red.
#
# WHY, measured rather than argued. On 2026-08-21 a row was added asserting a
# quantity that §12.2 of the FIRST document had already measured — by a method
# §12.1 had refuted the day before. The `quantity` gate did not fire because a row
# can only collide with a row, and 47 of that document's 70 sections had never
# been poured. A ledger citing a document and covering a third of it reads as
# complete, and did.
#
# The numbers are low on purpose: pinning them at "everything" would make the gate
# unmeetable and it would be deleted. Raise them as documents are poured; the gate
# only forbids going backwards.

[[claim]]
id = "f6-uniform-gs-degenerates-the-whole-manifold"
claim = "At uniform g_S the sigma_S sum rule degenerates the ENTIRE spinor manifold, not four phases: Sum_{S even} sigma_S(zeta) = 1 for any normalised zeta, so eps_MF = g/2 everywhere on CP^12. The degeneracy is continuous, which is what order-by-disorder requires."
status = "live"
lifecycle = "registered"
ladder = """exact: an algebraic identity in the symmetric two-body subspace — there is no cutoff axis to walk. Control: the 50x50 (g_10, g_12) scan returns eps_MF = 1/2 for all four candidate spinors at the scalar point, and `_f6_pd_sigma_S` reproduces each fingerprint."""
type = "B"
scope = "Any F, any normalised spinor, uniform g_S. An identity, so it has no grid, seed or duration. It is a statement about the UNIFORM-g_S reference point only — the physical Eu g_S are not uniform, and how far they sit from it is the six unmeasured channels."
uncertainty = "Exact — the even-S two-body states span the symmetric subspace, so Sum_S sigma_S(zeta) = <zeta⊗zeta|zeta⊗zeta> = |<zeta|zeta>|^2 = 1 identically. Confirmed numerically: the four candidate spinors all return eps_MF = 1/2 at the scalar point in the 50x50 (g_10, g_12) scan, and `_f6_pd_sigma_S` reproduces the fingerprint for each."
uncertainty_basis = "control"
evidence = ["docs/research_notes/F6_phase_boundaries.md", "src/analysis/phases/F6_phase_diagram.jl"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/research_notes/F6_phase_boundaries.md"
section = "Result"
pr = 0
note = """Registered because the four-phase form was written in CAMPAIGN.md §11 and in memory and
UNDERSTATES the result, in the one way that matters. Order-by-disorder needs a CONTINUOUS accidental
degeneracy: four isolated points are selected between by an energy difference, and only a continuous
manifold carries a would-be Goldstone mode for the selection to gap. So "four phases degenerate" would
have made the pseudo-Goldstone programme (#455) look impossible on paper, while the identity makes it
the same structure Turner-Barnett-Demler-Vishwanath (PRL 98, 190404) exploit for the spin-2 nematic
manifold, at higher dimension.

WHAT IT DOES NOT SAY. Uniform g_S is a reference point, not Eu. The physical g_S are not uniform and
six of the seven even channels have never been measured, so the SIZE of any residual continuous
degeneracy at the physical point is an a_S-dependent question (layer ii). What IS a_S-free is the
stretched sector: the fully polarized cloud and its first magnon are pure S = 2F
(`test/oracles/test_stretched_channel_invariance.jl`), which is why #455 is scoped to the FM branch."""

[[claim]]
id = "ddi-cdd-f-squared-voids-pre-april-ddi"
claim = "`c_dd` carried an unnecessary F^2 factor (36 at F=6) until `c6b1ed0b` (2026-04-28), so every DDI-dependent result produced before that ref is void — effective DDI 7,647 against a correct 275,292 is the wrong way round: the run used 275,292 where 7,647 was meant."
status = "live"
lifecycle = "registered"
ladder = """exact: F^2 = 36 at F = 6 is an algebraic factor, not a converged quantity. Control: the guard at coefficients.jl:95 and the F=6 constraint test that landed with c6b1ed0b."""
type = "A"
scope = "Any run with DDI active whose producing commit does not descend from `c6b1ed0b`. Scalar (F=0) runs are unaffected: `c_dd = mu_0 mu^2` there and no spin operators enter H_dd."
uncertainty = "Exact — F^2 = 36 at F = 6 is an algebraic factor, not a fitted one. `c_dd` carries `(g_F mu_B)^2`, NOT `(g_F F mu_B)^2`; the F^2 that maps eigenvalues is already in the spin operators, so multiplying again double-counts it. Control: the guard at `src/hamiltonian/coefficients.jl:95` and the F=6 constraint test that landed with the fix."
uncertainty_basis = "control"
evidence = ["docs/campaign/fix_list.toml", "src/hamiltonian/coefficients.jl"]
evidence_status = "in_tree"
commit = "c6b1ed0b"
doc = "docs/campaign/fix_list.toml"
section = "ddi-cdd-f-squared"
pr = 0
note = """UNTIL 2026-08-25 THE ONLY RECORD OF THIS ANYWHERE IN THE TREE WAS A CODE COMMENT.
`src/hamiltonian/coefficients.jl:95` reads "DO NOT MULTIPLY BY F^2 <- this docstring exists
because Bug-3 was an F^2/36x", and nothing the ancestor gate reads knew the fix existed. So
guard 1 could not disqualify a single pre-2026-04-28 DDI run, which is the #343 class again
and at the OLDEST correction in the campaign. The value-level SSoT was never the problem:
one line computes c_dd and the comment defends it. What was missing was the statement, in
the place that is executed.

DIAGNOSED WRONG TWICE FIRST. The ITP energy oscillation was read as Yoshida w0 < 0
back-diffusion, then as a failure of DDI self-consistency, and only then as c_dd. That is
why the guard is phrased as an imperative rather than a description.

CATEGORY, because it has been miscategorised in discussion: this was found by INTERNAL
diagnosis (tracking the ITP oscillation), not by comparison against a published result, so
it is not an example of the oracle problem. It is a coefficient/scaling defect - numbers ran
before the analytic limit was checked - compounded by one piece of physics living in more
than one place and drifting. The second half is already digested into the HamTerm protocol
and day-0 gated redundancy; this row records the first half, which had no home.

PROVENANCE IS OUTSIDE THE REPO. Project chat conv 606ac7ca (2026-04-22) found it; conv
41595d89 (2026-06-11) audited it, called it the highest-impact numerical bug in the project's
history, and drafted a ledger row that never landed. A fix whose only witness is a chat log
is a fix the gate cannot see, which is the general lesson and the reason this row exists."""

[[claim]]
id = "stored-runs-corpus-is-written-off"
claim = "The 261 GB corpus under `runs/` is not a cache and cannot become one: 0 of 219 stored `summary.json` carry a producing commit, and 0 run directories use the current `compute_run_dir` naming. Re-deriving any of it means re-running it."
status = "superseded"
superseded_by = "stored-runs-corpus-is-attributable-not-admissible"
retired_literal = ["is not a cache and cannot become one", "Re-deriving any of it means re-running it"]
replacement_literal = ["224 of 230", "attributable"]
retraction_evidence = """artifact, `summary.json` -> `point_*.jld2` - the file the provenance gate actually opens (`_assert_point_provenance`, `run_registry.jl:243`). 0 of 219 summaries carry a stamp and 224 of 230 points do, with 23 distinct producing commits. The summary measurement was correct and was generalised to the wrong artifact: summaries are what documents cite, points are what the cache admits."""
type = "A"
scope = "The stored outputs as of 2026-08-25, in the main checkout (`/home/suzume/workspace/BEC-simulation/runs`; a git worktree does not share them). The committed YAML CONFIGS are a separate corpus and are fine — 566 of them, all parseable."
uncertainty = "Exact counts, with the scan calibrated against the writer's own key format: `summary[\"_env_<k>\"]` (`src/workflow/experiments/analyzers/misc.jl:220`). Positive control - a synthetic dict carrying `_env_git_hash` is detected; negative control - a dict carrying a bare `git_hash` is not. The FIRST pass of this scan looked for `env.git_hash` and returned the same 0, which is why the control matters: a wrong extractor and an empty corpus print the same number."
uncertainty_basis = "control"
evidence = ["docs/validation/stored_results_vintage_audit.md", "src/workflow/experiments/analyzers/misc.jl", "src/workflow/experiments/pipeline/run_registry.jl"]
evidence_status = "in_tree"
commit = "6571fc25"
doc = "docs/campaign/fix_list.toml"
section = "stored-corpus"
pr = 0
note = """WHY THIS IS A ROW RATHER THAN A KNOWN INCONVENIENCE. Planning has been assuming a cache
exists and asking which effect spoils its hit rate - near-duplicate configs, byte-keying,
provenance mismatch. The measurement says the question is mis-posed for THIS corpus: there is
nothing for `_assert_point_provenance` to compare, so no stored point is a reuse candidate at
all. A canonical parameter grid is still worth having, but it is a policy for FUTURE runs and
must not be sold as a way to recover what is on disk.

TWO INDEPENDENT SIGNS, which is why this is not one scan's artifact. (1) No summary carries an
`_env_*` stamp; #139 landed that stamping on 2026-07-29 and states that pre-#139 runs cannot be
retro-dated, only re-run. (2) `compute_run_dir` keys on `sha256(bytes)[1:16]` and every directory
on disk carries an 8-hex suffix, so the whole store also predates the current naming.

WHAT IS NOT WRITTEN OFF: the 566 committed YAML configs, the figures already derived and cited
with their own provenance, and the arms re-run under the campaign gate since. This row is about
the stored OUTPUTS.

MEASURED SIDE-RESULT, kept because it bounds a different question. Byte-keying exposure is small:
of 566 committed configs only 9 groups are canonically identical, 7 of them byte-different, 18
files in total (3.2 %). One pair has materialised on disk as two run directories -
`matsui_edh_baseline_9ca97308/` and `matsui_edh_baseline_529e3a77/`. So unifying on the
`Experiment` (canonical-spec) path is a real but small win, and was NOT the dominant term."""

[[claim]]
id = "stored-runs-corpus-is-attributable-not-admissible"
claim = "The stored corpus is ATTRIBUTABLE and RE-ANALYSABLE but not auto-reusable and not campaign-admissible: 224 of 230 `point_*.jld2` carry `env/git_hash` over 23 producing commits, yet all 224 also record `git_dirty = true`, and the three dominant vintages are 2026-05-21/23/26, before the June-July corrections."
status = "live"
lifecycle = "registered"
ladder = """exact: a census of 230 files, not a limit. Control: 137 / 32 / 9 for 15a9f1ee / 306ef71a / e8168dcb, reproduced independently from `_NOT_A_CORRECTION` in test_campaign_fix_list_gate.jl."""
supersedes = ["stored-runs-corpus-is-written-off"]
type = "A"
scope = "The stored outputs as of 2026-08-25 in the main checkout (a git worktree does not share them). Three different questions are separated here and must not be collapsed again: attributable / re-analysable / admissible."
uncertainty = "Exact counts from a scan of every `point_*.jld2` (230 found, 0 unreadable), probing the group the gate reads (`env/git_hash`, written as `f[\"env/$k\"]`). CROSS-VALIDATED against the tree: `test_campaign_fix_list_gate.jl`'s `_NOT_A_CORRECTION` already names 15a9f1ee as the producing commit of 137 stored runs, 306ef71a of 32 and e8168dcb of 9 — the scan reproduces 137 / 32 / 9 exactly, which is a positive control nobody had to construct."
uncertainty_basis = "control"
evidence = ["src/workflow/experiments/pipeline/run_registry.jl", "test/test_campaign_fix_list_gate.jl"]
evidence_status = "in_tree"
commit = "dd50479f"
doc = "docs/campaign/fix_list.toml"
section = "stored-corpus"
pr = 0
note = """THREE QUESTIONS, COLLAPSED INTO ONE BY THE ROW THIS REPLACES.

ATTRIBUTABLE - yes. 224/230 points name their producing commit. What is unstamped is
`summary.json`, which is what documents and figures cite; #290 added the stamp there on
2026-08-02 and everything older lacks it. Both facts are true and they are about different files.

RE-ANALYSABLE - yes, and there is precedent IN THE TREE. `97ec124e` corrected the EdH observable
(the peak must be taken inside the hold) and "all affected numbers were re-extracted from cache
with no recompute" - the entire 10.4 nT window correction was post-processing of stored psi. So
the corpus already earns its disk space for any question that is a new READING of an old run.

NOT AUTO-REUSABLE - `_assert_point_provenance` refuses on two independent grounds, and the second
cannot be repaired retroactively by anything: the recorded hash differs from HEAD, AND all 224
points record `git_dirty = true`, so "neither commit identifies the code" holds even at a matching
hash. `SPINORBEC_ALLOW_STALE_POINTS=1` is the deliberate opt-out and is the right switch for
re-analysis, where the code difference provably cannot reach the answer because no propagation is
being redone.

NOT CAMPAIGN-ADMISSIBLE - the three dominant vintages predate the June-July integrator, LHY and
Zeeman corrections. That is physics, not bookkeeping, and no amount of provenance work changes it.
This half of `stored_results_vintage_audit.md` stands.

THE LESSON IS THE ONE THE LEDGER KEEPS RE-LEARNING. The measurement was right and the artifact was
wrong: 0/219 was a real number about summaries, and it was reported as a conclusion about the cache.
The gate opens a different file. Say which file a count is a count OF."""

[[claim]]
id = "spgpe-full-steady-state-is-not-the-equilibrium"
quantity = "N_C at the full-SPGPE steady state @ T=10, mu=11.724, kappa=1.8, 64cubed"
claim = "The full-SPGPE (energy-damping-on) steady state N_C ~= 2.9e3 is NOT the grand-canonical equilibrium of its own reservoir: the C-region equilibrium at (T=10, mu=11.724, k_cut=6.59) is 1.9e4-3.3e4, which the growth-only steady state (2.96e4) lands inside and the full one sits 6.5x below."
status = "scoped"
lifecycle = "candidate"
ladder = "exact: closed-form occupancy at fixed (T, mu, eps_cut) — no cutoff axis to walk. The SPREAD is the anisotropy, not a convergence residual: the TF formula as coded is isotropic (omega enters only eps_0), and a (1,1,kappa) trap carries an extra 1/kappa in the volume element, giving 1.886e4 with the factor and 3.272e4 without. Both ends are quoted."
type = "A"
scope = "The NUMBERS: the eu334 nucleation ramp: box 24, 64^3, kappa = 1.8, c0 = 0.046873 (norm-N form, n_atoms=1 preset), k_cut = min(sqrt(2(mu+T)), k_max = 8.378), evaluated at the ramp's TERMINAL mu. It is a statement about the STEADY STATE of the two dissipative schemes, not about which one is right for a growth problem."
uncertainty = "The bracket 1.9e4-3.3e4 IS the uncertainty and it is a modelling spread (isotropic vs kappa-corrected TF, plus `coherent_population`'s validity condition - see the note), not a statistical one. The DIRECTION survives it: 2.9e3 is 6.5x below the lower end. Positive control, and it was not constructed for the purpose — the growth-only arm's measured steady state 2.96e4 falls INSIDE the independently computed range, so c0, omega and eps_cut are not mis-set; had that failed, nothing here would be readable."
uncertainty_basis = "control"
evidence = ["src/solvers/evaporation/spgpe_reservoir.jl", "src/solvers/spgpe.jl"]
evidence_status = "in_tree"
commit = "807260b4"
doc = "docs/campaign/claims.toml"
section = "spgpe"
pr = 0
window = "the ramp's terminal reservoir point"
reduction = "steady-state value (the plateau), against a closed-form equilibrium at the same (T, mu, eps_cut)"
boundary = "n/a"
note = """SCOPED 2026-08-25, THE SAME DAY, BY THE MEASUREMENT THIS ROW ASKED FOR. The
numbers above stand. The INFERENCE drawn from them — "so the energy-damping implementation
places the field off its own equilibrium" — does not, and #485 is where it died.

WHAT WAS MEASURED. `test/dynamics/test_spgpe.jl` already contains the mode-resolved
instrument: a free field (c0 = 0, no trap) where eps_k = k^2/2 is exact, so
N_C^eq = sum_{|k|<k_cut} T/(eps_k - mu) is unambiguous. It runs at M = 0.0, i.e. with
energy damping OFF. Turning M on in that fixture, and then adding interactions and a trap
one axis at a time:

    fixture                  M = 0     M = 1.628e-3 (production)   ratio
    free field              4349.31    4338.87                     0.9976
    interacting, no trap    1575.92    1575.04                     0.9994
    interacting + trap       687.04     686.94                     0.9999
    weakly int. + trap       833.99     834.00                     1.0000

At 60x production the free field degrades to 0.8974 and the interacting+trapped case only
to 0.9831 — i.e. the interacting case is LESS sensitive, not more. So the channel's
stationary distribution is right to 0.01-0.24 % at the strength production uses, and the
"broken fluctuation-dissipation relation" reading is refuted where the equilibrium is
knowable.

WHAT REMAINS, AND IT IS NOW THREE THINGS RATHER THAN ONE. (a) The eu334 regime differs in
ways these fixtures do not reproduce — T = 10 against 2, a RAMPING mu, 13 components, DDI,
kappa = 1.8, and a SEED at 11530 rather than a field grown from zero. (b) Neither arm may
have reached its stationary state: #418 read a plateau as arrival, and at production M the
channel is slow enough that a plateau is weak evidence (400 steps move mu-tilde by 1e-4).
(c) The equilibrium bracket in this row rests on Thomas-Fermi plus `coherent_population`,
and that function has a VALIDITY CONDITION nothing in the tree gates.

MEASURED THE SAME DAY, on (c). `coherent_population` is a BOSE sum over ideal harmonic levels
that SKIPS every level below mu, on the stated grounds that "those are absorbed into the
condensate that N_0(mu) already counts". Against numerical SPGPE equilibration at four
(T, mu, c0, kappa) points with a LARGE c0 — where the TF condensate is 1-3 atoms and so
absorbs nothing — it undercounts by 7.0x to 10.1x (53.8 against 427.8; 53.2 against 370.7;
206.7 against 1492.7; 49.6 against 501.7), and the whole gap is in the thermal term.

So the condition is "mu >> eps_0 AND the TF condensate genuinely holds the sub-mu levels",
and eu334 satisfies it far better than those probes: c0 = 0.046873 there gives
N_0^TF = 1.7-3.4e4 against a thermal term of 1.5e3, so the condensate dominates and does hold
them. The bracket therefore STANDS — but on a NAMED condition that was previously unexamined
rather than on an untested formula. Gating the function is its own issue.

WHAT IT STILL OVERTURNS. #418 closed with "not a defect, two steady states, the full arm has
ARRIVED". The arrival is what (b) doubts. And the threat to the flower nucleation result
stays dissolved either way: the equilibrium sits ABOVE the flower minimum atom number
N0* ~= 1.5e4 and growth-only lands there, so "a more complete theory forbids flower" needs
the full arm to be AT its equilibrium, which nothing now asserts.

WHAT THIS OVERTURNS. #418 closed with "not a defect, two different steady states, the full arm
has ARRIVED rather than failed". The arrival is confirmed; the destination is not the
reservoir's equilibrium. So the threat this posed to the flower nucleation result dissolves:
the equilibrium sits at 1.9-3.3e4, ABOVE the flower minimum atom number N0* ~= 1.5e4, and the
growth-only arm lands there. "A more complete theory forbids flower" does not follow from an
arm that is not at its own equilibrium.

THE METHOD IN THE ISSUE WAS ALREADY REFUTED IN THE TREE, and this is the third instance of
that pattern here. #454 proposed checking mu_psi against T. `field_chemical_potential`'s own
docstring (PR #433) says a scalar mean cannot answer a mode-resolved question, reports the
measurement on this very ramp — both arms carry mu-tilde ~ 16-20 against mu_res ~ 8.6 and
growth-only grows anyway — and states that reading it as "the drive" produced a wrong
conclusion its own control refuted. I wrote the issue without reading the docstring of the
function it named. The instrument that DOES answer it is a different one two files away."""

[[coverage]]
doc = "docs/campaign/edh_quench_polarisation_decision.md"
covered = 27
note = """70 sections. §12 poured 2026-08-21 as the correction to the incident above; §9.1,
§10.1, §10.2 and §12.1 followed the same day. The ratchet moved only 26 -> 27 for four new
rows because most cite sections already covered — which is the honest arithmetic and worth
seeing: pouring rows is not the same as covering sections, and a ledger can grow while its
coverage does not."""

[[coverage]]
doc = "runs/saito_li_torus/README.md"
covered = 1
note = """5 sections cite this file. Poured 2026-08-22 by the EdH retraction
(`saito-li-edh-response-1p75x`): "continued 6" is the section whose table states 0.03750, and it
is the one covered. The other four — the torus reproduction, the Fig 3 cigar verdict, Fig 5, and
the DDI f(kappa) box oracle — carry no ledger row yet and are the honest gap. Note the retraction
does NOT reach them: only the EdH response number failed to reproduce."""

[[coverage]]
doc = "docs/theory/lhy_scheme_selection_eu_f6.md"
covered = 6
note = "18 sections. The second document poured; the decision + the ambiguity + criteria B/C/D."

[[coverage]]
doc = "docs/guides/eu_kappa_hysteresis_loop.md"
covered = 1
note = "15 sections. Cited by one row from another session; not poured."

[[coverage]]
doc = "docs/guides/eu_lambda_min_resolved.md"
covered = 1
note = "6 sections. Same."

[[coverage]]
doc = "docs/guides/eu_spinodal_spectrum.md"
covered = 1
note = "9 sections. Same."

[[claim]]
id = "edh-104nt-observable-not-window-robust"
claim = "At B = 10.4 nT `peak P_adj in the hold` does not measure the hold for 16 of 20 arms: their maximum falls on frame 32, the hold's FIRST frame, where the decaying pre-hold transient still dominates. It is not merely window-dependent — for 80 % of the scan it is reading a different physical quantity."
status = "open"
quantity = "peak_P_adj @ B=10.4nT, observable definition"
window = "hold"
reduction = "max"
boundary = "reject"
type = "A"
scope = "B = 10.4 nT only. At 1.3 / 2.6 / 5.2 nT the whole-trajectory and in-hold peaks are identical on all 43 arms (§12.1), so the observable is well-posed there and every claim at those fields stands."
uncertainty = "MEASURED, not argued. The hold boundary is frames 32-42: prep 13 + quench 13 + pre-delay 5 = 31 frames by construction, and the spread across 20 arms differing ONLY in the hold trap is exactly 0.000000 at frames 30-31 and 0.000213 at frame 32 — a zero-to-nonzero transition at the predicted boundary, which fixes the geometry independently. But 0.000213 across a 3× change in radial trap frequency means the trap has not yet acted at frame 32, so the maximum there is the transient's tail. Baseline: 0.27574 over 32-42, 0.20139 over 34-42."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff1p000_B10p4nT.yaml", "runs/klaus_quench_weff/klaus_weff0p650_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
note = """OPEN because I closed it twice and both closures were premature. First I quoted
0.27574 from a whole-trajectory peak, which §12.1 had already refuted. Then I swapped in
§12.2's 0.20139 and declared myself corrected — which replaced one window-dependent number
with another rather than noticing that the dependence IS the finding. §12's extraction code
is not in the tree (only its numbers are), so the 34-42 choice cannot be read, only
inferred, and inferring it from which value matches is the exact error this whole day was
about. What would close this: define the observable without a free window — the endpoint,
or the peak of the hold response after subtracting the transient, or a cut justified by
something other than agreement with a previous answer."""

[[claim]]
id = "edh-104nt-peak-reads-the-transient-for-80-percent"
claim = "At B = 10.4 nT the peak-in-hold reading takes frame 32 — the hold's first frame — for 16 of 20 arms, and those peak values RISE monotonically with ω_eff, opposite to the physical trend. Ranking the scan by `peak` therefore ranks the transient."
status = "live"
type = "A"
scope = "B = 10.4 nT, 20 arms at the 8 ms hold. Not a statement about 1.3 / 2.6 / 5.2 nT, where §12.1 measured the whole-trajectory and in-hold peaks identical on all 43 arms."
uncertainty = "Counts, not estimates: 16 arms with argmax at frame 32 (the set of argmax frames over those 16 is exactly {32}), 4 with argmax at 42. Rank correlation between `peak` and `mean` is -0.507 over all 20, -0.797 over the 16 edge arms, and -0.400 over the 4 interior ones — the last on n = 4, which is one rank swap and decides nothing."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff1p000_B10p4nT.yaml", "runs/klaus_quench_weff/klaus_weff0p650_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
prediction = "If the negative peak-vs-mean rank correlation comes from `peak` reading the transient on the flat arms, then (1) those arms have argmax at frame 32, (2) their peak values rise monotonically with ω_eff, and (3) restricting to arms with argmax >= 34 makes the correlation positive."
prediction_registered = "before"
prediction_outcome = "miss"
note = """(1) and (2) HOLD; (3) FAILS — the interior correlation is -0.400, not positive, though on
n = 4 it decides nothing either way. A fourth expectation stated alongside them, that the
edge `mean` would fall monotonically with ω_eff, also fails: `mean` is not monotone. So the
mechanism is established for the edge population and the interior remains unexplained.
Recorded with the misses because a prediction that is 2-for-4 and reported as confirmed is
how a mechanism gets believed.
CONSEQUENCE FOR `AGREE`: the three readings agreeing on argmax = 0.650 is NOT evidence of
robustness. `peak` can only place its argmax among the 4 interior arms — the other 16 are
all pinned at the same transient value — so the argmax and the ranking come from different
populations. An agreement produced that way says nothing about the other 16."""


[[claim]]
id = "edh-optimum-not-quotable-at-either-field"
claim = "Neither B = 5.2 nT nor B = 10.4 nT yields a quotable ω_eff optimum: three readings of the same cached arms place the argmax at three different points, and at 10.4 nT the mean-reading argmax sits on the scan boundary."
status = "live"
quantity = "weff optimum @ EdH quench, 5.2 and 10.4 nT"
window = "hold"
reduction = "max, mean and endpoint - three readings of the same cached arms"
boundary = "reject"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned. Readings: peak-in-hold, mean-over-hold, hold endpoint. All from cache, no recompute."
uncertainty = "argmax by reading — 5.2 nT: 0.550 (peak at the 8 ms hold), 0.650 (peak over all arms), 0.750 (mean); 10.4 nT: 0.650 (peak, mean, final agree) against 0.350 for the mean over the 20-arm scan, which is the LOWER EDGE of the sampled range. A boundary argmax is not an optimum — it says the range does not contain one."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff0p750_B5p2nT.yaml", "runs/klaus_quench_weff/klaus_weff0p350_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
note = """`mean` is NOT hereby declared the right reading. It is a different physical quantity —
the hold average, diluted by the same transient that contaminates `peak` — and picking it
because it is well-posed would be choosing a definition to get an answer, which is the
error this whole thread is about. What the mean reading DOES establish is negative and
solid: under a reading with no free parameter, 5.2 nT is a single broad maximum with no dip
and no second branch, +6.3 % rather than the published +17.0 %, and 10.4 nT has no interior
maximum at all. Three readings, three argmaxes, one of them on the boundary."""

[[claim]]
id = "edh-104nt-scan-range-excludes-the-mean-optimum"
claim = "The 10.4 nT ω_eff scan does not contain its own mean-reading optimum: the mean rises monotonically toward the lower edge (0.350, +10.6 %) and the interior is a minimum at 0.790 (−11.6 %)."
status = "open"
quantity = "weff scan range @ B=10.4nT"
type = "A"
scope = "The 20-arm 8 ms scan over ω_eff ∈ [0.350, 1.000] at 10.4 nT."
uncertainty = "A boundary argmax carries no interval — the optimum is somewhere below 0.350 or there is none. The shape between 0.500 and 0.650 is nearly flat (+1.97 % to +2.78 %), so the rise toward the edge is the only structure with any amplitude."
uncertainty_basis = "grid"
evidence = ["runs/klaus_quench_weff/klaus_weff0p350_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
note = """OPEN, and the cheap next step is arms below ω_eff = 0.350 — NOT a 64³ grid check, which
was the queued job before this. Grid-refining a scan whose optimum is outside its own range
would have measured resolution against a boundary artifact. The 64³ arms stay cancelled
until a reading and a range are settled. Note also that the centrifugal limit bounds this
from below: ω_eff = √(1−Ω²) → 0 as |Ω| → ω_⊥, so a static trap below ~0.3 is a very weak
radial trap and the cloud size becomes the constraint."""

# --- §9, §10 and §12 poured 2026-08-21. The coverage ratchet exists because §12
#     being unpoured is what let a refuted method be used again; leaving the
#     sections either side of it unpoured would be the same bet on a different
#     number.

[[claim]]
id = "edh-2p6nt-omega-scan-has-an-interior-maximum"
claim = "At B = 2.6 nT the Ω scan has an interior maximum at |Ω| = 0.70 (peak P_adj 0.65905, +24.9 % over Ω = 0), falling to 0.441 at |Ω| = 1.00."
status = "live"
quantity = "peak_P_adj @ B=2.6nT, Omega-scan, 32cubed"
window = "hold (identical to whole_trajectory at this field, measured on all 16 arms, section 12.1)"
reduction = "max"
boundary = "reject"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, hold-only + 2 ms delay, 20 Ω cells. Whole-trajectory and in-hold peaks are IDENTICAL at this field (§12.1, measured on all 16 arms), so the reading is not in question here as it is at 10.4 nT."
uncertainty = "±0.04 on the vertex from a 5-point parabolic fit over |Ω| ∈ [0.58, 0.80], c₂ = −2.70, fit rms 4.7e-3. Criteria C1-C4 fixed before launch: interior maximum required, > 5 % enhancement, ≥5-point fit with negative curvature and interior vertex, digits limited by the fit."
uncertainty_basis = "fit"
evidence = ["runs/klaus_quench/klaus_quench_omm0p7_holdonly_delay2ms_refine.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.1"
pr = 392
note = "This field is the one where the observable is not contested, which is why it remains the anchor while 5.2 and 10.4 nT are unquotable."

[[claim]]
id = "edh-2p6nt-static-scan-reproduces-the-rotating-one"
claim = "At B = 2.6 nT a static weakened trap reproduces the rotating scan at every matched ω_eff, worst disagreement 0.06 % across ω_eff ∈ [0.6, 1.0] — not only at the optimum."
status = "live"
quantity = "static-vs-rotating agreement @ B=2.6nT"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, four matched points."
uncertainty = "0.00 / 0.06 / 0.06 / 0.04 % at ω_eff = 1.000 / 0.750 / 0.714 / 0.600, against a dt/2 reproducibility floor of 0.029 %. The agreement is at the floor, i.e. as good as the method can show."
uncertainty_basis = "dt"
evidence = ["34 static-trap arms, §10.1"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "10.1"
pr = 392
note = "The substitution is what makes ω_eff the scan variable at all. It holds at 2.6 nT; §17 (another session) finds it splits at long hold, where the peak agrees and the endpoint does not."

[[claim]]
id = "edh-1p3nt-has-no-operating-window"
claim = "At B = 1.3 nT there is no operating window: +1.8 % across a flat range in ω_eff ∈ [0.65, 1.0]. The absence is the finding, and `|Ω|/ω_⊥ ≈ 0.3 at 1.3 nT` has no replacement."
status = "live"
quantity = "weff optimum @ B=1.3nT"
window = "hold (identical to whole_trajectory on all 7 arms, section 12.1)"
reduction = "max"
boundary = "reject"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, 7 arms. Whole-trajectory and in-hold peaks identical on all 7 (§12.1)."
uncertainty = "+1.8 % over a flat range against a dt/2 floor of 0.029 % — resolved as a rise, but far below the 24.9 % at 2.6 nT. Bounded as an absence rather than a value: no fit was attempted because there is no interior maximum to fit."
uncertainty_basis = "dt"
evidence = ["7 static-trap arms at 1.3 nT, §10.2"]
evidence_status = "absent"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "10.2"
pr = 392
note = "Do not re-fit. A flat +1.8 % has a numerical argmax and quoting it would manufacture a prescription out of the noise floor — the same move the 5.2 nT two-branch reading made with more amplitude."

[[claim]]
id = "edh-observable-correction-touched-one-field-only"
claim = "Re-extracting the peak inside the hold changed 10.4 nT and nothing else: 0.00 % on all 43 arms at 1.3, 2.6 and 5.2 nT, against 7 of 10 arms moving by up to −23 % at 10.4 nT."
status = "live"
lifecycle = "registered"
ladder = """exact: a re-extraction of cached arms under two window definitions — no propagation is redone, so no cutoff axis exists. 43 arms at 1.3 / 2.6 / 5.2 nT move by 0.00 %, 7 of 10 at 10.4 nT by up to -23 %."""
quantity = "in-hold vs whole-trajectory peak, per field"
window = "hold vs whole_trajectory - the comparison IS the claim"
reduction = "max"
boundary = "reject"
type = "A"
scope = "Every arm measured up to 2026-08-20, re-extracted from cache with no recompute."
uncertainty = "Exact zeros, not small numbers: the two readings coincide arm-for-arm at the three lower fields. That is what makes the correction diagnostic rather than a global rescaling — it isolates the Zeeman-re-pinned field, where the hold suppresses the cascade and the transient wins."
uncertainty_basis = "control"
evidence = ["docs/campaign/edh_quench_polarisation_decision.md"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.1"
pr = 410
note = "The reason §9, §10 and §11 survive an instrument correction of this size is measured here, not assumed. Worth keeping as a row because 'the fix only touched one field' is exactly the kind of claim that gets asserted without the table."

[[claim]]
id = "sec12-extraction-method-not-in-the-tree"
claim = "§12.2's numbers are reproducible but its METHOD is not: the tree carries the values and not the code that produced them, and the window they correspond to (hold frames 34-42) is not derivable from the config."
status = "open"
quantity = "provenance of the 12.2 extraction"
type = "A"
scope = "The 10.4 nT in-hold re-extraction of 2026-08-20 (`97ec124e`). Not a statement about the numbers, which reproduce exactly."
uncertainty = "Reproduced to five decimals: max over hold frames 34-42 gives 0.20139 at ω_eff = 1.000, matching §12.2. But frames 32-42 is what the config implies — prep 13 + quench 13 + pre-delay 5 = 31, and the arm-to-arm spread is exactly 0.000000 at frames 30-31 and 0.000213 at 32 — so the two-frame difference has no derivation, only a value that matches."
uncertainty_basis = "control"
evidence = ["scripts/validation/klaus_weff_extract.jl"]
evidence_status = "in_tree"
commit = "97ec124e"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 410
note = """This is `evidence_status = absent` applied to a METHOD rather than to data, and it is the
sharper version: absent arms can be re-run, an absent extraction has to be re-derived, and
re-deriving it from which value matches is how a window gets chosen to fit an answer. I did
exactly that for one commit before catching it.
What closes this is not archaeology. `scripts/validation/klaus_weff_extract.jl` is now in
the tree, computes three readings, and derives its window from the config with the frame
arithmetic written down — so from here the method travels with the number. §12.2's own two
frames remain unexplained and its numbers should be quoted with the window stated."""

[[claim]]
id = "edh-g4-mirror-agrees-to-five-decimals"
claim = "The repaired mirror pair agrees to < 1e-5 on peak P_adj (0.61698 vs 0.61698), stronger than the `3-digit match` the sheet claimed for the pre-repair pair."
status = "live"
quantity = "mirror pair agreement @ 2.6nT"
type = "A"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, the two `*_keeprot*` arms. Acceptance gate G4."
uncertainty = "< 1e-5 relative, i.e. below the dt/2 reproducibility floor of 0.029 % by three orders. Bounded by the comparison itself; there is no fit."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench/klaus_quench_omm0p5_keeprot.yaml", "runs/klaus_quench/klaus_quench_omp0p5_keeprot_mirror.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.4"
pr = 392
note = "Both members now sit at the anti-aligned preparation, so the pair is mirror-correct AND in the regime where the enhancement exists — the two were separate repairs."

[[claim]]
id = "edh-cost-64cubed-is-5x-not-8x"
claim = "A 64³ arm costs 5.1× a 32³ one (3026 s against ~590 s on the same core), not the 8× the cell count suggests, because the FFT work does not scale linearly and the run is partly memory-bound at 3.0 GB RSS."
status = "live"
quantity = "64cubed vs 32cubed walltime ratio"
type = "A"
scope = "klaus_quench protocol, CPU, 8 ms hold. A cost model, not physics."
uncertainty = "unbounded: two timings, one core, no repetition — nothing here bounds the ratio. It is an order-of-magnitude planning number, and the gate refusing the first wording is the point: `two timings and no repetition` reads like a bound and is not one."
uncertainty_basis = "none"
evidence = ["runs/klaus_quench/klaus_quench_omm0p5_keeprot_n64.yaml"]
evidence_status = "in_tree"
commit = "b2d746cc"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "9.4"
pr = 392
note = "Recorded because it is the number anyone sizing a grid check will guess wrong in the expensive direction, and because it is what made the cancelled 64³ job look affordable."

# --- The mechanism, found by measuring the cloud instead of the populations ----

[[claim]]
id = "edh-quench-excites-a-breathing-mode"
claim = "The B quench leaves the cloud breathing, and ω_eff sets the breathing FREQUENCY, not whether it breathes: the ω_eff = 1.000 arm — whose trap is never changed — oscillates in radial RMS radius with a period of ~5 frames, while ω_eff = 0.650 oscillates with ~11."
status = "live"
quantity = "radial breathing @ B=10.4nT"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, B = 10.4 nT, radial RMS radius over the hold, read from cached snapshots. Seen at both hold lengths."
uncertainty = "unbounded: the periods were read off by eye from 11 and 22 samples with no fit, so `~5` and `~11` frames carry no interval. What IS bounded is the EXISTENCE of the oscillation — the ω_eff = 1.000 trace runs 2.885 → 1.999 → 2.693 → 1.984 → 3.163, swings of 30-40 % against a dt/2 reproducibility floor of 0.029 %."
uncertainty_basis = "none"
evidence = ["runs/klaus_quench_weff/klaus_weff1p000_B10p4nT.yaml", "runs/klaus_quench_weff/klaus_weff0p650_B10p4nT.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
note = "The ω_eff = 1.000 arm is the control that makes this a statement about the QUENCH: its trap is identical before and during the hold, so an oscillation there cannot be caused by weakening the trap."

[[claim]]
id = "edh-weff-structure-is-breathing-phase-not-resonance"
claim = "The ω_eff structure at B = 10.4 nT is a phase sample of the breathing mode at a fixed hold length, not a resonance in the cascade: radial expansion over the hold has a minimum at ω_eff ≈ 0.59-0.62 where the cloud CONTRACTS, and doubling the hold moves the ratios."
status = "live"
quantity = "origin of the weff structure @ B=10.4nT"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, B = 10.4 nT, 26 arms over ω_eff ∈ [0.15, 1.00] at the 8 ms hold plus two at 16 ms."
uncertainty = "The ratio r(end)/r(hold start) runs 1.571 (ω_eff = 0.200) → 0.949 (0.590, the minimum) → 1.140 (0.830) → 1.097 (1.000). Doubling the hold moves it from 0.989 to 1.006 at ω_eff = 0.650 and from 1.096 to 1.033 at 1.000 — a resonance would not move. The first 11 frames of the 8 ms and 16 ms holds agree exactly, which is the check that the window extraction is right."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff0p590_B10p4nT.yaml", "runs/klaus_quench_weff/klaus_weff0p650_B10p4nT_hold2p0x.yaml"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.2"
pr = 0
prediction = "If the structure is a breathing phase rather than a resonance, doubling the hold must move the expansion ratio; a resonance would leave it where it is."
prediction_registered = "before"
prediction_outcome = "hit"
note = """NOT a supersession of `edh-104nt-peak-reads-the-transient-for-80-percent`, and it was
written as one for a minute until the link checker refused it. That row is still true —
22 of 26 arms take their maximum at frame 32 — and this one EXPLAINS it. Explanation and
replacement are different relations, and the ledger only has vocabulary for the second.

THIS EXPLAINS THE POPULATION I HAD RECORDED AS UNEXPLAINED. The four arms whose `peak`
falls in the hold interior — 0.590, 0.620, 0.650, 0.680 — are EXACTLY the four with the
smallest radial expansion, three of them contracting. Where the cloud is compressed at the
end of the window the density is high, the cascade is still running, and the maximum lands
on the last frame; everywhere else the cloud expands, density falls, the cascade stalls and
the maximum is the pre-hold transient. Two independent readings — the argmax frame of
P_adj, and the radial dynamics — pick out the same four arms.
It also removes the reason to look for a cascade resonance at either field: a structure in
ω_eff at fixed hold length is what a breathing phase looks like. That does not by itself
refute the 5.2 nT reading, which has not been measured this way — but it is now the
hypothesis to beat there, and it is cheap to test from the same cached snapshots.
MEASURED 2026-08-22 (#444, §12.5): it was cheap, and 5.2 nT behaves the same way.
See `edh-5p2nt-structure-is-breathing-phase-not-resonance` — same mechanism, and
there the hold-doubling test inverts the ORDERING rather than merely moving it."""

[[claim]]
id = "edh-5p2nt-structure-is-breathing-phase-not-resonance"
quantity = "origin of the weff structure @ B=5.2nT"
claim = "The ω_eff structure at B = 5.2 nT has the same origin as at 10.4 nT — a breathing phase sampled at fixed hold length, not a cascade resonance. Radial expansion over the hold is a single oscillation with a minimum of 0.9868 at ω_eff = 0.600 (the cloud CONTRACTS over 0.570–0.620) and a maximum of 1.1441 at 0.770; the P_adj argmax frame jumps from the last frame (42/42, a truncation) to an interior frame (38/42) exactly at the ω_eff = 0.650 valley; and doubling the hold INVERTS the ordering, taking 0.650 from valley to maximum."
status = "live"
type = "B"
scope = "32³, CPU, one seed, `lhy: none`, anti-aligned, B = 5.2 nT, 20 arms over ω_eff ∈ [0.42, 1.00] at the 8 ms hold plus 4 at 16 ms. Read from cached snapshots — no new runs."
uncertainty = "The EXISTENCE of the structure is bounded well clear of any floor: expansion swings 0.9868 → 1.3587 (38 %) against a dt/2 reproducibility floor of 0.029 %, and the hold-doubling ratios run 1.024 → 1.287. The POSITIONS are quantised by the ω_eff grid — 0.02–0.05 wide near the extrema — so 'minimum at 0.600' means 'at the sampled point 0.600', with no fit and no interval. The 16 ms arms are themselves truncated (3 of 4 peak at frame 53/53), so their VALUES bound the inversion but are not converged."
uncertainty_basis = "control"
evidence = ["runs/klaus_quench_weff/klaus_weff0p600_B5p2nT.yaml", "runs/klaus_quench_weff/klaus_weff0p650_B5p2nT_hold2p0x.yaml", "scripts/validation/klaus_weff_cloud_size.jl"]
evidence_status = "in_tree"
commit = "unknown"
doc = "docs/campaign/edh_quench_polarisation_decision.md"
section = "12.5"
pr = 0
prediction = "If the 5.2 nT two-branch structure is a breathing phase, (1) radial expansion must have structure in ω_eff, (2) the arms whose P_adj peaks inside the hold must coincide with the least-expanded group, and (3) doubling the hold must move the structure — which a resonance cannot do."
prediction_registered = "before"
prediction_outcome = "hit"
note = """Registered in #444 before the measurement. (1) and (3) hit outright. (2) hit in the
form the MECHANISM predicts rather than the form the prediction guessed, and #444 had
already flagged that: at 5.2 nT all 20 arms peak inside the hold, so 'inside vs outside' —
the discriminator that split 10.4 nT four-to-twenty-two — is degenerate here. The
information is in the argmax FRAME, which splits the arms at exactly the valley.

Same mechanism, both fields: where the cloud is compressed the density is high, the
cascade is still running and the maximum lands on the last frame; where it expands the
density falls, the cascade stalls and the maximum moves inside. At 10.4 nT that picked out
four arms; at 5.2 nT it picks out a boundary at ω_eff = 0.650.

The sharpest single number is the ORDERING inversion, not the ratios. At 8 ms
0.550 > 0.770 > 0.650; at 16 ms 0.650 > 0.550 > 0.770. A resonance is a property of the
field and the trap and does not care how long you watch.

Does NOT close `edh-104nt-observable-not-window-robust`: this explains why the window
matters, it does not supply a window-independent observable. Does not restore an optimum
at either field — §12.3 stands.

Method note, because it worked twice: the populations were re-read seven ways at 10.4 nT
and every reading disagreed with the others; the cloud settled it in one pass, from data
already in the cache. Look at a different quantity before inventing another reading of the
one you have."""

# --- The ¹⁵¹Eu evaporation arc (issue #75) ----------------------------------
#
# The third document poured. Opened 2026-08-22 while writing the arc up as one
# wiki article, which is what made the gap visible: the retractions were being
# tracked in a page OUTSIDE the repository, where no gate reaches them.
#
# The arc is a two-layer answer and the layers must not be quoted as one. The
# THERMODYNAMIC layer says how many atoms the ramp allows; the DYNAMICAL layer
# says how many the classical field actually collects. They disagree by three
# orders of magnitude, and that disagreement is the result, not an error.

[[claim]]
id = "eu-evap-thermodynamic-peak-4p41e4"
claim = "The euv3 ramp allows a peak of N₀ = 4.41×10⁴ condensed atoms at t = 1.70 s — 88 % of the measured 5.02×10⁴ — and falls monotonically to 3.5×10³ by the end of the ramp, so optimising this ramp means stopping it at the peak."
status = "live"
anchors = ["miyazawa2021:omega_bar_bec"]
type = "C"
quantity = "peak equilibrium N_0 on the euv3 evaporation trajectory"
window = "the whole euv3 ramp"
reduction = "max"
boundary = "reject"
supersedes = ["eu-evap-peak-18-point-sample"]
scope = "The semiclassical Hartree-Fock constraint (Popov / Zaremba-Griffin-Nikuni) evaluated at EVERY point of one 0-D euv3 trajectory. Equilibrium, not dynamics — whether the field can follow is `eu-evap-cfield-does-not-collect-the-target`, and it cannot. N_total(t) is 0-D output and carries the K₃ systematic."
uncertainty = "Trajectory-sampling error bounded at 10 % by the two scans themselves (18 points reads 4.01e4, every point reads 4.41e4). The K₃ systematic on N_total is NOT folded in: K₃ moves the total, not whether a condensate exists — see `eu-evap-k3-outside-universal-vdw-band`."
uncertainty_basis = "grid"
evidence = ["scripts/kz/eu_number_conserving.jl", "scripts/kz/mu_lda_allocation.jl", "src/solvers/thermal_cfield.jl"]
evidence_status = "in_tree"
commit = "e09020de"
doc = "docs/guides/spgpe.md"
section = "The number-conserving formulation, and what it gives"
pr = 442
note = "The 0-D model's own final N₀ = 1789 (3.6 % of measured) is the value at the WRONG END of this curve, not a failure of the model. That distinction is the whole content of the row."

[[claim]]
id = "eu-evap-peak-18-point-sample"
claim = "The peak equilibrium N₀ on the euv3 trajectory is 4.01×10⁴ at t = 1.726 s."
status = "superseded"
retraction_evidence = """sampling, 18 points -> a dense sweep of the SAME euv3 trajectory under the same constraint: 4.01e4 at t = 1.726 s -> 4.41e4 at t = 1.70 s, i.e. the 18-point value was 10 % low. Same physics and same trajectory; only the sampling density changed, which is why this is `superseded` and not `refuted`."""
superseded_by = "eu-evap-thermodynamic-peak-4p41e4"
type = "C"
scope = "Same constraint, same trajectory — sampled at 18 points."
uncertainty = "10 % low. The two numbers are produced by the same constraint from the same trajectory; the difference is sampling, not physics."
uncertainty_basis = "grid"
evidence = ["scripts/kz/eu_number_conserving.jl", "src/solvers/thermal_cfield.jl"]
evidence_status = "in_tree"
commit = "e09020de"
doc = "docs/guides/spgpe.md"
section = "The number-conserving formulation, and what it gives"
pr = 442
retired_literal = ["4.01e4", "4.01\\times10^4"]
replacement_literal = ["4.41e4", "4.41\\times10^4"]
note = "Kept because the 18-point table is still the readable one and is still in the document; the row it straddles is the one that must never be quoted as the peak."

[[claim]]
id = "eu-evap-halve-the-ramp-plus-36pct"
claim = "Halving the euv3 ramp raises the peak equilibrium N₀ by 36 %, 4.41×10⁴ → 6.01×10⁴, with a broad optimum over 0.3–0.5×."
status = "scoped"
type = "B"
quantity = "ramp-time scaling that maximises peak equilibrium N_0"
window = "the whole ramp, at each time-axis scaling"
reduction = "max"
boundary = "unchecked"
scope = "Stretching the `FortRamp` TIME AXIS at fixed beam powers — a numerical experiment, not a ramp design. Valid only at and above 0.3×: below that the trap depth outruns thermalisation (0.02-0.05 elastic collisions per e-folding against the conventional requirement of a few) and `condensate.jl`'s quasi-static assumption is void. The recommended 0.3-0.5× sits at 30.6 and 74.2 collisions per e-folding."
uncertainty = "unbounded: a one-trajectory scan with no repeat. The DIRECTION is corroborated independently — a 3-parameter Bayesian search over the same ramp finds duration monotone with +42 % at ≈0.4×, and adding a finite-evaporation-rate penalty turns that knife-edge into an interior optimum at ≈0.6× worth +22 %. Three instruments, one direction, three different magnitudes."
uncertainty_basis = "none"
evidence = ["scripts/kz/ramp_slowdown_design.jl", "scripts/kz/ramp_quasistatic_validity.jl", "docs/guides/eu_shape_finite_t.md"]
evidence_status = "in_tree"
commit = "29815c56"
doc = "docs/guides/spgpe.md"
section = "The design answer: halve the ramp"
pr = 442
note = "`scoped` rather than `live` because the non-monotone N₀ below 0.3× (3.29 → 1.85 → 1.64 → 3.41e4) is what a model looks like when asked outside its assumptions, and the first version of this table had no validity column. A genuinely slower ramp also costs more three-body loss, which the time-axis stretch does not model — scanned separately and pointing the same way."

[[claim]]
id = "eu-evap-cfield-does-not-collect-the-target"
claim = "Doubling the thermodynamic target by halving the ramp does not move the c-field's condensate at all: 41.1 ± 14.3 atoms at 0.5× against 41.6 ± 10.9 at 1.0×, three seeds each, a difference of 0.05σ. The rate limit is on the field's side, not in how many atoms the thermodynamics allows."
status = "live"
type = "B"
quantity = "c-field N_0 at end of ramp, 0.5x vs 1.0x euv3"
scope = "Full SPGPE (growth + energy damping) with both coefficients derived from the reservoir, handoff at 1.85 s, three seeds per arm. A NULL — and the seeds were run before the claim was written, not after someone asked."
uncertainty = "± 14.3 and ± 10.9 (sd over 3 seeds); the difference is 0.5 ± 10.4, i.e. 0.05σ. Seed-limited, and the seed floor is the same size as the effect the earlier one-trajectory reading reported."
uncertainty_basis = "seed"
evidence = ["scripts/kz/eu_number_conserving.jl", "scripts/kz/fixed_point_condensation.jl"]
evidence_status = "in_tree"
commit = "cc8e0e20"
doc = "docs/guides/spgpe.md"
section = "The design answer: halve the ramp"
pr = 446
note = "The positive control that this is not a broken solver: at the SAME end conditions held fixed the field condenses to f₀ = 0.56 and is still rising, and on the ramp the thermal modes track equilibrium to 30 % while the condensate mode does not grow at all. A broken engine misses both."

[[claim]]
id = "eu-evap-slowing-helps-by-7x"
claim = "Slowing the ramp 3x improves the c-field condensate by 7x (2.9x on a second reading)."
status = "refuted"
retraction_evidence = """readout time, a single end-point marker -> six matched markers along the trajectory. The 3x run declines monotonically while the 1x run climbs to f0 = 0.255 and overtakes it from the third marker on, so a verdict taken at one end is the opposite of the verdict taken at the other. The axis the original never varied was WHERE along the trajectory it was read, not how many seeds it had."""
type = "B"
scope = "Read from the first TWO markers of a six-marker trajectory pair."
uncertainty = "Not a bound — a sign error. Over six matched markers the 3x run declines monotonically while the 1x run climbs to f₀ = 0.255 and overtakes it from the third point on. A verdict taken at either end is the opposite of the one taken at the other."
uncertainty_basis = "control"
evidence = ["scripts/kz/ramp_slowdown_design.jl", "scripts/kz/eu_number_conserving.jl"]
evidence_status = "in_tree"
commit = "cc8e0e20"
doc = "docs/guides/spgpe.md"
section = "The dynamics: the field does not condense at the derived rates"
pr = 446
retired_literal = ["7× and 2.9×"]
replacement_literal = ["Slowing does NOT help"]
note = "Killed by extending the same comparison from two matched markers to six, in `scripts/kz/ramp_slowdown_design.jl`. No successor row: the replacement is not another number but the absence of one — over the full six the ordering inverts, so there is no ratio to quote in either direction. `eu-evap-halving-the-ramp-is-worse` is the same mistake made once more from one seed, and `eu-evap-cfield-does-not-collect-the-target` is what the properly-seeded comparison actually returned: 0.05 sigma."

[[claim]]
id = "eu-evap-halving-the-ramp-is-worse"
claim = "Halving the ramp makes the c-field condensate worse: 27.8 against 39.7."
status = "refuted"
retraction_evidence = """seed count, 1 -> 3 per arm: 41.1 +- 14.3 at 0.5x against 41.6 +- 10.9 at 1.0x, a difference of 0.05 sigma. The reported 27.8 against 39.7 was one draw from a distribution whose seed-to-seed scatter is +-12, the same size as the claimed effect."""
type = "B"
scope = "One seed per arm."
uncertainty = "The seed-to-seed scatter is ±12, the same size as the reported difference. Refuted by running two more seeds per arm."
uncertainty_basis = "seed"
evidence = ["scripts/kz/ramp_slowdown_design.jl", "scripts/kz/eu_number_conserving.jl"]
evidence_status = "in_tree"
commit = "cc8e0e20"
doc = "docs/guides/spgpe.md"
section = "The design answer: halve the ramp"
pr = 446
retired_literal = ["27.8 against 39.7"]
replacement_literal = ["41.1 ± 14.3"]
note = "Recorded as a pair with `eu-evap-slowing-helps-by-7x` on purpose: the same document produced both errors from the same cause — a verdict read off a sample smaller than its own scatter. The second time, the seeds were run BEFORE the claim, which is the only reason this row says `refuted` instead of the document saying it."

[[claim]]
id = "eu-evap-k3-outside-universal-vdw-band"
claim = "No K₃ inside the universal van der Waals band reproduces the measured ¹⁵¹Eu endpoint: matching 5.02×10⁴ needs K₃ ≈ 2.45×10⁻⁴⁰ m⁶/s at T₀ = 50 µK and ≈ 2.74×10⁻⁴⁰ at 18 µK, against a band top of K₃ = 3Cℏa⁴/m ≤ 9.7×10⁻⁴¹ at C = 67, a = 110 a₀. The endpoint gap is a model deficiency, not a parameter to tune."
status = "live"
anchors = ["miyazawa2021:three_body_L", "miyazawa2021:a_s_thesis"]
type = "C"
quantity = "K_3 required to reproduce the measured euv3 endpoint"
window = "the measured euv3 endpoint, a fixed time"
reduction = "none - a fixed-time value, not an argmax"
boundary = "n/a"
scope = "0-D two-component model with the exact all-η LRW rate and ⟨n²⟩ = 8/21 n₀², swept over K₃ ∈ [10⁻⁴²·³, 10⁻³⁹·³] at both loading anchors. The band top is the zero-range Efimov result; finite-range corrections move it by a few per cent, not by 2.5×."
uncertainty = "The 2.5× exceedance is far outside the band's own width, which is the full [0, 67] oscillation in C and is already taken at its maximum. Independently re-evaluated 2026-08-22: 3 × 67.1 × ℏa⁴/m = 9.7×10⁻⁴¹ m⁶/s for ¹⁵¹Eu at a = 110 a₀."
uncertainty_basis = "control"
evidence = ["docs/guides/figures/eu_evap_k3_band_exclusion.jl"]
evidence_status = "in_tree"
commit = "7318b458"
doc = "docs/guides/evaporation_model.md"
section = "Robustness — the optimum is near a cliff"
pr = 442
note = "The deficiency is LOCATED, not merely asserted: the K₃ loss law itself is correct ab initio — a closed-system 3D decay fits N₀^1.796 against the 0-D N₀^1.800 — and the density profile is TF-accurate to ~2 %. What is missing is the formation dynamics, because the 0-D model forms the BEC while the trap is still tight. That is the same hole the SPGPE sees directly from the other side."

[[claim]]
id = "eu-evap-eta-start-is-2p07"
claim = "The shipped euv3 defaults start evaporation at η = U/(k_BT₀) = 2.07, BELOW the η ≈ 4 floor at which evaporation can start, and re-anchoring T₀ does not lift it — both self-consistent epoch choices land at ≈ 2."
status = "live"
anchors = ["miyazawa2021:alpha_scalar_1550nm"]
# `registered` again as of 2026-08-25, and the round trip is the record: promoted,
# refused by the `anchors` rule within the hour that rule landed because
# `refs/miyazawa2021.toml` did not exist, then promoted back once the source was
# registered. The ladder was kept through the demotion precisely because it was
# correct and only the arbitration was missing.
lifecycle = "registered"
ladder = """exact: eta = U/(k_B T_0) evaluated on the shipped euv3 defaults; a value read off a configuration, not a converged result. Control: both self-consistent epoch choices land at ~2, so the number does not depend on which one is taken."""
type = "C"
quantity = "eta_start on the shipped euv3 defaults"
supersedes = ["eu-evap-eta-window-4p5-to-11"]
scope = "`evaporation_summary(run_euv3_evaporation()).eta_start`, the shipped defaults. 2022 trap + T₀ = 50 µK gives 2.07; 2023 trap + T₀ = 18 µK gives 2.02; the MIXED pairing gives 5.75 and is an epoch-mixing artefact, not a third option."
uncertainty = "Exact for the model: 2.0682, one deterministic call. What is uncertain is the apparatus, not the number — the 2023-07 notebook measured η_start = 3.7 directly in a configuration where the model gives 10.5, the same 2.84× that separates the two epochs' trap depths."
uncertainty_basis = "control"
evidence = ["src/solvers/evaporation/euv3.jl", "test/solvers/test_evaporation.jl", "docs/guides/evaporation_model.md"]
evidence_status = "in_tree"
commit = "b672e1ba"
doc = "docs/guides/evaporation_model.md"
section = "Is the trap capable of runaway evaporation?"
pr = 442
note = "Found by running the very call the document tells the reader to make. Consequence for everything downstream: treat absolute numbers from the defaults as suspect, and quote ratio-type conclusions instead."

[[claim]]
id = "eu-evap-eta-window-4p5-to-11"
claim = "η stays in a valid 4.5–11 range over the euv3 ramp."
status = "superseded"
retraction_evidence = """epoch anchoring, the guide's assumed T0 -> both self-consistent epoch choices, on the shipped euv3 defaults: eta_start = 2.07, not 4.5. More than a factor of two, and on the wrong side of the eta ~ 4 floor at which evaporation can start at all, which changes the KIND of statement rather than its precision. Re-anchoring T0 does not lift it - #356 corrects #333 and #348 on exactly that point."""
superseded_by = "eu-evap-eta-start-is-2p07"
type = "C"
scope = "Asserted for the shipped euv3 defaults, in four places in the guide."
uncertainty = "Wrong by more than a factor of two at the start, and on the wrong side of the eta_min = 4 floor — which changes the KIND of statement it is, not its precision."
uncertainty_basis = "control"
evidence = ["docs/guides/evaporation_model.md"]
evidence_status = "in_tree"
commit = "b672e1ba"
doc = "docs/guides/evaporation_model.md"
section = "Is the trap capable of runaway evaporation?"
pr = 442
retired_literal = ["4.5–11"]
replacement_literal = ["2.07"]

[[claim]]
id = "eu-evap-condensate-n2-moment-is-8-over-21"
claim = "The atom-weighted mean-square density of a Thomas-Fermi condensate is ⟨n²⟩ = ∫n³/∫n = (8/21)n₀², which with n₀ ∝ N₀^(2/5) gives the three-body attractor dN₀/dt = -γN₀^(9/5), hence N₀ → [(4/5)γt]^(-5/4) — independent of the initial atom number."
status = "live"
anchors = ["miyazawa2021:tau_3body_initial"]
lifecycle = "registered"
ladder = """exact: a ratio of Beta integrals B(3/2,4)/B(3/2,2). Two independent numerical confirmations — a TF seed measures 0.381 = 8/21 rather than 0.571 = 4/7, and a closed-system 3D SGPE decay fits the exponent to 1.796 against the predicted 1.800."""
type = "B"
quantity = "TF condensate mean-square density coefficient"
scope = "Any Thomas-Fermi condensate. Aspect-ratio invariant, so it survives DDI deformation."
uncertainty = "Exact — a ratio of Beta integrals, B(3/2,4)/B(3/2,2). Confirmed numerically two independent ways: a TF seed measures 0.381 = 8/21 rather than 0.571 = 4/7, and a closed-system 3D SGPE decay fits the resulting exponent to 1.796 against the predicted 1.800."
uncertainty_basis = "control"
evidence = ["src/solvers/evaporation/condensate.jl", "docs/theory/eu_evaporation_three_body_theory.md"]
evidence_status = "in_tree"
commit = "a5c7db4d"
doc = "docs/theory/eu_evaporation_three_body_theory.md"
section = "BEC 数を決めるアトラクタ"
pr = 442
note = "Carried as (4/7)n₀² until 2026-07-25 — a 1.5× over-loss. 4/7 is the MEAN density ⟨n⟩/n₀ = ∫n²/∫n, a different moment, so this was a moment error and not an arithmetic one; it moved the headline trap-shaping gain from 1.46× to 1.63×. Deliberately NO `retired_literal`: `4/7` is too common a string to hunt for without reddening on unrelated text, and the retracted form lives in deleted code rather than in a quotable number. A row whose retraction cannot be gated should say so rather than declare a literal that would get the gate deleted."

[[claim]]
id = "eu-evap-trap-shaping-gain-not-poured"
claim = "The trap-shaping half of this arc — the 1.63× joint waist + Feshbach gain, the m_ω ≳ 0.6 gravity floor, the 1.28× three-dimensional SGPE confirmation of the waist mechanism — is not in this ledger."
status = "open"
type = "B"
scope = "docs/guides/eu_evaporation_optimization.md, FROZEN 2026-07-28, plus the 3D shape work in docs/guides/eu_shape_finite_t.md."
uncertainty = "unbounded: that is the content of the row. Those numbers have never had their scope, their uncertainty or their evidence status written into a field, and the guide holding them is frozen."
uncertainty_basis = "none"
evidence = ["docs/guides/eu_evaporation_optimization.md"]
evidence_status = "in_tree"
commit = "5dd7092d"
doc = "docs/guides/spgpe.md"
section = "Known limits"
pr = 442
note = "Recorded rather than poured, because pouring it honestly needs the 3D SGPE arm re-read and its gravity-free symmetric trap stated as a scope restriction, and that is a session's work rather than a paragraph. An `open` row is what makes a deferral distinguishable from an oversight — the same argument the `closed` status exists for, one step earlier."

[[claim]]
id = "saito-li-edh-response-1p75x"
quantity = "EdH magnetisation response ratio vs Li-Saito Fig 4b @ 0.05 mG"
claim = "RETRACTED: the 1.75x ratio between our EdH magnetisation response and Li-Saito Fig 4(b) at 0.05 mG rests on ours = 0.03750, and that value does not reproduce on current code AT ITS OWN RECORDED SETTINGS."
status = "refuted"
retraction_evidence = """reproduction at the claim's OWN recorded settings (box 8 / n 80 / dt 2e-4 / orient=rotate / t_end 10): 0.03750 -> 0.2778 +- 0.0185, a factor 7.4 against a per-sample scatter of 6.7 %. The diagnostic is the edge density, 1.546e-3 against the record's 6.08e-4, and edge density is a property of the evolved STATE rather than of the analysis window - so this is not the same trajectory read differently."""
retired_literal = ["0.03750", "0.03722"]
type = "C"
scope = "runs/saito_li_torus, the EdH quench arm at B_z = 0.05 mG, 14.5 ms window, mean |f_z| over t > 3 ms. Binds README 'continued 6' and h16_digitise_fig4b.py's OURS dict."
uncertainty = "The NON-REPRODUCTION is bounded well clear of any floor: re-run at the recorded settings (box 8 / n 80 / dt 2e-4 / orient=rotate / t_end 10) gives 0.2778 +/- 0.0185, a factor 7.4, against a per-sample scatter of 6.7 %. Four independent routes agree within 27 % on 0.22-0.28 — static response 0.2214 (no dynamics at all), the 0.025 mG dynamic arm doubled 0.2627 (that arm PASSES every gate, edge 3.6e-4), box-10 dynamic 0.2805, box-8 dynamic 0.2778. What is NOT bounded is the replacement value itself: three of those four are box-gate-marginal (edge 1.3-1.5e-3), and only the 0.025 mG arm is clean."
uncertainty_basis = "control"
evidence = ["runs/saito_li_torus/cells/stiff_n64_Bz0p025.yaml", "runs/saito_li_torus/cells/stiff_n64_Bz0p050.yaml", "runs/saito_li_torus/h6_edh.jl"]
evidence_status = "in_tree"
commit = "unknown"
doc = "runs/saito_li_torus/README.md"
section = "1"
pr = 0
note = """THE DIAGNOSTIC IS THE EDGE DENSITY, NOT f_z. At identical box / n / dt / orient the
re-run reports max edge frac 1.546e-3 against the record's 6.08e-4 — 2.5x. Edge density is a
property of the evolved STATE, not of the analysis window, so this is not the same trajectory
read differently. Something in the state being propagated changed between the record and now,
and that has not been identified.

WHAT THIS DOES AND DOES NOT SETTLE. It does NOT explain the 1.75x — it removes it, by showing
the numerator is not reproducible. Rebuilt from a reproducing value the discrepancy against the
paper would be ~13x, i.e. possibly much LARGER. That is a report that the problem may be worse,
not a diagnosis.

It also does not license quoting 0.2778. Only the 0.025 mG arm passes every gate; the 0.05 mG
arms are edge-marginal at both box 8 and box 10, and #435's own warning stands — the edge-density
gate does not guarantee the DDI box, which must be set by h19_ddi_fkappa_oracle.jl's f(kappa)
comparison.

The 0.10 mG comparison point is separately void and NOT for want of a bigger box: the static scan
in this session puts the ground state at that field on the POLARISED branch (<F_z> = -5.851,
E = -7.8527) rather than the torus (E = -1.6557). The quench there drives the system off-branch,
which is why its edge density reaches 7.7e-2 and corr(f_z, L_z) flips from -0.99 to +0.22."""

[[coverage]]
doc = "docs/theory/eu_evaporation_three_body_theory.md"
covered = 0
note = "1 section by the numbering regex — and it is a false positive: the heading is '0 次元の限界と、これから', a Japanese prose heading that happens to start with a digit. Pinned at 0 so the ratchet still guards a real fall if the document is ever numbered."
