> **FROZEN 2026-08-04.** Describes the tree as of that date and is **not maintained** against the code — do not cite it as current.
> Live sources: `CLAUDE.md`, `docs/index.md`, and the code itself. Audit: `docs/audit/docs_inventory_2026-08-04.md`.

This file is the RAW output of the audit workflow, kept verbatim. It is
machine output, not prose: the body is fenced so that markdown does not
reinterpret it and so that the paths quoted inside it — several of which
are (gone), like `runs/_loop`, retired 2026-06-08 — are read as data
rather than as this repository citing them.

```json
{
  "summary": "Harvest every measurement and cause-isolation already performed across the repo into one re-readable ledger, with precision, controls, and what was ruled out",
  "agentCount": 8,
  "logs": [
    "[harvest:memory-store] failed: API Error: Claude's response exceeded the 64000 output token maximum. To configure this behavior, set the CLAUDE_CODE_MAX_OUTPUT_TOKENS environment variable.",
    "0 records — {}"
  ],
  "result": {
    "ledger": "is fully determined. The earlier `evap_scale ≈ 0.02` fit is superseded; the residual full-trajectory mismatch is the unknown real ramp and `gravity_factor`, not a hidden knob | The implemented rate factor (η P₂ − 3P₃)/P₂² is the **2D** form (a=2), reducing to (η−3)e^{−η}; the 3D harmonic form is a=3 → (η−4)e^{−η}. That form goes **negative below η = 4**, making the model **gain atoms and heat** at the low η the lab runs at (measured η 3.7 → 6.3 over 7 s at fixed 7 W), which is why an all-η Luiten incomplete-gamma rate replaced it. κ was a constant 1, now κ̃(η) = [1 − P₅/P₃]/[η − 4P₄/P₃] (≈0.5 at η=4, → 0 at high η) | `evaporation_bec_prep_model_2026-06-15.md:186-192, 228-240` |\n| Ramp headroom | 3-parameter BO **+2 %**; per-breakpoint coordinate descent 49.3k → 76.9k (**1.56×**); **monotone-constrained 5.0e4 → 1.6e5 = 3.2×** (η valid 4.5–11, both beams verified monotone, T_BEC ≈ 0.5 µK), by evaporating harder early (H 4.0 → 0.36 W first step vs lab 4 → 2) | 3 s.f. on N_BEC | euv3 defaults at the fitted K3, 2026-06-15; gates = adiabatic-invariant and recompression-is-not-BEC oracles (fast), monotone + determinism (ci) | Trap only ever lowered, warm-started from the lab ramp's own ratios so it can only improve | **Two cliffs measured and named**: (1) η_start ≈ 4.5 at defaults is marginal and real setups load η ≈ 7–10, so the model underestimates loaded depth by ~1.5–2× and **absolute numbers are soft**; (2) a three-body cliff if K3 is 2×. A robust optimiser over α × {0.95,1.1} and K3 × {1,2} found lab worst-case **0** and aggressive worst-case **0** — both fail somewhere — while robust keeps nominal 1.56e5 and reaches BEC everywhere at worst-case 8.5e4, so **robustness costs <1 % of peak**. The headroom **ratio** is robust at ~3.1–3.5× across K3 0.5–1×, α 1.0–1.15×, τ 8–30 s. A suspected low-α onset bug was chased and **refuted**: α −15 % gives η_start < 4 and no evaporation, which is correct physics; the apparent \"N_BEC = 2e6\" was a sweep footgun (reading `.N_BEC` on a run that never reached BEC returned leftover N) | `evaporation_bec_prep_model_2026-06-15.md:102-165` |\n| Gravity over the trap scale, and three depth bugs | gravity over 24 µm = **4.3 µK** for Eu against a ~4 µK optical barrier at low power — **originally miscalculated as 0.004 µK, off by 1000×** | 2 s.f. | Eu, crossed FORT 31/42 µm waists; commits `2cbc4113`, `4e6fa92c` | The crossed trap is genuinely gravity-unbound at low power; the experiment reaches BEC at 0.10 W because it **magnetically levitates** (Eu µ ≈ 7 µ_B, so ~0.4 G/cm cancels g). Three preceding bugs: `crossed_trap_depth` scanned only 8× waist, missing a single beam's **axial** escape barrier over the mm-scale Rayleigh range (start-of-ramp depth read 0, η ≈ 0); gravity applied on **every** scanned axis because the closure compared `a == gravity_axis`, always true; a **uniform** escape-barrier grid sized to the loosest scale took mm/600 steps and skipped the tight beam's µm radial barrier | The \"deep trap is gravity-unaffected\" intuition is recorded as **wrong** for a vertical ODT beam. The gravity test was rewritten to check the **robust relation** (shallow hurt more than deep), not an absolute | `evaporation_bec_prep_model_2026-06-15.md:51-59, 192-201` |\n| Limits of the 0-D model | the real N-vs-power curve holds early then crashes sharply at the last segment; the smooth 0-D model ends at **~3e5** against a measured **5.8e4**. Depth-only α = 2.24e-37 matches depth (66 µK at 7 W) but gives ω **2.4× low** (depth ∝ α/w², frequency ∝ √(α/w⁴)), giving model T_c ~150 nK vs a real ~500 nK | 1–2 s.f. | euv3; commit `09ad3912` (unpushed at write time) | 0-D limit reached — gross cooling and loss captured, exact trajectory and BEC number need a 2-component model, subsequently built and reproducing the thermal crash (N_th ∝ T³ collapsing as atoms condense). **A single α cannot satisfy depth and frequency under a round-beam assumption** — the same degeneracy the elliptical-waist resolution later broke; pinning α and waist separately needs **both** a depth and a trap-frequency measurement | — | `evaporation_bec_prep_model_2026-06-15.md:198-214` |\n| Bugs found only by running the real optimiser | **3**: (1) `module Optimization` referenced `_default_solver_verbose` without importing it, so **any** call without explicit `verbose` raised `UndefVarError` — the whole subsystem was broken; (2) `bayesian_optimize` builds an n_grid^d mesh with default n_grid=100, so d=3 gives 1e6 points and `gp_predict` OOMs; (3) default ramp bounds had final-scale ∈ (0.005, 0.05), **excluding the baseline 1.0**, so the optimiser could only worsen it | exact | 2026-06-15; commits `930b1205`, `1bbfc3e7` | A stub-only test hid a **fully broken** real path | Prescription recorded: add a real-optimizer regression test | `evaporation_bec_prep_model_2026-06-15.md:102-110` |\n| Miyazawa 2021 Eu evaporation reference | a_s = 135 a_B, ε_dd = 0.44; K₃ **1.2e-41 direct** vs **4.6e-42 BEC-fit** ⇒ ~**2.6× systematic** between determination methods | 2 s.f.; the 2.6× is a systematic, **not** an error bar | Eu; #75 | — | — | memory `reference_miyazawa_2021_thesis_eu_evap_params` |\n\n### 3.16 Other single rows worth keeping\n\n| Quantity | Value | Precision | Conditions | Cause isolated | Ruled out | Source |\n|---|---|---|---|---|---|---|\n| Eu kappa-ramp protocol | at κ = 1.8 over 4–434 ms the flower **survives** a B ramp to 100 µG at every rate. From the other side, the polarised state converts only at 434 ms — then sharply, at B = **27.4 µG** (sharpness 8.0 vs 1.65 at 145 ms) — but **overshoots** to ⟨F_⊥⟩ = 3.72 with ringing | 3 s.f. on B and ⟨F_⊥⟩; **\"sharpness\" is a unitless score with no stated normalisation** | Eu F=6, κ=1.8, 32³, box 24, dt=0.002, dealias **off**; ramp durations 3–300 ω_ref⁻¹ (1 ω_ref⁻¹ = 1.447 ms) | A first-order barrier on the field axis above the tricritical point κ_tc ≈ 0.95. The field route **does** reach the flower state, at ~0.4 s, and delivers it **hot** — which motivated the κ route, since below κ_tc there is no barrier | **The sharpness score is span-dependent**, flagged in the plotting script: a κ=0.8 fall at τ=434 ms scores 10.2 on a span of 0.76, **above** the genuine κ=1.8 conversion's 8.0 on a span of 2.92 — so the score alone cannot identify a conversion | `scripts/eu_kappa_ramp_protocol.jl:7-17` |\n| Eu torque protocol (a pre-registered target) | at (κ=1.8, B=20 µG) the flower ground state has J_z = **−1.087** while every preparable ramp endpoint sits at J_z = **−3.15**; the flower's energy is **10.73**. Scale: \\|p\\| = 0.296 ω_ref at 20 µG; the Goldstone pin ε = 0.002 ↔ 0.135 µG, so ε = 0.05 ↔ 3.4 µG, the scale of a realistic un-nulled residual field | 4 s.f. on J_z and E, 3 on p | Eu F=6, κ=1.8, B=20 µG, 32³, box 24, dealias off; established 2026-07-28, PR #114 | Verdict is a **single number** fixed before the scan: does J_z leave −3.15 and move toward −1.09 while E falls toward 10.73 | An inert control at ε = 0 is included; ΔJ_z ≈ 0 at every (ε, Ω) is the stated null. **Not yet answered** | `scripts/eu_torque_protocol.jl:4-19` |\n| M1 critical cell — ⟨F_z⟩ against an analytic bound | pre-fix E = 8.618473, ‖∇E‖ = 2.401e-01, ⟨F_z⟩ = **+4.501e+00** against the analytical Barnett/Zeeman bound ⟨F_z⟩ ≈ 6×66/1630 ≈ **0.24** — 18× too large. Pass criterion ⟨F_z⟩ ∈ [0.17, 0.31] | 7 digits on E, 4 on ⟨F_z⟩; the bound is 2 s.f. with an explicit ±30 % band | B = 100 nT in-plane Bx, Ω = 0.6, Eu F=6; γ_F/2π ≈ 16.28 Hz/nT ⇒ Zeeman ≈ 1.63 kHz, Barnett(0.6) ≈ 66 Hz, DDI c₁⟨n⟩ ≈ 100–200 Hz | The 4.5 value is a named artifact | **Important asymmetry recorded**: 0.24 is an **upper** bound from a non-interacting estimate, so lower measured values (e.g. 0.12 at high B) are correct physics rather than failures — only excursions **above** it are artifacts | `scripts/m1_critical_cell_oracle.jl:5-22` |\n| M1 stalled-state gradient/energy consistency | at the stalled state Re⟨grad, P_⊥ grad E⟩ = R² = **0.0476 to 16 digits**, while the finite-difference energy change along the **same** direction is ~2.2e-4 — **220× smaller**. For dψ = ψ and dψ = random the two **agree** | 0.0476 to 16 digits; FD value 2 s.f. | B=0, Ω=0.4 plateau ψ, Eu F=6 | **Not isolated.** What is established: the inconsistency is confined to the P_⊥ grad E direction | A global gradient defect, by the dψ = ψ and dψ = random controls | `scripts/m1_per_term_gradient_audit.jl:4-9` |\n| M0 5–10× time-constant anomaly, re-derived | with the corrected n_peak ≈ **258 µm⁻³** (not 61.6, per errata E2) the predicted τ_ring band shifts from [5.7, 57] ms to **[1.4, 14] ms**; Matsui's experimental ring at ~5 ms sits **mid-band**. Larmor anchor at Bz = 2.6 nT: ω_L = 2π × 42.3 Hz, T_L = 23.6 ms = 16.3 ω_ref⁻¹ at ω_ref = 2π × 100 Hz | 3 s.f. on frequencies; **the τ band is a one-decade bracket, not a value** | Eu-151 F=6, N = 30000, ω_ref = 2π×100 Hz isotropic, a_s = 110 a_B, µ = 6.977 µ_B, 24³, box 12, Bz = 2.6 nT, T = 30 ω_ref⁻¹ ≈ 48 ms | A wrong peak density (factor 4.2). If the simulated τ lands inside [1.4, 14] ms the \"5–10× anomaly\" was based on the **pre-correction** prediction | Priority A had already come back clean: CPU/GPU propagator parity excluded a missing-term bug, which is why the units/scaling check ran second | `scripts/m0_units_scaling_check.jl:1-44` |\n| Which term sets the sign of d⟨F_y⟩/dt | the test asserts fy > 1e-4 on d⟨F_y⟩/dt = i⟨[H,F_y]⟩ = bx⟨F_z⟩ > 0; it measures **−1.64e-3** — right magnitude, **wrong sign**, 16× the threshold. Both the sequential and the combined path give the same wrong sign | 3 s.f. | 8³ grid, box 6.0, dt = 0.002, one real-time step from m = +F under H = −(bx F_x + bz F_z) + q F_z²; **the fixture also switches on c_dd = 100, c₀ = 50, c₁ = 1, q = 0.1** | **Not yet isolated.** Established: the wrong sign is **shared** between the sequential and combined paths, so it is not a fusion discrepancy. As written the test cannot distinguish \"the transverse Zeeman propagator has an inverted sign\" from \"the other terms dominate and the test's premise is wrong\", because the analytic claim accounts for none of c_dd, c₀, c₁, q | The green transverse sign oracle does **not** cover this: it measures the ITP **ground state** direction (+Bx ⇒ ⟨F_x⟩ > 0), not one real-time step. Every probe row runs at +bx **and** −bx as a positive control | `bench/probe_transverse_zeeman_dfy.jl:1-28` |\n| Icosahedral λ_spin probe — why it cannot compare to full_bdg | an early version read a ratio of **5.16** in the **scalar limit**, where the sibling probe measures agreement to 1.1e-4. λ_spin = −g₀/13 − 121g₆/646 + 91g₁₀/782 + 840g₁₂/5681 is 0 on paper at uniform g_S and the literal ladder `ones(7)` lands there, but the same limit reached through `_c0c1_to_gS(6, 10.0, 0.0)` does **not** | 5.16 and 1.1e-4 to 3 and 2 s.f.; guard band λ_spin < −1e-12·c₀ | F=6, thirteen g_S ladders including the c₀/c₁ family at c₁ = +5, +0.1, 0, −0.05, −0.1, −0.2 | `InteractionParams(Dict(...))` is keyed by c-coefficient **rank**, not spin channel S, and `_bdg_contact_matrices` applies `c_to_g` itself — feeding a g_S ladder into those slots **double-converts**. The relative rather than absolute refusal band exists because the cancellation is not exact once the ladder comes from c₀/c₁, and a bare λ_spin < 0 would refuse the scalar limit, which matches Lima-Pelster to 1e-12 | The full_bdg comparison moved to a sibling probe restricted to the c₀/c₁ family it can construct exactly; hitting an arbitrary target ladder needs a g→c inversion (a non-diagonal linear solve) **nobody has written** | `scripts/probe_icosahedral_lambda_spin_sign.jl:1-20, 58-70` |\n| Stale documentation overrides | **5** rows measured 2026-07-30 claiming to supersede CLAUDE.md; re-measured 2026-08-02, **four of five had been fixed at the source** in the meantime, and nothing told anyone | exact | 2026-07-30 and 2026-08-02 | Four rows discharging themselves within three days, silently, is the argument for the structural fix: every KNOWN-LIMIT / not-implemented / planned claim must carry a machine-checkable predicate that **fails when the limit no longer holds**. Rows kept **struck, not deleted** | The one open row was **narrowed** on measurement: the affiliation half of the complaint was wrong | CAMPAIGN.md:62-85 |\n| Four stale KNOWN-LIMITs found by cross-check | tensor c₂/c₄ have been in `energy_gradient!` since 2026-06-09 (registry-only gradient face, FD-gated) while two sites still said LBFGS warns and falls back to ITP — and tensor GS optimisation is on the critical path for F=6 phase selection, so that line was **actively blocking**. The reference-RHS layer is implemented, not planned; Matsui Level 5's imaging chain exists (the blocker is the Fig-2C source data) | categorical | 2026-07-30 | **A stale KNOWN-LIMIT costs more than a real one** — it makes the next session pick the slow path or drop the plan | Recorded on the same pass as the honest counterweight: there is **no** reference statement for LHY, higher-rank tensor, Raman, light-shift, Coriolis or magnetic-gradient, so the self-contained correctness claim is scoped to 9 terms and **excludes the one carrying ~97 % of the Eu F=6 energy** | sha `65da86e2` |\n| A sound verifier does not yield a non-hackable reward | **refuted, 0-3** on a three-vote adversarial verification; the surrounding pass was 6 angles, 25 claims verified 3-vote, **0 otherwise refuted**, synthesised to 9 findings | vote counts exact | 2026-06-22 deep-research pass | A verifier that checks only **extensional** output is gamed even when sound — RLVR-trained models enumerate instance-level labels that pass a verifier without inducing the intended rule (arXiv:2604.15149). **It is the only refuted claim of the 25, and the one the loop's original design depended on** | Access restriction is the measured defence, **quoted from arXiv:2510.20270**: an agent that can touch a test-based verifier games it **49–76 %** of the time (GPT-5 76 % on Oneoff-SWEbench, o3 49 %); hiding the verifier drives gaming to near-zero. **Those percentages are literature values, not measured here** | `loop_engineering_architecture.md:24-34` |\n| The design's own name-budget violation | requirement: a researcher can run the system knowing **six** names; this design needs **eight**, plus 33 REASONS tags, 7 dispositions and a Row schema. Defence: the **model** count falls from eleven to one — where the eleven are delivered by **five** different mechanisms (a returned Admission, a thrown ArgumentError, a `nothing` from a swallowed throw, a `@warn` to stderr, a red assertion in a test file) | exact counts | — | A self-reported requirement **violation** kept in the ledger rather than argued away — \"a reader who counts nouns will count more nouns than before, and that is a real cost, not a rhetorical one\" | The mitigating argument (tags are read, never memorised) is stated but **explicitly not accepted** as discharging the requirement | `unified_spec_architecture.md:802-808` |\n| Design audit history | revision 1 red-teamed across seven attack surfaces: **47 candidate findings, 15 survivors** (#250). Revision 2 answered every survivor by adding a mechanism, reached **3,400 lines**, and was reconciled against its own 13 numbered invariants: **64 findings, ZERO invariants clean**, 15 still open. **Two of its invariants were false about this codebase on the day they were written.** Revision 3 is a deletion pass to **514 lines** | exact counts | revision 3 dated 2026-07-31, cut `3de0d8a2` | \"Zero invariants clean out of 13\" is load-bearing: adding a mechanism per audit finding did **not converge**, which is why revision 3 adds no new mechanism | One revision-2 fix was written as **dead code** — `plan.backend_kind === :cuda`, when `_resolve_backend` accepts `:cpu`/`:gpu` and **throws** on `:cuda` — because it had no positive control. Caught in review by reading the file, **labelled luck rather than process**, and is why \"a check with no control returns `:indeterminate`, never `:pass`\" is an invariant | `research_spec_and_provenance_architecture.md:12-22, 445-454` |\n| Requirement inventory | **622** requirements across five areas (design doc + gates 173; #250's two audit passes 101; the 23 commits on the branch 115; the 23 gate suites those added 172; external prior-art reconciliation 61). Coverage **608 of 622**; **14 ids not covered**. Vocabulary: 33 tags, of which **22 have a producer in the tree today and 11 do not** | exact integers, summing to 622 | verified against `633e74f4`, 2026-08-02; every file:line was opened | §5's \"fourteen ids across twelve items\" is exactly the complement | A meta-gate asserts every tag is reachable by some fixture, so a tag nothing can produce is as red as a refusal with no tag | `unified_spec_architecture.md:437-450` |\n\n---\n\n## 4. Isolations with no control\n\nThese rows assert a cause but exclude nothing. They are **not established**; each needs a negative control before it can be cited as a mechanism.\n\n| Claim | What is missing | Source |\n|---|---|---|\n| \"ε_LHY/E_MF ≈ 3 % for Eu vs ~2 % Cr (no droplet) and ~5 % Er (droplet), so Eu at a_s = 110 a_B is below the droplet threshold\" | **One significant figure, no error bar, no conditions on the density used, and no source given for the Cr/Er comparison values.** Vague row | `eu_collapse_lhy_insufficient.md:108-124` |\n| F=6 partial-LHY g_eff ranking: I_h 93.92 (winner), biaxial 94.22 (+0.32 %), cyclic 94.24 (+0.34 %), polar 94.46 (+0.57 %), FM 99.55 (+5.99 %) | **No n, no c₀/c₁, no grid, no date, no tolerance** — a 0.32 % separation quoted to 4 s.f. with no convergence statement. The header itself says the right tool is something other than this ranking. See §6 for the FM disagreement | `scripts/sprint5_B_partial_lhy_rank.jl:7-13` |\n| \"The SpatialLHY spin term the propagator omitted is 2.3 % of the gradient norm\" | Conditions not stated in the header — no grid, atom, or date | `test/hamiltonian/test_spatial_lhy_spin_substep.jl:8` |\n| \"True LHY gradient is 628.9 where the code returned 0\" | 4 s.f. reference with **no grid, atom, or commit** beyond the 2026-07-28 window | `test/hamiltonian/test_lhy_gradient_all_modes.jl:6` |\n| \"The padded/unpadded gradient-energy mismatch is worth 2–5 %\" | The 2–5 % is imported from the *field*-error measurement; **the magnitude of the resulting minimiser error was never measured** | `test/hamiltonian/test_ddi_gradient_padding_parity.jl:14` |\n| \"P_C is ~40× worse because the soft mode is diagonal in neither real nor Fourier space\" | The 40× is 1 s.f., one cell, 2026-06-23, never re-derived; the mechanism has been **retracted twice** and the current one, though structurally supported by the rank-1 measurement, has not been tested by building the preconditioner it predicts | sha `d496dd71`; `test/solvers/test_precond_default_is_off.jl:5-24` |\n| \"13 of 22 M1 cells had a disk grad_norm 30–50× off\" — cause | **Deliberately not isolated**; the fix is structural. The class is therefore closed only where that fix reaches | `src/solvers/lbfgs/atomic.jl:1-27` |\n| \"~33 % is the finest-defensible-partition invalidation rate\" (the comparator for the measured 52.7 %) | **No derivation anywhere in either document.** Reads as a measurement and is an estimate | `unified_spec_architecture.md:816-822` |\n| \"The 1.5× between the per-point time sum and the whole-job wall is JIT plus per-point workspace rebuild, analyze and I/O\" | Asserted, never ablated | `parameter_contract_with_Ueda.md:305-317` |\n| \"The 8.5e-6 cutover energy gap traces to `code_tree_hash()` moving allocation alignment, hence the plan, hence ψ at 1e-16, amplified by the config's ε-continuation\" | **The document itself labels this \"a story, not a measurement.\"** The single-variable job exists to turn it into one | `scripts/tsubame/submit_parity_bisect.sh:6-17` |\n| \"Eu F=6 reaches vortices ~50× earlier than Prasad's scalar dipolar prediction\" | Offered with a \"plausible cause\" naming **two** candidates (stronger DDI energy scale at F=6; the simultaneous B quench opening spin channels) and **no control separating them** | `klaus_quench_protocol_pivot_2026-05-26.md:257-280` |\n| \"The TDHFB φ-slope is a static property of the W^φ eigenstructure\" | Empirical only after three dynamic hypotheses were refuted; **not understood at the BCH derivation level**, and the named next diagnostic (per-step local-error decomposition) was not run | `integrator_order_mechanism_tdhfb.md:230-290` |\n| \"The Fig. 4B dip width is set by the spatial distribution of the dipole field\" | The paper's claim; our supporting measurement is a **two-point** hold-time slope (0.28× of 1/T) | `matsui_experiment_parameters.md:79-86` |\n| \"The 47-minute 32³ divergence-sweep row is 35 fresh `make_workspace` calls\" | **Refuted** by per-cell timing (36 s first, 0.1 s after) and by cutting steps 3000 → 800 (no change). **Nothing replaced it** — the 47 minutes is unattributed, and a later 16³/100-step smoke hung 42 minutes on its fourth cell | sha `8645e3f3`, `69a36897` |\n| \"ΔE = 43.4 in the phase-gap reference arm, between states that agree to 1.6e-3\" | Suspicion recorded only: each seed builds its **own** full_bdg table from its own stage-1 state, so ΔE there may be a difference of **tables** rather than of states. Labelled suspected, not established | `bench/phase_gap_error_budget.jl:114-119` |\n| \"Newton at 336 s/iteration is useless on the soft manifold\" | A cost measurement plus a judgement; no accuracy-per-second comparison against the L-BFGS anchor | `scripts/submit_eu_bscan.sh:41-45` |\n| Y4-midpoint drift extrapolated to T = 10⁴ as ~1e-8, used to **exclude** switching to a true energy-preserving (AVF) scheme | The 2.8e-10 at T = 20 is measured; the T = 10⁴ figure assumes drift scales **linearly in T** and was never checked. The exclusion rests on the extrapolation | `integrator_ch3_8_narrative.md:20-43` |\n| TDHFB 32³ (~16 s) and 64³ (~130 s) per-step CPU costs | **Linear extrapolations from voxel count**, labelled as such in the source's own notes column — do not cite as measurements | `tdhfb_gpu_port_design.md:20-38` |\n| \"54 MB for ψ against roughly 2.1 GB of L-BFGS two-loop memory at 64³ × 13\", used to justify the fork-resolution policy | **Quoted from issue #250**, explicitly not measured under that design; the design files this as a mechanism weaker than the requirement asks | `unified_spec_architecture.md:782-786` |\n| \"ccache has 28 named uncacheable counters\" (cited twice in-tree without a version pin) | **Second-hand**: the source states its author did not read ccache's source. External audit measured 20+8 = 28 at v4.9–v4.11 and 21+8 = 29 at master, so the version-free claim is true of one line and false of another | `unified_spec_architecture.md:829-833` |\n| Dashboard `SharedArrayBuffer` rejected | Excluded **by argument, not measurement** — the atlas worker already transfers buffers zero-copy | `dashboard_perf_notes.md:61-71` |\n| Multi-GPU \"~3.5×\" 4-GPU scaling; mixed precision \"~2× throughput, ~1/2 VRAM\" | Both marked **projection/expected**; the multi-GPU bench is listed as future work item 9 | `multi_gpu_design.md`; `mixed_precision_design.md` |\n| Eu evaporation exponents γ ∝ ω̄^{12/5} and attractor ∝ ω̄^{−3} | **The document itself flags them as wanting a joint re-derivation** | `eu_evaporation_three_body_theory.md:1-63` |\n| \"The GPU F32 reduction differs from CPU by orders (tree vs atomic)\" | The CPU pairwise-sum figure (~1.6e-8) is measured; the **GPU** claim has no measurement behind it — re-scoped rather than retracted | `hamiltonian_layered_architecture.md:447-452` |\n| Eu-151 K3 ≈ 1e-41 m⁶/s | Back-solved from a thesis 1/e lifetime of 1.4 s; n₀ depends on a BEC number the thesis does not state, so it is **an order of magnitude only**, consistent with an independent L ≈ 1e-29 cm⁶/s | `eu_evaporation_three_body_theory.md:1-63` |\n\n---\n\n## 5. Unsettled\n\nEach row states the measurement that would settle it and whether it needs compute.\n\n**Needs compute:**\n\n1. **The last 0.037–0.040 nT (≈1.5 %) of the Matsui Fig. 4B centre.** Every candidate has been excluded (§3.1). It is bounded well inside the paper's own ±10 nT axis systematic and ~1 nT fluctuation, so it may not be worth settling. *(`matsui_residual_root_cause.md`)*\n2. **Which side of the DDI kernel treatment is right.** Only the sensitivity is established. Two named candidates survive: their padding may sample the analytic continuum kernel on the padded grid rather than the DFT of the truncated real-space kernel (so it is not image-free, and their box is 36.2 a_ho vs our 16); or their DD0(k=0) = −4π/3 vs our 0, bounded at 5e-3 nT for **their** box but never evaluated for theirs and ours jointly. **Settled by one arm**: reproduce their padded convolution exactly as a fourth kernel mode. *(`matsui_ddi_kernel_sensitivity.md:73-87`)*\n3. **The ~20 % transfer excess**, now formally separate from the centre gap and with its leading candidate (their FD Laplacian) excluded with the wrong sign. *(`matsui_residual_root_cause.md`)*\n4. **The full-SPGPE energy-damping equilibrium failure.** Two candidates with the separating measurement named: (a) seed the Rayleigh-Jeans state at γ=0, where the term conserves N exactly; (b) run at two box sizes, since ε̃(k) = M̄/|k| is a 3D result and diverges logarithmically in 1D. *(sha `ec62374d`)*\n5. **SPGPE equilibrium number vs the closed form (6.7×).** Discriminator specified: remove the trap, making the Rayleigh-Jeans prediction a plain mode sum with no LDA in the way, and evaluate both cutoff conventions against the same run. Not done. *(`test/dynamics/test_spgpe_equilibrium_number.jl`)*\n6. **The cross-process fused spin-chain discrepancy (~1e-9 relative, observed once).** Seven mechanisms excluded, each with a positive control. **Untried and listed by the source**: eleven concurrent SpinorBEC processes doing FFT and DDI work on one device — not eleven GEMM loops. *(`test/oracles/test_spin_chain_fusion_parity.jl:66-112`)*\n7. **True order of MPS-{6,8} and of Y6-midpoint** — both floor-limited. Needs a Y6-mid reference at dt ≤ 2e-6. *(`integrator_architecture_completion_plan.md:180-196`)*\n8. **Whether the adaptive controller helps on a real near-instability.** Test 4.2's synthetic pulse is not stiff. The decisive test is Eu post-quench at F=6, T=1.0, ~30 min for the fixed-dt baseline alone. Not run. *(`integrator_architecture_completion_plan.md:285-360`)*\n9. **DDI's k-structure in the BdG anchor.** Q(k=0)=0 means the k=0 matrix anchor cannot probe it; a finite-k FD-Hessian is the follow-on. *(`hamiltonian_layered_architecture.md:417-445`)*\n10. **Beyond-mean-field correction to f_s for a dipolar supersolid**, and the LDA validity of LHY across a modulated droplet state (assumed by both us and the reference, in a state whose density varies on the healing-length scale). Only literature calibration point is solid-⁴He at ~20×. *(`dipolar_supersolid_tube.md`)*\n11. **A cylindrically truncated dipolar kernel** to remove the residual 1/L_t² tail in absolute E_dd (no closed-form Fourier transform). *(`dipolar_supersolid_tube.md:181-201`)*\n12. **c₁ < 0 with c₀ > 0 — the actual Eu production side — has never been run through the ITP fusion / lhy=none benches.** Everything measured at \"c₁ < 0\" in that thread was at negative c₀. *(sha `f03b5b3f`)*\n13. **The phase-gap two-stage design confound**: both seeds run through a common LHY-free stage 1, so if stage 1 already collapses them, \"0 distinct\" says nothing. Separation is now recorded at seed, end-of-stage-1 and end-of-stage-2 — the numbers exist but the verdict has not been re-taken. *(`bench/phase_gap_error_budget.jl:35-119`)*\n14. **The honest tolerance for the F=6 LHY closed-form vs full_bdg gate.** The sweep design (fix physics, sweep quadrature; a plateau *is* the honest gate) is specified; **its outcome is not recorded in the file.** *(`bench/a4_lhy_closed_form_residual.jl:1-29`)*\n15. **The GPU-ratchet 32³ anchor has never been measured** — `best.json` holds only N128 keys. Extrapolating by cell count is unsafe because 64³ already runs at only 83–85 % device-busy. The whole D1a cost estimate rests on it. *(`scripts/tsubame/submit_gpu_ratchet_n32.sh:392-403`)*\n16. **The batched-rFFT question cannot be answered by its own profiler** — `profile_ddi_convolve.jl`'s 3×/1× discriminator is contaminated by an unamortised sync on one arm. `bench/micro_batched_rfft.jl` was written to settle it; result not recorded. *(`bench/micro_batched_rfft.jl:10-16`)*\n17. **The sign of d⟨F_y⟩/dt in the combined-spin-step fixture** — wrong sign, 16× the threshold, shared by both paths, and the test's analytic premise accounts for none of the other active terms. The one-term-at-a-time probe exists; result not recorded. *(`bench/probe_transverse_zeeman_dfy.jl`)*\n18. **Whether the L-BFGS soft mode's λ₁ is isolated or in a cluster.** Rank-1 deflation is free against a 25 ms iteration but works only if λ₁ is isolated — κ after deflation is µ_max/λ₂. The author records having \"twice skipped and twice been wrong about by ~4×\" exactly this sizing step. *(`bench/probe_lbfgs_soft_mode_count.jl:6-25`)*\n19. **What made a 32³ LHY divergence-sweep row take 47 minutes.** Explicitly unattributed after the leading hypothesis was refuted. *(sha `69a36897`)*\n20. **The Eu torque protocol's pre-registered verdict** (does J_z leave −3.15 toward −1.09 while E falls toward 10.73). Target fixed 2026-07-28; not answered. *(`scripts/eu_torque_protocol.jl`)*\n21. **The FORTRESS spin-1 cross-check** — scoped at 4–8 h install with a ≤1 % agreement target on energies and populations; **never executed**. *(`fortress_cross_check_scope.md:1-89`)*\n22. **The M1 disk-grad_norm mechanism**, never isolated by design; the class is closed only where the structural fix reaches. *(`src/solvers/lbfgs/atomic.jl`)*\n23. **Whether the M1 Ω>0 floor hides a soft mode at the unconverged transition cells themselves** — every *measurable* converged neighbour is gapped, and the document says so. *(`m1_omega_conditioning_floor.md:1-36`)*\n24. **The Dy164 Y6 ε=1e-3 frozen result** (\"may be physical — Dy F=8 is in a deeper adiabatic regime — but needs ε=1e-6 confirmation\"). That confirmation was in flight at write time and **no resolution appears anywhere in the corpus.** *(`thesis_batch_audit_2026-04-28.md:42-44`)*\n25. **Whether the m=−4 ring 2↔3 bifurcation appears at all.** At T=14.5 ms on 16×16×12, all three c₁/c₀ ∈ {0, 1/36, 1/18} return 2 rings. Three candidate causes listed (full 40 ms; finer radial grid; m-dependent K3) and **none tested**. *(`sprint3_static_gate_baseline_2026-06-01.md:262-294`)*\n26. **The 3D texture-vs-uniform question.** A1 v3 closes it in 2D by kinetic frustration; 2D quasi-2D undercounts axial textures (axial circulation, 3D skyrmion winding, axial flux closure). *(`north_star:693-800`)*\n27. **The whole R32–R39 Phase-2 measurement campaign was never run at Eu scale.** Every \"measured\" column reads TODO; all \"previously measured\" entries are synthetic or F=1 proxies (MFBO 3.11× on an F=1 pair; pseudo-arclength residual <1e-7 on synthetic 2D shapes; active learning 66.7 % vs 40 % on synthetic 2D; triple-point 21/21 on a synthetic equilateral). The projected 70× for pseudo-arclength is an **estimate**. *(`MEASUREMENT_CAMPAIGN_PHASE2.md:13-136`)*\n\n**Does not need compute (reading, grepping or arithmetic only):**\n\n28. **Write amplification of per-attempt records** — the executions-per-artifact mean is unmeasured; if it is ≥3 the index needs sharding and \"rebuildable and safe to delete\" has to go. A design commitment made ahead of the measurement that should precede it. *(`unified_spec_architecture.md:703-707`)*\n29. **Whether UGE delivers a catchable signal at the h_rt boundary.** `kill -INT` is verified uncatchable in Julia script mode; the UGE half is unmeasured, and the tombstone plus a dated bound make the answer safe either way. *(`unified_spec_architecture.md:711-715`)*\n30. **TWA ensemble divergence contamination.** `grep -n isfinite src/solvers/twa.jl` returns **nothing**, and 33 configs use `twa:`; `_welford_update!` is in-place and cumulative, so one diverging member NaNs the mean for every later member. Named but not built. *(`unified_spec_architecture.md:716-721`)*\n31. **`R-VERDICT-TRUTH`**: no gate checks that a stored verdict is *true*. Forcing `converged = true` at the writer leaves **all 168** marker assertions green. The candidate fix (recompute grad_norm from the stored ψ) is cheap relative to the solve it certifies and is **not implemented**. *(`research_spec_and_provenance_architecture.md:966-984`)*\n32. **Design §5 open ids (8 of 14 remain)**: fork-resolution costs quoted rather than measured; trusted producers (`require` is verdict-shaped, not host-shaped); within-build dedup (the frontier releases up to 64 stages); no LaTeX check over prose (the gate domain is code); the name-budget violation. *(`unified_spec_architecture.md:612-836`)*\n33. **The `with_reference_accuracy` degeneracy is pinned, not fixed** — fixing it turns the testset red on purpose. *(sha `839dea8a`)*\n34. **The L7 `interactions` key-precedence defect is left unfixed on purpose** — fixing it changes what those runs compute and needs its own gate plus a retraction of any L7 claim. *(`research_spec_and_provenance_architecture.md:674-685`)*\n35. **The two `B_direction` configs still run with B along +z instead of −z.** *(`research_spec_and_provenance_architecture.md:687-691`)*\n36. **The ~880 stored jld2 payloads have never been audited at all** — the vintage audit covered `summary.json` only, and says so. *(`stored_results_vintage_audit.md`)*\n37. **Whether the KZ number-damping reproduction survives the local-slope test** the global-fit retraction introduced. Dropping the four τ_Q points outside the measured scaling regime leaves β = 0.128 ± 0.029, which is 2.1 σ from the published full value and 1.2 σ from the number-damping one — **it does not distinguish them.** *(sha `ec083957`)*\n38. **`SPIN_TAYLOR_TOL` freeze proposal (#271)**: its own measurement (rel|dψ| = 6.46e-16) establishes the tolerance does not move the **observable**, not that it does not move the **degree** — and the degree is 5–9 at production R, nowhere near the floor. *(sha `826fca0d`)*\n\n---\n\n## 6. Contradictions found\n\nI looked for these three ways: by cross-reading the same number where it appears in more than one harvest source (commit message vs test header vs design doc vs research note); by checking every arithmetic identity a document states about itself (sums, per-suite totals, row counts vs header counts); and by comparing figures that ought to be the same quantity under different names. Fifteen are listed. **This is not exhaustive** — the corpus is ~150 documents and I read them once.\n\n| # | The disagreement | Both citations | What to do |\n|---|---|---|---|\n| 1 | **671 `.jld2` under `runs/` with zero markers** vs **0 `.jld2` exist under `runs/`** — in the **same document** | `research_spec_and_provenance_architecture.md:484` vs `:772` | Almost certainly a checkout-scope difference (payload counts are from the **main checkout**; the design was written on a worktree), stated once at `:28-30` and repeated at **neither** use site. **Neither number is citable alone.** |\n| 2 | **429** committed configs vs **446** configs under `runs/` | `research_spec_and_provenance_architecture.md:26-84` vs `unified_spec_architecture.md:674-702` | Different scopes: committed vs everything on disk. Quote the scope. |\n| 3 | **15** module-level `Ref`s under `src/`+`ext/` vs **20** `const … = Ref(…)` bindings | step-4 scanner (`research_spec:774-790`) vs `research_spec:26-84` | A pattern-scope difference (`Ref(` / `Ref{` / `Base.RefValue` vs `const … = Ref(…)`) that **neither site reconciles**. |\n| 4 | Store invalidation **52.7 %** (at `bc23d150`) vs **53.4 %** (at branch HEAD) vs a loose **\"53 %\"** | `unified_spec_architecture.md:816-822`, `:834-836`, `:340` | Both measured figures are true of their revision; quote the revision. The bare \"53 %\" should not be cited. |\n| 5 | `config_metadata_blocks.toml` **prose header** says 302 configs / 2,107 physical lines / 59 distinct keys and 4,673 comment lines; its own `[dump]` **table** says 321 / 2,225 / 60, and the file carries **321** `[[block]]` rows | `config_metadata_blocks.toml:14-16` vs `:61-66` | The machine tables are self-consistent (per-suite `configs_read` 31+35+45+57+102+121 = 391 and per-suite items 100+32+70+32+32+34 = 300 both match the row counts), so **the prose headers are the stale half**, undercounting by 19 configs, 118 lines, 1 key and 674 prose lines. Both are quotable and only one is right. |\n| 6 | Reference-RHS **DDI tolerance**: \"< 1e-10 relative\" vs \"1e-6..1e-8 (grid/cutoff bound)\" | `self_contained_validation_report.md:71` vs `ueda_status.md:100` | Neither states which is **achieved** and which is **accepted**. The gating test exists and would settle it in seconds. |\n| 7 | **MPS-4 order on the lab path**: \"1 (collapse) … structural failure persists even with Track A1 midpoint plumbing\" vs measured **3.20** with the midpoint kernel | `integrator_ch3_8_narrative.md:17, 94-99` (2026-05-11) vs `integrator_architecture_completion_plan.md:181-196` (2026-05-12) | **One day apart.** The completion plan is both the later and the measured one; the narrative was never updated and still lists MPS-4 under \"Methods to AVOID\". |\n| 8 | **Fig. 4B width \"target\"**: six configs quote **15.0224**, `test_matsui_fig4_dip.jl` pins **14.5414**, a design example quotes **12.84** | `research_spec_and_provenance_architecture.md:806-825` | All three are correct measurements of the same curve over **different windows**. A live instance of the drift the `refs/` TOML layer exists to remove — and the cutover instruction to populate it \"from the numbers already pinned in the test\" was itself wrong. |\n| 9 | **The retracted Ntot factor-2 / 34 % claim is still asserted in the tree** | retraction at `research_spec_and_provenance_architecture.md:847-857`; live assertion at `matsui_reproduction_status.md:22-25` | That file is a live trap. The correct figure is **10/7** (4687.266 vs 3281.086). |\n| 10 | **Round-8 vs Round-9 GPU speedup for the same span**: 1.51× / 1.66× vs 1.38× / 1.47× | `bench/perf_targets.txt:68-78` vs `:96-116` | Cross-tree instrument drift; the file's own note says prefer Round 9. Round 8's post-#142 numbers *do* reproduce, so only the pre-#142 baseline was drifted. |\n| 11 | **Eu FM effective coupling: 188.91 (+100 %) vs 99.55 (+5.99 %)** — while the other four states agree to the digits printed (I_h 93.9209 vs 93.92; biaxial 94.2209 vs 94.22; cyclic 94.2371 vs 94.24; polar 94.4513 vs 94.46) | `north_star_phase_diagram_plan_2026-06-02.md:497-541` vs `scripts/sprint5_B_partial_lhy_rank.jl:7-13` | Different quantities under similar names (mean-field inert ranking vs a partial-LHY effective coupling), but **four values agreeing to four figures and the fifth differing by 2× is the shape of a copied table with one column recomputed**. The second script states no conditions at all, so it cannot be adjudicated as it stands. |\n| 12 | **Campaign first-point JIT: ~240 s (40 % of the job) vs 277 s** — in the same document | `where_the_campaign_time_goes.md:9-31` vs `:77-116` | Minor, but both are quoted as the JIT figure and the document does not reconcile them. |\n| 13 | **Bogoliubov dω/dB: \"up to 195 Hz/nT\" vs \"≈17 Hz/nT\"** | `sprint3_static_gate_baseline_2026-06-01.md:296-320` vs `scripts/sprint5_bogoliubov_dB_sensitivity.jl:4-14` | Different mode sets and different fields (stretched Bogoliubov modes vs Matsui's 2.7 nT / 45 Hz Larmor point). Both are used to argue the **same** conclusion — that the σ_freq = 0.1 Hz assumption is optimistic — so the conclusion is safe and the number is not transferable. |\n| 14 | **The 2×4 K3 × LHY factorial's fourth column**: icosa −5.74686 / −2.29143 vs full_bdg −5.75017 / −2.34171 | sha `27432c45` (#197) vs `day_inventory_2026_05_26.md:66-177` | Two regenerations at different times — icosa **before** the I_h refusal at c₁ < 0, full_bdg **after** it. Both are labelled as the same eight-cell table. |\n| 15 | **The unpadded DDI field error: 1.27e-1 (L2 ratio vs pad 3) vs 2.1e-2..4.7e-2 (free-space reference, field-magnitude measure)** | `bench/ddi_pad_factor_accuracy.jl:1-21` vs commit `9c117c05` | **Flagged in the source itself** as the same phenomenon on different meters, with an explicit instruction not to quote one as reproducing the other. Included here because that caveat lives only in one file's header. |\n\n**Near-misses I checked and found consistent:** the two M1 Lanczos niter sequences (7.6@12 / 1.35@30 / 0.72@40 and 1.34 → 0.048 by 200) agree at their overlap; the two F32 buffer measurements (0.1152 vs 0.5000, and the per-term 32³ Na23 table) are the same defect on different fixtures; the two per-point campaign step costs (~1.5 ms at 32³ from job records, 0.604 ms/step from the ablation) differ because the first is per-point wall over 3456 steps including setup and the second is stepping only, and the documents say so.",
    "counts": {},
    "topics": {},
    "total": 0
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Harvest"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Reconcile"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Ledger"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "harvest:commits",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "a1a08d54ab65ac6ac",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202304,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785808479545,
      "tokens": 366312,
      "toolCalls": 40,
      "durationMs": 2277240,
      "resultPreview": "{\"records\":[],\"notes\":[\"!! SCHEMA CONFLICT — READ FIRST. The `records` array could not be used. Its item schema lists `conclusion` in `required` while `additionalProperties: false` permits only {topic, what, number, cause_isolated, instrument, precision, conditions, ruled_out, source, status}. Including `conclusion` fails with 'must NOT have additional properties'; omitting it fails with 'must hav…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "harvest:test-headers",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "a182fb8aff5a12713",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202305,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785807858035,
      "tokens": 342544,
      "toolCalls": 39,
      "durationMs": 1655730,
      "resultPreview": "{\"records\":[],\"notes\":[\"SCHEMA CONFLICT — READ FIRST. The `records` item schema lists `conclusion` under `required` but does not define it under `properties`, while `additionalProperties` is `false`. Including `conclusion` fails with 'must NOT have additional properties'; omitting it fails with 'must have required property'. Both were attempted and both were rejected, so `records` is emitted empty…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "harvest:validation-docs",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "ac1d6adbc3142165f",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202306,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785808186657,
      "tokens": 369695,
      "toolCalls": 39,
      "durationMs": 1984351,
      "resultPreview": "{\"records\":[],\"notes\":[\"SCHEMA NOTE — WHY `records` IS EMPTY: the `records` item schema is unsatisfiable as given. `conclusion` appears in `required` but not in `properties`, while `additionalProperties` is `false`. Submitting with `conclusion` fails 'must NOT have additional properties' on all 68 rows; submitting without it fails 'must have required property conclusion' on all 68. Both were attem…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "harvest:research-notes",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "abdb3ef3a1e900361",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202306,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785808842354,
      "tokens": 366548,
      "toolCalls": 47,
      "durationMs": 2640048,
      "resultPreview": "{\"records\":[],\"notes\":[\"SCHEMA IS UNSATISFIABLE — records[] is empty for that reason, not because nothing was found. `conclusion` is in the tool's `required` list but not in its `properties`, and `additionalProperties:false` rejects it. Probed with a minimal 1-record payload carrying exactly the 5 required keys: still 'must NOT have additional properties'. Every row below is a record; format is TO…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "harvest:design-open-rows",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "ae82aecd2132e8ebf",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202307,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785808191757,
      "tokens": 309477,
      "toolCalls": 57,
      "durationMs": 1989450,
      "resultPreview": "{\"records\":[],\"notes\":[\"SCHEMA FAILURE, READ FIRST: `records` is empty NOT because there are no records, but because this tool's schema is unsatisfiable — it lists `conclusion` in `required` while omitting it from `properties` under `additionalProperties: false`. Three submissions were rejected (with conclusion: 'must NOT have additional properties'; without it: 'must have required property conclu…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "harvest:memory-store",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "acd6ecb746f4cb620",
      "model": "claude-opus-5[1m]",
      "state": "error",
      "startedAt": 1785806202307,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/suzume/.claude/projects/-home-suzume-workspace-BEC-si…",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785809118786,
      "error": "API Error: Claude's response exceeded the 64000 output token maximum. To configure this behavior, set the CLAUDE_CODE_MAX_OUTPUT_TOKENS environment variable.",
      "tokens": 422647,
      "toolCalls": 161,
      "durationMs": 2916479
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "harvest:bench-scripts",
      "phaseIndex": 1,
      "phaseTitle": "Harvest",
      "agentId": "afde305715430d6d2",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785806202307,
      "queuedAt": 1785806202293,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785807962454,
      "tokens": 411358,
      "toolCalls": 43,
      "durationMs": 1760146,
      "resultPreview": "{\"records\":[],\"notes\":[\"SCHEMA NOTE — READ FIRST. `records` is empty NOT because nothing was found (77 records were assembled) but because the records item schema is self-contradictory and cannot be satisfied: `conclusion` is in `required` but absent from `properties`, and `additionalProperties: false` is enforced. Verified in two passes — including `conclusion` returns 'must NOT have additional p…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "ledger",
      "phaseIndex": 3,
      "phaseTitle": "Ledger",
      "agentId": "af649b2f0a4c819b7",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785809118790,
      "queuedAt": 1785809118789,
      "attempt": 1,
      "promptPreview": "Repository: /home/suzume/workspace/BEC-simulation/.claude/worktrees/purring-chasing-phoenix (git worktree at current origin/main + 2). Run everything\nfrom there; do NOT cd to the parent repo.\n\nREAD-ONLY, and NO SIMULATIONS. Do not re-measure anything. The point is that a\ngreat deal of measurement and cause-isolation has ALREADY been done in this\nproject and is scattered across commit messages, tes…",
      "lastProgressAt": 1785810751960,
      "tokens": 461947,
      "toolCalls": 0,
      "durationMs": 1633170,
      "resultPreview": "is fully determined. The earlier `evap_scale ≈ 0.02` fit is superseded; the residual full-trajectory mismatch is the unknown real ramp and `gravity_factor`, not a hidden knob | The implemented rate factor (η P₂ − 3P₃)/P₂² is the **2D** form (a=2), reducing to (η−3)e^{−η}; the 3D harmonic form is a=3 → (η−4)e^{−η}. That form goes **negative below η = 4**, making the model **gain atoms and heat** at…"
    }
  ],
  "totalTokens": 3050528,
  "totalToolCalls": 426
}
```
